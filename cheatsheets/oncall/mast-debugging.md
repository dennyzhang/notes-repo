# MAST Job Deep Debugging Cheatsheet

> **CLI-first**: All commands below use `meta ai.mast-job` CLI (5-15x faster than MCP). Fall back to MCP tools only if CLI output is insufficient.

## Debugging Methodology: Hypothesis-Driven (Source: Agans' "Debugging" + 5 Whys)

**Never start by reading logs.** Start by forming hypotheses.

```
1. OBSERVE   → What are the symptoms? (error, metrics, timeline)
2. HYPOTHESIZE → List 3 possible causes, rank by likelihood
3. TEST      → For top hypothesis, what evidence confirms/refutes it?
4. ACT       → Confirmed → fix. Refuted → next hypothesis. 3 fails → re-observe.
```

### Common MAST Failure Hypotheses

| Symptom | Likely cause | Test command |
|---------|-------------|-------------|
| FAILED immediately | Bad package/config | `meta ai.mast-job error --name=JOB` |
| FAILED after running | OOM or data issue | `error` + `meta ai.mast-job insights --name=JOB` |
| DEAD (killed) | ML Guardian or TTL | `meta ai.mast-job scheduling --name=JOB` |
| Stuck PENDING | No capacity | `scheduling` (check PENDING duration) |
| Performance drop | Data quality or config drift | `meta ai.mast-job system-metrics --name=JOB` |

### IS / IS-NOT Table (Source: Kepner-Tregoe)

When a failure is selective (some jobs fail, others don't), fill this out:

| Dimension | IS (fails) | IS NOT (works) | Distinction |
|-----------|-----------|----------------|-------------|
| **What** | Model X fails | Models Y, Z train fine | Model X uses feature group G |
| **Where** | Fails on cluster A | Works on cluster B | Cluster A has different NCCL |
| **When** | Started failing Tuesday | Worked Monday | Package push Monday night |
| **Extent** | All runs fail | Not intermittent | Rules out hardware flakiness |

The "Distinction" column points to root cause. Fill it out before deep-diving into logs.

### ML Training Failure Quick Reference

| Category | First check | Common fix |
|----------|------------|------------|
| OOM | batch_size, model size, activation memory | Reduce batch_size, enable gradient checkpointing |
| Loss NaN/plateau | learning rate, data pipeline, gradient norms | Reduce LR, verify real data flowing |
| NCCL timeout | network, straggler nodes, NIC errors | Check all ranks alive, restart on healthy nodes |
| Data stall | partition availability, upstream Hive tables | Verify data landed, check dataloader workers |
| Checkpoint corruption | file integrity, partial writes | Load and inspect tensors, use last-known-good |
| Stale model | SV rejections, Conveyor pipeline | Check SV logs, verify publish path |

## Fact-Gathering Signature Catalog — the OBSERVE probe set (run FIRST on zombie / QPS→0 / hang / FAILED)

`OBSERVE` (step 1 of the methodology) is not "skim whatever log you happened to
fetch." It is running a **fixed probe set** and recording which signatures fire —
*then* you hypothesize over real evidence. A triager (human or agent) that reasons
over un-probed text will confidently miss the root cause: the line that proves it
was never fetched.

> **Origin — S670887 (2026-06-03).** A GPU **CUDA-OOM** zombie was first mis-triaged
> (one pass correlated it to an unrelated IMA SEV; the cron monitor listed OOM only as
> an *unconfirmed candidate*) because **nobody probed GPU memory** — the 99.88%-mem
> signal and the `SIGTERM` line were never in the fetched text. The fix is not better
> reasoning; it is running probes #1–#2 every time.

### Step 0 — known-issue / history match (run BEFORE the probe set)

A recurring symptom is **not** a novel investigation. For zombie / QPS=0-while-RUNNING / "RUNNING but not training", FIRST match against history — it yields the canonical root + the existing mitigation + the fix diff in ONE step, and the probe set below then only CONFIRMS it applies (don't re-derive from logs).

- **Prior SEVs of this class:** S665454, S670887.
- **The canonical root for the OT "RUNNING-but-zombie" class:** a worker crashes (e.g. `CUDACachingAllocator INTERNAL ASSERT FAILED`/`free_block` → `terminate()`/SIGABRT, or CUDA-OOM) and SJD *catches* it — **but a base-layer bug prevents the process from exiting cleanly, so MAST never registers the failure and creates no new attempt** (even with 100 retries configured). End state: MAST=RUNNING, QPS=0, no auto-restart. **Permanent fix: base layer ≥ [D98638473](https://www.internalfb.com/diff/D98638473)** (clean exit on worker crash). **Mitigation now: manually kill the job → MAST creates a new attempt and resumes.** MAST/SJD/TMS will NOT self-recover a zombie (confirmed by the model platform — Li Lu, 2026-06-09).
- **Runbook:** mrs.ot post `1344542434307166` (Denny, 2026-06-06) — canonical write-up + actions.
- **It's a CLUSTER, not one job.** These arrive as N root models at once (2026-06-09: 3 — 2121054758, 2120474184, +1). Pull the full affected-model list first; one diagnosis covers the cluster.

**Confidence guard:** do NOT tag root-cause confidence "high" until Step 0 (history) AND probe #1 have both run. Over-fitting to the most-frequent log lines (e.g. background `STORAGE_SVC_PUT_CHUNK RESOURCE_BUSY_TIMEOUT` storage noise) and naming them the root — without confirming the actual crash signature (probes #3/#4) or checking history — is the exact miss Step 0 prevents. (2026-06-09: a deep triage of 2121054758 rooted on storage timeouts at "high" confidence; the real root was the base-layer exit bug, already documented 3 days earlier in the runbook above.)

### The probe set (run all — each is cheap; record fired/not-fired)

| # | Class | Probe (runnable) | Signature = hit |
|---|-------|------------------|-----------------|
| 1 | **Trainer liveness — progress ≠ heartbeat** | `meta scuba.dataset query -d mvai_metrics -a count -w '[{"column":"model_entity_id","op":"eq","values":["<MID>"]}]' --hours=18 --time-bucket="30 minutes"` | emission → **0 while MAST=RUNNING** ⇒ zombie. THE defining signal; a stuck-job detector watching heartbeat/lease never sees it. |
| 2 | **GPU memory / OOM** | `meta scuba.dataset query -d gpu_dyno_stats -a "max(gpu_memory_utilization_float)" -w '[{"column":"model_entity_id","op":"eq","values":["<MID>"]}]' --hours=18 --time-bucket="30 minutes"` (add `host_name` group-by to find WHICH GPU) | any GPU **>99% mem util** ⇒ CUDA OOM occurred/imminent. **The probe missed in S670887.** |
| 3 | **Rank death / SIGTERM** | `meta ai.mast-job error --name=JOB --version=V --no-truncate` (+ logs) | `SIGTERM`, `Signal 15`, `ChildFailedError`, `exited with code`, `Fatal Python error`, `terminate called`. Also: a top-level `error` of "killed by user … stuck" ⇒ a human, not auto-recovery, ended it. |
| 4 | **CUDA OOM (log)** | logs grep | `CUDA out of memory`, `torch.cuda.OutOfMemoryError`, `CUDA error: out of memory` |
| 5 | **CUDA IMA** | logs grep | `an illegal memory access was encountered`, `CUDA error: an illegal memory access`, offending kernel (e.g. `permute_multi_embs_kernel`) |
| 6 | **NCCL collective hang/timeout** | logs grep | `Watchdog caught collective operation timeout`, `NCCL timeout`, `ProcessGroupNCCL`, `has not joined`, `operations have failed or timed out` |
| 7 | **Publish-path GPU accumulation** | logs grep + correlate GPU-mem(#2) climb vs publish events | `full snapshot`, `snapshot publish`, `PublishRateLimiter`, `push://`. S670887: GPU mem steps up at each **full-snapshot publish** (publish reads GPU→snapshot, holds allocations); `empty_cache` before publish did **not** free it. |
| 8 | **Stuck-job-detector blindness** | check SJD / lease-renewal thread | training thread stuck **but** lease-renewal thread independently renews `mvai_monitor` ⇒ SJD never fires ⇒ no auto-kill. (S670887's "why wasn't it auto-killed.") |
| 9 | **GIL / Python deadlock** | py-spy dump per rank | `_preload_item_pool`, non-daemon thread hang, all threads parked on one lock |
| 10 | **Loss NaN/Inf** | logs grep | `loss is nan`, `NaN`, `Inf detected`, `assert .*finite` |
| 11 | **Host OOM-killer** | dmesg / host logs | `Out of memory: Killed process`, `oom-kill` |
| 12 | **DPP starvation (is the trainer data-starved?)** | PRIMARY: per-mast-job DPP starvation ODS `fburl.com/canvas/pzoyunzx` (filter by mast job id) + scribe token ingress Scuba `dpp_stats_v2`. CORROBORATE: trainer INFO log `DataClient.cpp "Current output queue size is: N"` (pull INFO logs, NOT `fetch_mast_job_errors`). | ODS starvation high / scribe ingress dropped ⇒ DPP starvation (upstream data) ⇒ hand DPP/scribe oncall the per-job ODS as evidence. Output-queue persistently 0 corroborates; **but a single short queue snapshot can look fed during a REAL fleet starvation (2144239965 in S674219) — trust the per-job ODS/ingress trend, not one sample.** S674219 (2026-06-10): EAG scribe drain → ~75% DPP ingress drop → fleet OT QPS=0; tracked S674227, fix D94521578. |

### Reading the result
- **Exactly one fires** → confirmed root cause; cite the probe output verbatim (self-reporting-from-data).
- **Several fire** → order them into ONE causal chain, don't report N root causes. Canonical zombie chain (S670887):
  `#7 publish-path leak → #2 GPU 99.88% → #4 CUDA OOM → #3 SIGTERM on the rank → #6 79 ranks block on collective → #1 QPS=0 while RUNNING → #8 SJD blind → ~2h until human kill`.
- **#1 true but #2–#11 all false** → genuine silent hang → escalate to #9 (py-spy) + flight recorder. Do **not** write "root cause unknown; candidates OOM/GIL/race" until #2 and #9 have actually been run — that string is the smell of a skipped probe set.

## Performance Regression — pre-SEV early indicators (gradual, not a failure)

A model can degrade *without* failing: training **example age** creeps up,
**training QPS** drifts down, **QPS startup / age-drain after restart** gets
slower. None of these trip a SEV on their own, but they are the leading edge
of one (the S670887 OOM zombie was preceded by days of "example age high,
slow to drain"). Catch them as **drift vs the model's own baseline**, not vs
an absolute threshold — every model's healthy QPS/age is different, so a global
number either false-alarms or misses.

### Detection principle — baseline-relative + slope
- Baseline = the model's own trailing **median ± MAD** over ~7–14d of healthy
  operation (robust to spikes). Compare current to *that*, per model.
- **Trend beats instantaneous** for early warning: a sustained positive slope
  toward the SEV threshold is the signal, even before the threshold is hit.
  (A flat-but-elevated value is steady-state; a slow ramp is the warning.)
- Flag rules (starting points, tune per model):
  - example age > 2× baseline sustained >30min, OR positive slope over 6h;
  - window QPS < 70% baseline sustained >1h;
  - post-restart age-drain time > 2× baseline (e.g. 5h vs ~1h);
  - GPU mem-util positive slope over 6h (leading indicator of the OOM path).

### Runnable signals (verified metric sources)
```bash
# Example age (freshness) — dedicated column, seconds
meta scuba.dataset query -d mvai_metrics -a p50 -a p90 -c data_age_at_publish_secs \
  -w '[{"column":"model_entity_id","op":"eq","values":["<MID>"]}]' --hours=48 --time-bucket="1 hour"
# Training QPS — window (current), not lifetime (smoothed)
meta scuba.dataset query -d mvai_metrics -a avg -c metric_value \
  -w '[{"column":"model_entity_id","op":"eq","values":["<MID>"]},{"column":"metric_name","op":"eq","values":["qps/global/window/train"]}]' --hours=48 --time-bucket="1 hour"
# GPU mem slope (leading indicator) — see catalog probe #2 (gpu_dyno_stats)
# Startup ramp — RUNNING transition (state-transition tool) → time until age/QPS back to baseline
```

### Debug decision matrix — which signal moved × co-signal → subsystem
| Primary | Co-signal | Likely subsystem | Next probe |
|---------|-----------|------------------|-----------|
| Example age ↑ | QPS normal, GPU normal | **Upstream / publish pipeline** (scribe volume, Hedwig backpressure, delta/snapshot publish lag) — trainer fine, freshness path behind | scribe lag, Hedwig queue waits, publish cadence (S670887's "5h to drain" 2nd symptom) |
| Example age ↑ | QPS ↓ | **Trainer can't keep up** (GPU-mem throttle, straggler rank, PT2 recompile, contention) | GPU-mem slope (#2), per-rank QPS variance, `mvai trace-doctor analyze -j <job>` |
| QPS ↓ | GPU mem ↑ slope | **Memory pressure / leak** — same path that ends in the OOM zombie; this is the early warning | gpu_dyno_stats slope; trace-doctor embedding/batch analyzers |
| QPS ↓ | GPU util ↓ | **Input-bound / data stall** (DPP readers, dataloader, partition availability) | trace-doctor critical-path; T1/DPP worker counts |
| Slow QPS startup / slow age-drain | (post-restart) | **Cold-start cost** (checkpoint/snapshot load, sharding replan, cache warmup, backfill catch-up) | compare RUNNING→steady time vs baseline; checkpoint-load duration |
| QPS sawtooth ~every 6min | aligned to publish | **Publish blocks training thread** (full-snapshot/delta publish stalls the train step) | correlate QPS dips to publish timestamps (S670887: "qps drops every 6min aligned with delta publish") |

**Reuse TraceDoctor for the throughput side:** `mvai trace-doctor analyze -j <job-id>`
is the first stop for any QPS-down regression — it auto-detects efficiency
issues (batch size, embedding comm, activation memory, input-bound) with impact
estimates, so don't hand-derive what it computes.

## Quick Reference — The Debug Ladder

When debugging a MAST job, go **top-down** through this ladder. Each level gives you more detail.

```
Level 1: WHAT happened?     → metadata + error
Level 2: WHEN did it happen? → scheduling (state transitions)
Level 3: WHY did it happen?  → insights + error-context + logs
Level 4: HOW to fix it?     → system-metrics + memory + stragglers + profiling
```

## Level 1: What Happened

```bash
# Job overview — status, hardware, package, owner, oncall
meta ai.mast-job metadata --name=JOB_NAME -o json

# Error message + stack trace (latest attempt by default)
meta ai.mast-job error --name=JOB_NAME -o json

# Specific version/attempt
meta ai.mast-job error --name=JOB_NAME --version=5 --attempt=2
```

## Level 2: When Did It Happen (State Transitions)

```bash
# Full scheduling timeline: PENDING → RUNNING → FAILED with timestamps
# Shows time spent in each state — catches scheduling delays, preemption, stuck jobs
meta ai.mast-job scheduling --name=JOB_NAME -o json

# List all attempts with status, timing, cluster info
meta ai.mast-job attempts --name=JOB_NAME -o json
```

**What to look for:**
- Long PENDING time → scheduling/capacity issue
- Short RUNNING before FAILED → crash on startup (config, package, entitlement)
- Multiple attempts → preemption or flaky infrastructure
- RUNNING → SHUTTING_DOWN without FAILED → killed externally (ML Guardian, user, TTL)

## Level 3: Why Did It Happen

```bash
# Combined error context: source failures (scheduler) + AITO pipeline classifications
meta ai.mast-job query-error-context --name=JOB_NAME -o json

# AI-powered insights: DPP starvation, GPU memory, stuck jobs, stragglers
meta ai.mast-job insights --name=JOB_NAME -o json

# ML Guardian compliance — was it auto-killed by policy?
meta ai.mast-job mlg-decision --name=JOB_NAME -o json

# Classify an error message into a specific type (GPU_OOM, NCCL, etc.)
meta ai.mast-job classify-error --error-message="RuntimeError: CUDA out of memory"

# Fetch logs (stderr by default, task 0, latest attempt)
meta ai.mast-job logs --name=JOB_NAME

# List available log files
meta ai.mast-job logs --name=JOB_NAME --list-files

# Specific task/file
meta ai.mast-job logs --name=JOB_NAME --task-id=1 --file-path=stdout
```

## Level 4: How to Fix It (Performance Deep Dive)

```bash
# GPU system metrics: SM utilization, memory, temperature, power
meta ai.mast-job system-metrics --name=JOB_NAME -o json

# GPU utilization summary (queries Scuba, last 7 days only)
meta ai.mast-job usage --name=JOB_NAME -o json

# Training metrics: loss curves, accuracy, etc.
meta ai.mast-job metrics --name=JOB_NAME --response-type=summary -o json

# List available metric names
meta ai.mast-job metrics --name=JOB_NAME --response-type=metric_names

# GPU memory snapshot analysis (requires version)
meta ai.mast-job analyze-zoomer-memory --name=JOB_NAME --version=42 -o json

# Communication straggler analysis
meta ai.mast-job analyze-zoomer-stragglers --name=JOB_NAME --version=42 -o json

# Profiling runs (AI Zoomer)
meta ai.mast-job profiling --name=JOB_NAME -o json
```

## Debug Recipes

### Recipe: Job Failed — Full Triage (30 seconds)

```bash
# Run all three in parallel (use separate tool calls)
meta ai.mast-job metadata --name=JOB_NAME -o json
meta ai.mast-job error --name=JOB_NAME -o json
meta ai.mast-job scheduling --name=JOB_NAME -o json
```

Then based on error type:
- GPU OOM → `analyze-zoomer-memory`
- Slow/stuck → `insights` + `system-metrics`
- Killed externally → `mlg-decision`
- Unknown → `query-error-context` + `logs`

### Recipe: Job Running But Slow

```bash
meta ai.mast-job usage --name=JOB_NAME -o json
meta ai.mast-job metrics --name=JOB_NAME --metric-pattern="loss" --response-type=data_points -o json
meta ai.mast-job analyze-zoomer-stragglers --name=JOB_NAME --version=V -o json
```

### Recipe: Preemption / Multiple Failures

```bash
meta ai.mast-job attempts --name=JOB_NAME -o json
# Then for each failed attempt:
meta ai.mast-job error --name=JOB_NAME --attempt=N -o json
```

## CLI vs MCP Mapping

| CLI Command | MCP Tool (fallback only) |
|---|---|
| `metadata` | `ai_infra_mast_job_search` / `ai_infra_mast_job_load` |
| `error` | `get_mast_job_error_tool` / `ai_infra_training_job_error_context` |
| `scheduling` | `get_mast_job_state_transition_time_tool` |
| `attempts` | `ai_infra_mast_job_attempts` |
| `insights` | (no direct equivalent — CLI only) |
| `query-error-context` | `ai_infra_training_job_error_context` |
| `mlg-decision` | `ai_infra_mlg_decision` |
| `logs` | `get_tw_logarithm_logs` |
| `system-metrics` | `ai_infra_mast_job_metric_data` |
| `metrics` | `ai_infra_mast_job_metric_data` |
| `analyze-zoomer-memory` | `ai_infra_zoomer_memory_analysis` |
| `analyze-zoomer-stragglers` | `ai_infra_zoomer_straggler_analysis` |
| `profiling` | `ai_infra_profiling_runs` |
| `usage` | (no direct equivalent — CLI only) |
| `classify-error` | (no direct equivalent — CLI only) |
| `kill` | (use with caution — mutative) |

## Common Mistakes

| Wrong | Right | Why |
|-------|-------|-----|
| Treat `meta` CLI timestamps as UTC | `meta` CLI outputs timestamps in **server-local timezone (PDT)** — label accordingly or convert before comparing with Unix epoch timestamps | Mixing PDT timestamps from `meta` CLI with UTC Unix epochs inflates time gaps by 7 hours. Always check `date` vs `date -u` to confirm the offset. (Learned 2026-05-20) |
| Snapshot publish cadence = trainer healthy | Snapshot publishing is a PUBLISH-PATH liveness signal, NOT a training-compute liveness signal. STUS-role models can publish snapshots while doing zero training compute (GPU SM util 0%, QPS=0). Always check mvai_metrics + TensorBoard QPS for actual training liveness, not just `meta ai.model.instance list` publish cadence. (Learned 2026-05-23: m2145336177 published every 3 min while training was stalled 25 min/hr) |
| Skip `mlg-decision` | Always check when job was killed (not failed) | ML Guardian kills look like failures but have different fixes |
| Forget `--version` on zoomer tools | Always pass `--version=V` | Zoomer data is per-version, defaults may miss the right data |
| Publisher queue backlog (1+ TB) + CHC delivery alerts on same model, zero consumption, 100% violations | Don't assume trainer OOM/crash. Check Hedwig delivery infrastructure first—likely publisher service outage. File streaming_infra_oncall task. Clear queue before any restart (OOM guaranteed with TB backlogs). Debug: `meta ai.model metadata --model-id=<id>` to verify queue depth. (Learned 2026-03-29: OT autolearn) |
| Multiple unrelated models fire queue alerts within same 5-min window (~12:45-12:49 PDT), all TB-scale backlogs, all zero consumption | Indicates systemic publisher infrastructure outage, not individual model failures. Don't debug each model separately. Contact streaming_infra_oncall, check publisher service logs for that time window. Clear all queues before restart. (Learned 2026-03-29: OT autolearn) |
| Trainer dead 100+ hours, all 6 sparse channels simultaneously dead, 7TB+ queue backlog, queues flat/not draining | Trainer process unresponsive long-term accumulation. DO NOT restart trainer without clearing all queues first—OOM guaranteed. Use runbook https://fburl.com/wiki/km3v8nam. Clear queue before attempting recovery. Contact model owner immediately. (Learned 2026-03-29: OT autolearn) |
| Multiple models (4+) fire with publisher queue backlogs at same timestamp | Don't debug individual model config. Check if ≥3 other models fired simultaneously with similar symptoms (e.g., m2130976595, m2145591142, m2143369370 all fired ~12:45 PDT Mar 28). If yes, systemic publisher service outage. Contact publisher_infra_oncall, clear ALL channels together per runbook https://fburl.com/wiki/km3v8nam, restart as batch. (Learned 2026-03-29: OT autolearn) |
| Publisher queue grows to 1GB+, trainer process offline for 3+ days | Check MAST trainer job status: is it DEAD/KILLED? Check trainer logs for OOM, ML Guardian decision, or config errors. If trainer dead, that's why queue has zero consumption. Clear queue before restarting trainer; don't assume queue self-drains. (Learned 2026-03-29: OT autolearn) |
| "Tier1 streaming dead" or similar unverified status persists across triage scans for 8-12+ days | Don't let status become stale carryover. On scan, verify GMPP Scuba metrics (https://fburl.com/scuba/gmpp/1qdb275c). If metrics still 0, file SEV immediately. If metrics show recovery, issue is resolved—update/close. Prevents blind spots. (Learned 2026-03-29: OT autolearn) |
| Hedwig channel delivery degradation: 79-88% message loss, periodic auto-recovery, publisher queue backing up over 10+ weeks | Check Hedwig channel health metrics. Do NOT restart trainer without clearing publisher queue first — OOM guaranteed. File streaming_infra_oncall task with: model ID, alert count, queue depth, and duration. Root cause is streaming infrastructure, not model code. (Learned 2026-03-30: OT autolearn) |
| Multi-channel publisher queue backlog: all 6+ channels failing simultaneously, zero consumption, queue growing on some channels | This indicates trainer crashed with OOM during restart. Check queue depth on ALL channels before any action. MUST clear all channels first using runbook before restart. Do NOT restart trainer with queued data — OOM guaranteed. (Learned 2026-03-30: OT autolearn) |
| Publisher queue oscillating ±2GB (steady oscillation, not draining) | Trainer is running but stuck somewhere — distinct from "trainer dead" (zero consumption, byte-identical readings). Verify trainer process is alive. Check for data stall, checkpoint loading hang, or network timeout. Use system-metrics to confirm trainer CPU/memory activity. (Learned 2026-03-30: OT autolearn) |
| Publisher queue backing up (951GB+) but actively draining at measurable rate (~900MB/min); trainer was previously stopped/slow | Monitor drain rate hourly; calculate ETA to zero at current rate; if drain rate drops to 0, MUST clear queue immediately before any restart or OOM guaranteed. Use runbook: https://fburl.com/wiki/km3v8nam. (Learned 2026-03-30: OT autolearn) |
| Tier1 streaming dead 9+ or 13+ days without auto-recovery; no alerts for recovery | Check GMPP metrics immediately to confirm streaming is still 0; if still dead after N days, file SEV and contact streaming_infra_oncall; do not wait for automatic recovery. (Learned 2026-03-30: OT autolearn) |
| Trainer process dead/stopped 4+ days while large queue (1.66TB+) backs up; queue cannot be consumed without trainer running | Before restarting trainer, MUST clear accumulated queue first using queue-clearing procedure or restart will fail with OOM; verify trainer aliveness status; if confirmed dead, clear queue then attempt restart. (Learned 2026-03-30: OT autolearn) |
| Publisher queue growing exponentially (0% drain rate) while trainer/consumer is dead, queue doubled in 17 days (434GB → 1.66TB) | Before restart: **(1)** Verify consumer truly dead (check trainer logs, GMPP metrics = 0). **(2)** Measure queue growth rate to confirm 0 drain. **(3)** Clear queue completely — do NOT restart trainer with backed-up queue or OOM guaranteed. **(4)** If producer still writing to dead consumer, fix upstream producer config first. Only resume after queue empty + consumer ready. (Learned 2026-03-30: OT autolearn) |
| Hedwig CHC streaming drops 20-30% of messages repeatedly every ~1 hour for 10 consecutive weeks (70+ alerts in 6 days, Mar 25-30) | This is *chronic systemic failure*, not transient glitch. **(1)** Clear publisher queue immediately (1.11TB+). **(2)** Do NOT restart trainer hoping it recovers — the infra (Hedwig message delivery layer) is broken. **(3)** File streaming_infra_oncall SEV for message loss root cause (network, broker, delivery pipeline). **(4)** Monitor queue drain + verify producer buffer cleared before resuming training. Single CHC spike may auto-recover; 70+ alerts = broken infra layer. (Learned 2026-03-30: OT autolearn) |
| All 6 sparse channels failing simultaneously with identical signature (100% queue violation, zero drain, byte-identical readings over hours) | Indicates *systemic upstream issue* (producer dead, trainer killed, config broken), not independent per-channel failure. **(1)** Check trainer status (dead 100+ hours?). **(2)** Check producer side — config, permissions, Hive data availability. **(3)** Verify network/firewall between producer and queue broker. Fix upstream first — restarting individual channels will fail if upstream still broken. Watch for multi-model cascade: Mar 28 saw 3 models alert within 2 min (same systemic root). (Learned 2026-03-30: OT autolearn) |
| Trainer stopped, publisher queue at 1-7TB, zero drain (static byte-identical readings) | CRITICAL: Clear ALL publisher channels before restart (use runbook: https://fburl.com/wiki/km3v8nam). Do NOT restart trainer—will OOM with queued data. Contact owner immediately. (Learned 2026-04-01: OT autolearn) |
| All sparse channels (sparse_0 through sparse_5) failing simultaneously with identical backlog growth | Trainer is stopped, not channel fault. Check trainer process state first. If stopped, clear all 6 channels before restart. If running but channels dead, escalate to streaming infra. (Learned 2026-04-01: OT autolearn) |
| Chronic streaming failures: CHC Tier1 alerts for 11+ weeks, publisher queue never drains, messages dropped ~1/hour | Systemic Hedwig infrastructure fault, not trainer failure. File streaming_infra_oncall task immediately (do not restart trainer). Escalate to Hedwig team with queue size and alert timeline. Clear queue only after infra team confirms delivery path restored. (Learned 2026-04-01: OT autolearn) |
| OT job restarts repeatedly with NCCL timeout errors; same communication timeout pattern seen in related model family within past week (e.g., gloo timeout in same ESR family) | Investigate cluster-level GPU communication health, not individual job config. (1) Check if model family shares training cluster. (2) Verify NCCL/gloo package versions match across all ranks. (3) Confirm all GPU ranks report alive to MAST; restart stuck ranks on healthy nodes only. (4) Check cluster topology changes in past 7 days. If ESR models hit within week, likely systemic cluster issue, not hardware flakiness. Escalate to NCCL/GPU platform team if rank communication desync persists. (Learned 2026-04-03: OT autolearn — S644354, S640330) |
| Training QPS suddenly drops to 0, job is alive and running normally, no publisher queue alert fires, no MAST crash occurs. Detection gap >6h with no standard alerts. | **Immediate check**: Query multicast subscription count in MRS_DELTA_UPDATE_SUBSCRIBER sitevar for this model. If >10 subscribers and trainer has limited capacity, trainer is consuming 100% of resources for snapshot registration instead of training. (1) List all downstream models registered in MRS_DELTA_UPDATE_SUBSCRIBER. (2) Identify and remove unnecessary subscriptions (config drift from prior prod refresh). (3) Verify subscription count matches actual downstream consumers. Fix via sitevar pruning. This failure mode is invisible to queue alerts and job liveness checks. (Learned 2026-04-03: OT autolearn — S644248 multicast bloat) |
| Configuration drift in sitevar subscriptions consuming trainer capacity; snapshot registration starvation without alerting | Standard monitoring (queue alert, job crash) does NOT catch trainer starvation from multicast subscription bloat. Add training QPS monitoring alert: flag sudden training QPS=0 with job alive and no queue alert within 2h. Audit all models in MRS_DELTA_UPDATE_SUBSCRIBER sitevar for subscription bloat post-prod-refresh. Multicast listener count should match actual downstream dependencies. (Learned 2026-04-03: OT autolearn — S644248 config drift pattern) |
| NCCL rendezvous timeout (10+ restarts); hedwig host lookup returns "SELECTION_NO_HOST_WITHIN_MINMAX" — blocks NCCL IP negotiation. Infrastructure layer issue, not hardware. | Check hedwig host lookup logs for "SELECTION_NO_HOST_WITHIN_MINMAX" (indicates IL misconfiguration or stale host pool). Verify IL team has healthy hosts in MINMAX range. Deploy gloo-based fallback (similar to D98536599). **Post-deploy**: re-run restart count before vs after fix to verify effectiveness. Escalate to minimal_viable_ai oncall if IL issue confirmed. (Learned 2026-04-06: OT autolearn) |
| MAST job restart triggers brief Hedwig peer connection loss → sparse delta CHC rate drops to 80-84% (below 90% threshold) for ~5 min. Pattern repeats across models (m876666733 Mar 26, m2143369370 Mar 30, m2147007224/m884447513 Mar 20). | Correlate MAST job restart events with sparse delta in-order push rate dips via timeseries analysis (e.g., `sigrid_predictor.hedwig.streaming.peer.client_push_seq_no.in_order`). Verify peer reconnect timeline is <5 min. Implement cross-model sparse delta CHC alert dedup — aggregate concurrent rate dips into single alert per cluster instead of per-model. Check MAST job restart is expected (not a crash loop). (Learned 2026-04-06: OT autolearn) |
| Tier-1 model fires 5 simultaneous NaN alerts (loss + 4 NE metrics) but remains unowned >24h; alert priority marked "low". Indicates shared encoder NaN/gradient explosion, not metric-specific bug. No SEV filed, no triage escalation. | Auto-lookup model owner in MLHub/AIM at triage ingestion time; pre-fill owner field in triage report. Raise NaN-in-LOSS alert priority to HIGH for Tier-1 models. For simultaneous multi-metric NaN: check shared embedding layer (gradient explosion before per-objective heads), verify checkpoint at NaN event, inspect upstream data for NaN feature values at T1. If transient: confirm clean restart; if persistent: trace to shared encoder or data pipeline. (Learned 2026-04-06: OT autolearn) |
| Job restarts repeatedly with NCCL timeout + `SELECTION_NO_HOST_WITHIN_MINMAX for tier hedwig.download.tracker.streaming.ai` error (IL GLOBAL hooks interference) | Check if IL GLOBAL hooks are enabled for this model. Verify D99499951 (prevent IL GLOBAL hooks NCCL deadlock) is landed and applied. If not, apply the fix or disable IL GLOBAL hooks. Check all ranks are alive post-fix. (Learned 2026-04-08: OT autolearn — S644354) |
| Training QPS falls behind Scribe data ingestion during peak hours; data age escalates from ~30 min to 70-90 min, approaching data loss threshold | Compare training QPS rate vs Scribe ingestion rate. Check for gloo timeout issues on publisher side (D98536599). If sustained, monitor for data loss risk. Consider adding GPU trainers or throughput efficiency improvements. (Learned 2026-04-08: OT autolearn — S640330) |
| OT QPS metric shows zero output but alert does not fire for 10+ hours | Configure sub-30-minute QPS staleness alerts instead of relying on long-latency thresholds. When QPS staleness alerts fire, route to model owner immediately; unassigned metric alerts tend to age silently. Check alert-to-owner routing for mvai_metrics NaN and staleness signals. (Learned 2026-04-08: OT autolearn — S644248, S644354) |
| NCCL ALLTOALL_BASE timeout on specific rank (e.g. Rank 6) with NumelIn=0 across consecutive attempts — same rank fails every time | Map Rank 6 to physical host across all failure attempts; if host is constant, escalate as host-level hardware/NIC topology fault to minimal_viable_ai (reference prior IL investigations like P2263447718); rule out model/code bugs by confirming other ranks healthy. (Learned 2026-04-11: OT autolearn) |
| SLO monitoring threshold misaligned with customer expectation (e.g. oncall dashboard at 90% SLO, customer contract at 95%) — team alignment reached weeks ago but config not updated | Verify customer SLO vs monitoring threshold in ODS/alerting config; update threshold to match commitment (e.g. 95%); track as blocking until implemented; gap means oncall sees green while customer sees yellow/red. (Learned 2026-04-11: OT autolearn) |
| GPU communication bottleneck during distributed training — NCCL timeout with high GPU util (>80%) but no OOM, no data stall (SM util low 30–40%) | Pull Zoomer trace; measure compute/comm overlap (exposed-comm = NCCL wallclock minus its overlap with compute — merge kernel intervals first; summing per-stream durations overcounts, e.g. 4 kernels ×1s on 4 streams = 4s sum vs 1s wallclock; method+code: `gpu-trace-analysis` skill §"Measuring Time: Wallclock vs Sum of Durations"); identify dominant collective kernel (e.g. ALLTOALL_BASE); if >40% exposed communication time, communication is saturating — check if specific ranks slow (compare send/receive patterns); consider model parallelism rebalance or batch size reduction. (Learned 2026-04-11: OT autolearn) |
| NCCL ALLTOALL_BASE timeout with **changing failure rank between attempts** (e.g., rank 42 → rank 6 NumelIn=0) | Root cause is IL (interconnect layer) hook instability, not static NCCL config. Verify with `meta ai.mast-job error --name=JOB` + `query-error-context`. Check if D100214795 + D99499951 (IL fix diffs) are deployed. Escalate to minimal_viable_ai team for IL hook investigation. (Learned 2026-04-12: OT autolearn — S644354 case) |
| Training job fires **4+ related NaN alerts simultaneously** (LOSS NaN + NE NaN on comment/like/share + calibration NaN from same job window) | Indicates model weights diverged (not flaky metric reporting). Do NOT promote checkpoints from this window. Assign model owner, check if job auto-recovered, validate checkpoint integrity with load test. Root cause often learning rate spike or upstream data corruption. (Learned 2026-04-12: OT autolearn — CFR MainPredictor case) |
| Model updates stop silently; TGIF async publish subprocess crashes with `ImportError: cannot import name 'StrEnum'` (or other 3.11+ API). Base layer Python 3.10, app code uses 3.11+ stdlib. Parent process does not fail immediately. | **First check**: `meta ai.mast-job error --name=JOB` and search logs for `ImportError` in subprocess. Check base layer Python version vs app dependencies for 3.11+ APIs (e.g., `StrEnum`, `tomllib`). **Fix**: Backport app code to Python 3.10 stdlib OR upgrade base layer. **Prevention**: Audit new OT models adopting 3.11+ features before merge. (Learned 2026-04-15: OT autolearn) |
| Model weight updates fail with misleading error "no full snapshot exists" when initializing delta publisher in deltaonly mode. Real root cause is upstream T3 TGIF async publish crash, not delta publisher itself. Engineers waste time debugging the wrong layer. | **Correct approach**: When you see "no full snapshot exists" in delta publisher init logs, DO NOT debug delta publisher first. Instead: (1) Check TGIF async publish logs for real failure (ImportError, network error, timeout, etc.). (2) Fix the upstream failure. The "no full snapshot" error is a symptom of failed async publish, not the root cause. Ensure error propagation surfaces upstream failures clearly. (Learned 2026-04-15: OT autolearn) |
| NCCL timeout in IG Feed ESR OT job causing frequent restarts. Initial diagnosis points to network/NIC issues, but actual root cause is interconnect layer (IL) configuration or hardware state, not traditional NIC errors. | **Correct approach**: For NCCL timeouts in IG Feed ESR specifically, escalate to MVAI team and IL oncall early. Verify: (1) Not a straggler rank issue (`meta ai.mast-job system-metrics --name=JOB` to check all ranks). (2) Check IL logs for configuration mismatches or HW state via IL oncall. (3) Common fix: restart on healthy nodes. If restarts don't fix, IL involvement required. (Learned 2026-04-15: OT autolearn) |
| Job publishes silently fail with StrEnum/Python 3.11+ ImportError (Python 3.10 base layer incompatible with model code) | Check job base Python version vs model code requirements. Enable hard failure in publish subprocess instead of silent degradation. Review job startup logs and observability (D100861178) to catch ImportError early. Coordinate with job owner to upgrade base layer Python version. (Learned 2026-04-15: OT autolearn) |
| Configerator config update causes OT job failure; app layer becomes unrecoverable; hot patch recommendation is incorrect | After configerator config change, verify TMS super-permission config sync is applied. Check app layer versioning matches config revision. If app layer stale, get prior revision, rebuild, and preserve from that checkpoint. Escalate config sync verification to TMS oncall. D100931466 tracks incorrect skill recommendation fix. (Learned 2026-04-15: OT autolearn) |
| Tier-1 PROD model (e.g., CFR MainPredictor) generates NaN training metric alerts with priority=low and no assigned_user, delaying oncall triage | Immediately assign Tier-1 NaN alerts to model oncall and escalate to training infra oncall. Audit alerting config to auto-assign owner and priority for tier-1 models. On NaN fire, first check: checkpoint integrity (load and inspect tensors), training data pipeline (verify real data flowing), gradient norms (detect numerical instability), learning rate (rule out too-high LR). (Learned 2026-04-15: OT autolearn) |
| OT job restarts repeatedly; CHC sparse delta in-order consumption rate drops to ~86% (below 90% threshold) for 14/15-minute windows | Check MAST job state transitions for restart frequency via `meta ai.mast-job scheduling --name=mvai-training-online-<MODEL_ID>`. If restarts are frequent, check publish timeout (D96358986) and SJD health. Root cause: job restart resets Hedwig sequence numbers → old sequences appear out-of-order. (Learned 2026-04-16: OT autolearn) |
| Async publish subprocess hard-fails with StrEnum incompatibility error on Python 3.10/3.11 | Check Python version in async publish subprocess environment. Verify StrEnum is available or backport from `typing_extensions`. Root cause: Python version mismatch between build container and runtime. (Learned 2026-04-16: OT autolearn) |
| Async publish subprocess crashes with `ImportError: cannot import name 'StrEnum'` → delta publisher init fails, no full snapshot, publishing blocked | Check base layer Python version (3.10?) vs model code (uses 3.11+ features like StrEnum). Add startup validation in `create_inference_module()` to hard-fail immediately on version mismatch instead of masking error as delta publisher failure. (Learned 2026-04-17: OT autolearn — threads_feed_lsr case) |
| Shadow training data stopped flowing; model receives zero shadow samples for 25+ days despite impression data normal | Check upstream label generation (Scribe CLIPS_PSELECT_POSITION cardinality for shadow traffic). If empty after recent upstream change, root cause is label pipeline breakage not model code. Escalate to LSR team to restore label generation; verify `filter_empty_shadow_label` isn't incorrectly dropping valid rows. (Learned 2026-04-17: OT autolearn — S638526 Reels ESR case) |
| Downstream component (delta publisher) fails to initialize; error logs point to init but real failure is in async subprocess | Check Logarithm for subprocess-level errors (workers, TGIF async tasks). Async subprocess exceptions often don't propagate to parent. Hard-fail in subprocess to surface errors immediately rather than letting silent failures cascade. (Learned 2026-04-17: OT autolearn — TGIF async publish pattern) |
| Sparse delta in-order consumption rate drops to 75–88% (threshold 90%), alert fires intermittently over days on Tier 1 model | Check MAST job state transition logs for restart frequency. Root cause: OT job restarts reset Hedwig push sequence number, causing predictors to drop out-of-order messages. If restarts frequent, investigate job failure mode (T259920238). If stable, check Hedwig channel health at network layer. (Learned 2026-04-18: OT autolearn) |
| Streaming update alert fires simultaneously across 10+ partitions on same timestamp (e.g., all cpu0–cpu7 at once) | Verify streaming updates recovered since alert time. If not, check MAST job status and delta publish logs for publish stall. Simultaneous multi-partition failure indicates OT publish stall or job restart, not individual partition issues. (Learned 2026-04-18: OT autolearn) |
| TGIF async publish subprocess crashes silently, model missing FULL_SNAPSHOT snapshots | Check subprocess import errors in MAST logs (e.g., "cannot import name 'StrEnum'"). Root cause: Python 3.10 base layer with Python 3.11+ dependencies. Verify publish subprocess Python version matches all dependency requirements. D100861178 adds better logging. (Learned 2026-04-18: OT autolearn) |
| OT training job stuck with publish stalled, no fresh model updates reaching serving | Check MAST job state transitions for job state; if publish is stalled, use restart runbook. Confirm whether this is a restart loop or true deadlock. (Learned 2026-04-20: OT autolearn) |
| Sequential teacher OT model cannot restart — 2nd occurrence of restart mechanism failure | Review resolution of first occurrence; identify what changed between occurrences. Coordinate with team on restart mechanism gap analysis (project task 2.12). (Learned 2026-04-20: OT autolearn) |
| Training checkpoint verification failure with NaN loss + NE violation in Tier-1 model | Check MAST job logs for recent training metrics (loss, NE) divergence point. Possible causes: data pipeline issue, LR instability, or bad checkpoint loaded. Assign immediately for Tier-1 model. (Learned 2026-04-20: OT autolearn) |
| Persistent T4 Hedwig alerts (15+/week) from Tier1 sparse delta in-order consumption rate drop | Run `ot-delta-status.sh --model=M` to verify T3 sparse delta cadence and publisher health. If T3 is healthy, escalate to Hedwig team with alert instance IDs from alert history. Check for T3 publisher restarts in the timeframe. (Learned 2026-04-21: OT autolearn) |
| Async publish crash: `StrEnum not found` or similar Python 3.11+ feature error in 3.10 environment | Verify target runtime Python version in job config/package metadata. Audit OT publish modules for 3.11+ features (StrEnum, PEP 673 TypeAlias, etc.). Add fail-fast guards in import path or pin feature usage to available version. Test locally against target Python version before landing. (Learned 2026-04-21: OT autolearn) |
| Trainer 60% blocked, DPP starvation ~70%, no error logs, FULL_SNAPSHOT running every cycle instead of SPARSE_DELTA | Check JK allowlist: `meta ai.mast-job logs --name=JOB \| grep sparse_enabled_model_entity_ids`. If False, model missing from `ai_infra/delta_publishing:sparse_enabled_model_entity_ids` JK allowlist. Coordinate with model owner to add model FBID to JK allowlist. Verify UMM baseline snapshot exists. Monitor GMPP log post-fix to confirm SPARSE_DELTA and DPP starvation <20%. (S652695 pattern) |
| Rank execution times diverge, training hang indefinitely (>30 min) at checkpoint sync, torch.compile recompilation high | Check for torch.compile'd preprocessing with dynamic shapes from Scribe (e.g., torch.unique() at preproc stage). Symptom: unbounded hang even at 1800s timeout, propagates via distributed barrier. Profile per-rank execution time. If confirmed, escalate to torch.compile team with per-rank traces. Test alternative: remove UHM_1D_DEDUP or dynamic shape ops from preprocessing. (S651642 pattern) |
| Low sparse delta streaming success rate, rate fluctuates even after recent patches | Verify dtype consistency: check full snapshot dtype (INT8) vs delta snapshot (Float/BF16). Run MRS OT patch (e.g., D101297690) to align dtypes. If streaming success still fluctuates after dtype fix, check publisher queue backup and flow control configuration (Hedwig rate limit tuning for large ESR models). Monitor streaming success rate to baseline before closing. (S650346 pattern) |
| Async subprocess crashed silently on version mismatch (e.g., StrEnum import on Python 3.10 when module requires 3.11); job continued RUNNING and retried publish 21x without alerting. | Check base layer Python version matches module requirements. Add fail-fast on non-retryable publish errors (e.g., ImportError, AttributeError). Emit ODS counter for alerting instead of silent retry. See D101226214 (fail-fast) + D101665702 (ODS counter) — landed Apr 20. |
| Periodic training QPS drops to 0 for ~2 min; DPP starvation at 70%, GPU SM utilization at 26%; MAST shows RUNNING with 0 restarts, suggesting job is healthy. Hypothesis: in-trainer full-snapshot checkpoint saving blocks the training loop. | Confirm checkpoint save duration from publish logs. If save duration >2 min, escalate to MVAI platform team to implement async checkpoint save or throttle publish timing to avoid blocking DPP. Check S652695 for resolution status. Contact p92_relevance_ranking (model oncall) for checkpoint config details. |
| Job registered as RUNNING in MAST but OT status tool returns null; CHC in-order drop alerts still firing; model appears to have stale state. | When MAST status contradicts metrics: (1) check OT status tool registration — null indicates possible decommission, (2) verify model owner is aware of current status, (3) if decommissioned, escalate alert routing to prevent false positives. For m2143369370 (IG Feeds LSR OT), contact model owner to clarify intent. |
| `aito_error_classification: UNKNOWN` / `UNCLASSIFIED_ERROR` with root cause `exitcode=-12 (SIGUSR2)` and no Python traceback — look like mysterious job kills | **AITO classifier blind spot** (can't bucket signal-only failures without a Python exception). Read `failure_message` from `query-error-context` — TW-level kill reason is there (twtask-pre-run-step Stopping cascade + LxcAgentHelper exit). SIGUSR2 = torchelastic worker-shutdown signal, triggered by upstream TW/MAST host drain, rendezvous rescale, or MVAI preemption coordinator. **4-check triage signature** (useful for triage routing, NOT for declaring root cause): (1) `FAILED → RUNNING` on next attempt in <10 sec; (2) at least one attempt has survived for hours on same app+base; (3) `mlg-decision → No ML Guardian decision found`; (4) host-rank-local_rank permutation varies across failed attempts (rules out host-specific hardware). If all four hold, **tentatively** route as infra noise — but do NOT claim "self-resolved" on a single version survival. Pattern recurs across datacenters and versions — e.g., mvai-training-online-2130806697 v6 attempts 0/2/3/4 on NCG3, v8/0 on VLL5. Root-cause-identifying requires runtime logs in the 60s before the SIGUSR2 timestamp. (Learned 2026-04-23) |
| Training QPS drops periodically to 0; model has `weights_delta_publish_config` set but every publish falls back to FULL_SNAPSHOT blocking training ~60% of the time | Check JK allowlist: verify model entity ID is in both `ai_infra/delta_publishing:sparse_enabled_model_entity_ids` AND dense equivalent. Silent fallback happens when config is set but JK returns False — no error surfaced. Also verify UMM baseline snapshot is registered. (Learned 2026-04-24: OT autolearn) |
| Async publish subprocess crashes with ImportError (e.g., StrEnum or other Python 3.11+ feature) despite job running fine | Python version mismatch: TGIF publish subprocess runs on Python 3.10 base layer; if any dependency uses 3.11+ features it crashes silently. Add Python version validation at subprocess startup. Check base layer vs dependency versions before adding new modules to the publish subprocess. (Learned 2026-04-24: OT autolearn) |
| Dense delta transitions fail at the predictor after DeltaOnlyPublisher starts; snapshot transition errors during model serving | DeltaOnlyPublisher missing bootstrap: publisher started without a valid full-snapshot bootstrap, making subsequent delta transitions fail. Verify bootstrap is correctly registered before delta publishing begins. Check T3 publish pipeline for the bootstrap step. (Learned 2026-04-24: OT autolearn) |
| DeltaOnlyPublisher causes snapshot transition failures with no obvious config error | DeltaOnlyPublisher requires a bootstrap snapshot to anchor the delta chain; if bootstrap is missing, the transition from snapshot to delta mode fails. Verify the publisher config includes a valid bootstrap snapshot reference before enabling delta-only mode. (Learned 2026-04-24: OT autolearn) |
| Oncall alerts CLI returns exit=1 and `--oncall` flag is disabled — alert list is empty | S650203 (excessive Mandoline load) can temporarily disable the `--oncall` CLI flag. Workaround: use `--observer-id` instead, or check the oncall alert portal manually to avoid a monitoring blind spot during the outage. (Learned 2026-04-24: OT autolearn) |
| Job shows RUNNING with no errors but training QPS drops 60%+ and DPP starvation ~70% — delta publishing silently disabled, falling back to full snapshots | Check publish log for `[delta update jk check] ai_infra/delta_publishing:sparse_enabled_model_entity_ids False`. If found, model_entity_id is missing from the JK allowlist. Model owner must request allowlist addition. Verify fix by confirming `PublishMode.SPARSE_DELTA` in publish log and DPP starvation drops below 20%. (Learned 2026-04-25: OT autolearn) |
| All shadow training samples drop to 0 (`effective_sample_cnt=0` in Scuba for shadow logging sources) while impression data is normal — no MAST errors surfaced | Likely an upstream diff changed label/field definitions causing a training filter (e.g., `filter_empty_shadow_label`) to silently drop every shadow row. Check cardinality of the relevant pselect/label field in current Scribe shadow data. Fix is upstream — escalate to the diff owner; MVAI code is not the source. (Learned 2026-04-25: OT autolearn) |
| Delta-publishing job fails with `ModelGenerationUserError: There is no valid snapshot available for model: <id>` blocking checkpoint saving | No valid UMM baseline snapshot is registered for the model. Register a baseline UMM snapshot per the wiki before enabling delta publishing. This error frequently co-occurs with the JK allowlist issue — check both: confirm allowlist membership AND snapshot registration. (Learned 2026-04-25: OT autolearn) |
| OT delta publishing stalls repeatedly with StuckJobException — publish subprocess inside the trainer hangs, blocking further delta publishing for that model (seen in S653341, S651642, S627484) | Immediate: restart the MAST job to clear stuck subprocess state; confirm delta publishing resumes via gmpp Scuba for the affected model. Do not wait for automated recovery — none exists until D96387953 lands. Long-term: track D96387953 (dead-subprocess liveness polling). (Learned 2026-04-25: OT autolearn) |
| Training QPS drops to 0 for ~2 minutes periodically (not OOM, not NCCL) | Check if model ID is in sparse delta JK allowlist (`ai_infra/delta_publishing:sparse_enabled_model_entity_ids`). If missing, every publish silently falls back to FULL_SNAPSHOT with 1500s gloo timeout that blocks training ~60% of the time. Fix: add model to JK allowlist AND register a UMM baseline snapshot. (Learned 2026-04-25: OT autolearn) |
| `online_train_publish` step stuck with StuckJobException; SJD shows job as "alive" | SJD monitors training-loop liveness only — it does NOT detect publish-phase hangs. Manually inspect MAST job publish subprocess logs, kill the stuck publish subprocess, and verify job resumes. Track D96387953 (dead publish subprocess detection) for permanent fix. (Learned 2026-04-25: OT autolearn) |
| OT job appears "stuck" indefinitely with no clear error — TGIF publish path | Non-retryable TGIF publish errors used to hang forever instead of failing fast. Check ODS counters for non-retryable TGIF publish error events. If hanging, the fix pattern is fail-fast on non-retryable errors (ref D101226214). Monitor ODS after deploy to confirm fix is active in prod. (Learned 2026-04-26: OT autolearn) |
| OT publish subprocess crashes, parent process blocks forever, job eventually dies with StuckJobException | Publish subprocess crash leaves parent blocked on `message_queue.get()` until Gloo timeout fires. Pattern seen on video_udd_lsr (Feb S627484, Apr S653341 — same conveyor, 2 months apart). If the original fix only patched one code path, audit all `message_queue.get()` call sites in publish path for missing timeout/exception handling. (Learned 2026-04-26: OT autolearn) |
| Training QPS drops periodically to 0 for ~2 min; training example age spikes to 200k+ ms; MAST still shows RUNNING | In-trainer full-snapshot checkpoint save is likely blocking the training loop synchronously. Confirm by correlating QPS drop timestamps with checkpoint intervals via TensorBoard or ODS. Fix: switch to async checkpoint or reduce checkpoint frequency. (Learned 2026-04-26: OT autolearn) |
| Hedwig/sigrid_predictor sparse-delta consumption rate drop alert fires into mrs.ot GChat — looks like a training-side issue | Check if alert is T4 serving-side (not T3 training): verify whether the model's alert routing should point to the serving team, not mrs_online_training. Route accordingly and don't debug training pipeline. (Learned 2026-04-26: OT autolearn) |
| Periodic QPS dip to 0 for ~2 min with no obvious error — checkpoint saving suspected | Investigate silent TGIF publish failures first: D101226214 adds fail-fast on non-retryable publish errors; D101665702 adds ODS counter. Check the ODS counter for non-retryable publish errors before tuning checkpoint behavior. (Learned 2026-04-26: OT autolearn) |
| SEV filed against mrs_online_training for a job that appears OT-owned — team begins debugging before confirming ownership | Always verify oncall team with `get_online_training_status_info` (or `meta ai.mast-job metadata --name=JOB`) before any investigation. Job may belong to another team (e.g., p92_relevance_ranking). Assist if needed but do not own the SEV. (Learned 2026-04-26: OT autolearn) |
| Job is RUNNING with no MAST errors, hourly checkpoints succeed, but training shows ~70% DPP starvation and periodic QPS drops to 0 — no sparse deltas produced despite model code using `WEIGHTS_DELTA_PUBLISH_CONFIG` | Platform JK allowlist `ai_infra/delta_publishing:sparse_enabled_model_entity_ids` may be returning False for the model_entity_id, silently falling back to FULL_SNAPSHOT. Check via `meta ai.model-type metadata` for the model, then ping lizichao / ai_infra oncall to add the model_entity_id to the JK allowlist. Full-snapshot publish (~2:29 each) blocks the training loop even when the job appears healthy. (Learned 2026-04-27: OT autolearn) |
| Delta publishing fails or produces no diffs even after JK allowlist is updated | UMM baseline snapshot may be missing — it is a hard prerequisite for delta diffing. Verify a valid baseline snapshot is registered in UMM for the model before expecting sparse deltas to appear. (Learned 2026-04-27: OT autolearn) |
| Same T4 alert ("sparse delta in-order consumption rate drop") fires 6+ times per week for weeks; model returns null from OT status API with no job, no state, no oncall | Model is likely retired or transferred but alert routing was never cleaned up. Check model ownership and remove from alert routing config. Add alert suppression for null-status OT models to prevent this class of noise. (Learned 2026-04-27: OT autolearn) |
| Dense delta publish fails or causes serving errors after switching to "Paced" mode — weight tensor element count in dense delta doesn't match full snapshot (e.g. 33728 vs 15424 elements) | Check whether paced dense delta uses a different parameter scope/shape than the full snapshot checkpoint serialization. The T3 publishing pipeline has no pre-publish shape validation. Compare dense delta weight shapes against full snapshot before enabling paced mode. CC IP runtime (Hongbo Qin) and SilverTorch (Ziqi Liu). (Learned 2026-04-27: OT autolearn) |
| Model owner expects TMS to auto-restart a stalled/failed online training job but nothing happens | TMS auto-restart only applies to PROD + ONLINE_READY model types. ADHOC + ONLINE_READY does NOT qualify. Check model type in MLHub; switch to PROD + ONLINE_READY if continuous auto-restart is needed. (Learned 2026-04-27: OT autolearn) |
| Online training job fails with DPP throttling error; switching model type (ADHOC → PROD) is suggested but job still won't recover | DPP throttling errors are non-retryable — changing model type only enables future auto-restart, it does not resolve the active throttle. Must escalate to DPP team directly to lift the throttle before any restart will succeed. (Learned 2026-04-27: OT autolearn) |
| Job uses `*OnlineStreamingMast*ConfigProvider` but QPS periodically drops to 0 with no errors logged; MAST shows RUNNING; no sparse deltas published — silent FULL_SNAPSHOT fallback | Run `jk_check_sparse_enabled_model_entity_ids(<model_entity_id>)` — if False, the JK allowlist is missing the model, causing silent fallback to FULL_SNAPSHOT every cycle. Fix: (1) add model_entity_id to JK allowlist, (2) register UMM baseline snapshot. This is distinct from "config-never-enabled" — the code path is correct but the platform gate is closed. (Learned 2026-04-28: OT autolearn) |
| Full snapshot load fails with "weights and bias element count mismatch" when both `item_streaming` and `dense_delta` are enabled | Concurrent update race: decoupled publish allows item delta and full snapshot to concurrently access shared buffer. Apply D96896453 (fresh index hold guard) + D97233396 (FIFO queue startup delay) patterns. Engage SilverTorch/IP runtime team if model uses decoupled publish. Ref: S632893 (OmniUV) as prior art. (Learned 2026-04-28: OT autolearn) |
| ADHOC+ONLINE_READY job hits non-retryable DPP throttling error and TMS does not auto-restart; model owner stuck waiting | TMS auto-restart only qualifies PROD+ONLINE_READY jobs — ADHOC is excluded by design. Resolution: re-register the job as PROD, then re-run; expect entitlement retries. Ref: D102692029 codifies this 3-layer contract (MAST non-retryable → TMS PROD-only → register-and-run path). (Learned 2026-04-28: OT autolearn) |
| ADHOC+ONLINE_READY model fails and never restarts — user sees a stopped job with no explanation | ADHOC jobs with ONLINE_READY model type do NOT auto-restart on failure. Manual restart required. Check job state with `meta ai.mast-job metadata --name=JOB`; if FAILED with no auto-recovery, restart manually and inform model owner. (Learned 2026-04-28: OT autolearn) |
| Training QPS drops to 0 / GPU 70–80% idle on DPP starvation; healthy baseline job exists at same time | Compare failing vs healthy job configs for DPP entitlement/quota differences (not just partition availability). Check DPP capacity changes around failure onset. Starvation dashboard is the primary signal; escalate to DPP owner for affected training partitions. (Learned 2026-04-28: OT autolearn) |
| MAST job killed with exitcode -12 / "Signal 12 received by PID" in torchrun workers | OS-level SIGUSR2 (signal 12) sent to trainer processes — not a guardian/TTL kill. Check `meta ai.mast-job error --name=JOB` for exitcode=-12. Workaround: apply D101908384 (scoped to sparse-streaming jobs as of Apr 2026); for other job types coordinate with Zheyue on root cause. Do not confuse with normal DEAD/guardian kills. (Learned 2026-04-29: OT autolearn, ref S654768) |
| OT job auto-killed ~4 min after start by TMS ("stopped by twshared,ai_infra__training_management_system") | Job is registered at ADHOC or QE priority — TMS kills lower-priority OT jobs by design. Not a bug. Fix: promote job to PROD priority via register-and-run (P2291775489). Same pattern seen for models 2127366584 and 2126928942 Apr 2026. Reply to user with ADHOC vs PROD TMS semantics before investigating infra. (Learned 2026-04-29: OT autolearn, ref S654768/mast.users) |
| Multiple OT tuning jobs (same model family) fail with DPP data starvation; one job runs fine | Compare partition configs between the good reference job and all failing jobs — starvation source is usually a diverged partition or scribe config, not the model code. Reference job ID is key. Escalate to dpp_distributed_systems with diff in configs. Check prior SEV for same model family (recurring pattern: ig_reels and LSR hit starvation ~every 2-3 weeks). (Learned 2026-04-29: OT autolearn, ref S656065, S652695, S638526) |
| OT model "weights and bias element count mismatch" crash when item streaming + dense delta run concurrently (same pattern seen in S632893 OmniUV Mar 2026 and T266619710 thread QE Apr 2026) | Disable dense delta and apply D97233396-style startup delay to serialize initialization; do not debug as a data or shape mismatch — root cause is concurrent write race during startup (Learned 2026-04-29: OT autolearn) |
| OT model snapshot stale 9–17h with no early alert; NE variance and miscalibration symptoms observed (S653939 IG Reels LSR) | Check GPU quota on the training cluster first (`meta ai.mast-job scheduling --name=JOB`); GPU overquota silently prevents blob distribution from reaching convergence threshold causing freshness to stall while alerts fire too late — verify quota restored and file staleness alert for tier-1 OT models if >12h threshold is missing (Learned 2026-04-29: OT autolearn) |
| MVAI job unexpectedly DEAD/killed with signal -12 (SIGUSR2) across multiple job types (S654768) | Do not assume ML Guardian TTL — check for SIGUSR2 kill signal specifically; apply sparse-streaming workaround D101908384 as mitigation while root-cause fix is in flight; nominate for MRS ML SEV Review if fleet-wide impact (Learned 2026-04-29: OT autolearn) |
| OT job stuck after failure, TMS does not auto-restart despite ONLINE_READY status | Check registration mode: ADHOC+ONLINE_READY is silently excluded from TMS auto-relaunch. Re-register as PROD mode to restore auto-restart behavior. Verify with `meta ai.mast-job metadata --name=JOB` and look for `mode: ADHOC`. (Learned 2026-04-30: OT autolearn) |
| OT job shows >70% DPP starvation; every publish blocks trainer ~2.5 min | Every publish is falling back to FULL_SNAPSHOT. Check if model IDs are in the platform JK delta allowlist (`ai_infra/delta_publishing:sparse_enabled_model_entity_ids`). If missing, coordinate with ai_infra/delta_publishing team to add them. Confirm DPP starvation drops below 20% after enablement. (Learned 2026-04-30: OT autolearn) |
| Element count mismatch error on full-snapshot load when both dense delta and item streaming are enabled | Concurrent update race: dense-delta-updated weights diverge in shape from full-snapshot weights in memory. Temporarily disable dense delta to unblock. Check if fix D96896453 (S632893 fix) applies. Pair with item-streaming team for permanent resolution. (Learned 2026-04-30: OT autolearn) |
| Recurring T4 sparse delta in-order consumption rate below threshold (77-80% vs 90%) for a model, firing daily — model's OT status returns null | OT job was likely deregistered while serving config still expects deltas, causing perpetual Hedwig/Sigrid message loss. Run `get_online_training_status_info_tool` on the model: if status is null, the OT job is gone. Escalate to serving team (T4 owner) to remove delta expectations from serving config — this is a cross-team cleanup, not an OT oncall fix. (Learned 2026-04-30: OT autolearn) |
| ADHOC + ONLINE_READY training job fails and does not auto-restart — oncall receives alert but no recovery happens | TMS only auto-restarts jobs with PROD + ONLINE_READY status. ADHOC jobs are excluded from TMS auto-restart even when ONLINE_READY. Manually restart the job and verify the job type in MAST config. If recurring, file a task to convert job to PROD or add explicit restart handling. Platform-level notification gap for ADHOC failures is a known open issue. (Learned 2026-04-30: OT autolearn) |
| StuckJobException (SJD stall detection) fires on multiple MVAI models simultaneously | Multiple simultaneous StuckJobExceptions indicate shared infra root cause, not per-model bugs. Do not debug each model in isolation. Run `mvai trace-doctor analyze -j <job>` across all affected jobs, compare DPP starvation % and publish latency. If ≥3 jobs share the same root cause (DPP capacity, MAST preemption wave, platform change), escalate to DPP oncall or MVAI platform team rather than per-model triage. (Learned 2026-04-30: OT autolearn) |
| OT job gets stuck in publish phase — MAST never retries, job hangs indefinitely | Async publish subprocess crash leaves NCCL/DPP/publisher C++ threads blocking Python shutdown. Check `meta ai.mast-job error --name=JOB` for StuckJobException. Apply publish timeout patch (D96358986) + os._exit() on publish failure path. Cross-ref S654315 if multi-model simultaneous. (Learned 2026-05-01: OT autolearn) |
| "OT cannot be turned on — automatically got turned off after enabling" with no error message | ADHOC-registered models are silently excluded from TMS auto-restart (TMS only restarts PROD + ONLINE_READY). Check job registration type: if ADHOC, follow ADHOC→PROD register-and-run flow. Also check Freeproxy capacity at assigned cluster if quota exhaustion is possible. (Learned 2026-05-01: OT autolearn) |
| ~60% training idle time and ~70% DPP starvation despite delta config being set correctly in code | Platform JK allowlist (`sparse_enabled_model_entity_ids`) may return False, causing silent FULL_SNAPSHOT fallback even when WeightsDeltaPublisherConfig is explicitly set. Each full-snapshot blocks trainer ~2.5 min. Check publish logs for FULL_SNAPSHOT vs SPARSE_DELTA; add model ID to JK allowlist and register baseline UMM snapshot as precondition. (Learned 2026-05-01: OT autolearn) |
| NE stops updating for 30–45+ min then suddenly spikes back (e.g. models 2125763080, 2125879500) | In-trainer GMPP full-snapshot publish is blocking the training loop (connected to S631731 deadlock). Check GMPP in-trainer publish logs around the freeze window. Fix: switch to out-of-trainer publish or apply publish timeout. Do not wait for StuckJobDetector — it will eventually kill the job but wastes time. (Learned 2026-05-01: OT autolearn) |
| Job passes MAST liveness checks but hits StuckJobException, eventually killed by StuckJobDetector | Publish-phase stuckness is invisible to MAST liveness — job is stuck in BACKWARD_PASS or publish path, not the forward pass. Check `meta ai.mast-job error --name=JOB` for "BACKWARD_PASS" or publish-path errors. Apply publish timeout workaround and restart. Escalate to streaming_infra_oncall if Hedwig channel is the block. (Learned 2026-05-01: OT autolearn) |
| Same model fires 5+ recurring MAJOR sparse delta CHC alerts in one week, all with Owner: 0, none acknowledged | Unowned alerts accumulate silently. First check GMPP Scuba publish stats. If publish rate is healthy, escalate to streaming_infra_oncall. If publish gaps correlate with alert windows, investigate training job stability and assign a permanent owner to the model. (Learned 2026-05-01: OT autolearn) |
| Training job crashes with SIGUSR2 (-12) + NO_OTHER_FAILURES on a sparse-streaming model (S654768/S654102 signature) | Do NOT restart without applying D101908384 first — restarting repeats the same crash. Apply the sparse-streaming top-10% momentum cap diff, then restart. (Learned 2026-05-02: OT autolearn) |
| MAST job is preempted despite being under quota | This is region capacity fragmentation (higher-priority tenant from same region), not a true quota issue. Confirm auto-reschedule occurred; if still pending, contact MAST oncall or resubmit targeting a different region. (Learned 2026-05-02: OT autolearn) |
| Training job rebuilt with new app package, then takes 11h+ cold-start ramp-up to reach production QPS | No warm checkpoint recovery is configured. Guide model owner to enable warm checkpoint restore — reduces ramp-up from ~11h to ~1-2h. (Learned 2026-05-02: OT autolearn) |
| OT job exits with SIGUSR2 -12 and NO_OTHER_FAILURES — looks like benign preemption | Do NOT treat as harmless restart. SIGUSR2 -12 + NO_OTHER_FAILURES is a known real-bug signature (sparse-streaming momentum issue, S654102/S654768). Apply workaround D101908384 and add model to SEV affected list. (Learned 2026-05-02: OT autolearn) |
| OT job fails and does not auto-restart despite TMS registration | Check TMS registration state: ADHOC registration silently disables auto-restart. Verify job is PROD-registered, not ADHOC. Promote ADHOC→PROD per runbook P2291775489 for any prod-intent or QE model. (Learned 2026-05-02: OT autolearn) |
| OT model stops publishing full_snapshots for several hours after upstream DPP/Scribe failure | Even if TMS auto-restarts the job, verify the restarted job is actually publishing via `ot-delta-status.sh <model>` or GMPP Scuba. A multi-hour freshness gap can persist silently after auto-restart catches up. (Learned 2026-05-02: OT autolearn) |
| OT job died with non-retryable error and missed several hours of full_snapshot; root cause traced to DPP/Scribe upstream failure (not model code) | When an OT job dies with a non-retryable error, check DPP/Scribe upstream health first before investigating model config or code. Pattern recurs across shifts — two incidents in one week (Apr 27 Shuguang model, May 2 ig_textpost_feed_m2m_retrieval v32). After upstream resolves, confirm the restarted job published at least one full_snapshot before closing. (Learned 2026-05-03: OT autolearn) |
| ADHOC+ONLINE_READY job failed to auto-restart after a transient failure; engineer assumed TMS would retry but job stayed down | ADHOC model types are excluded from TMS auto-restart per the TMS state matrix. If a job needs automatic recovery, the model must be registered as PROD+ONLINE_READY (not ADHOC). When diagnosing a job that is unexpectedly down with no TMS retry, check `meta ai.model-type metadata` for the model's type field before assuming infrastructure failure. (Learned 2026-05-03: OT autolearn) |
| Multiple MVAI models hit StuckJobException simultaneously; debugged as per-model issues in parallel, wasting time | Simultaneous StuckJobException across multiple unrelated models is a fleet-wide infrastructure signal, not a per-model code issue. First check MAST event logs for the shared time window for platform maintenance or capacity events (same pattern as S627484/S628346). Restart stuck jobs only after confirming there is no ongoing platform event — restarting during an active event will re-stuck immediately. (Learned 2026-05-03: OT autolearn) |
| OT training job exits with code -12 and `<NO_OTHER_FAILURES>` (SIGUSR2 signal, StuckJobException pattern) | Do NOT re-triage from scratch. This matches the S654768/S654102 real-bug signature. Route directly to S654768/S654102 owners for fix ETA. If QE is blocked, restart the job manually while awaiting the patch. (Learned 2026-05-03: OT autolearn) |
| OT model FULL_SNAPSHOT gap (>1h) with upstream DPP/Scribe failure as root cause; TMS auto-launched next version | Verify FULL_SNAPSHOT publishing resumed on the new version via `ot-delta-status.sh` or GMPP Scuba. File SEV4 when upstream issue causes >1h publishing gap (cross-team escalation). Confirm DPP/Scribe upstream root cause is resolved before closing. (Learned 2026-05-03: OT autolearn) |
| OT training job shows ~45-min pause followed by NE spike that does not recover; publish pipeline stall suspected | Check GMPP publish logs for the stall window to confirm publish pipeline as root cause. Restart the job — stall-induced NE spikes typically do not self-recover. After restart, verify training resumes at expected NE baseline before marking resolved. (Learned 2026-05-03: OT autolearn) |
| ADHOC QE job failed to auto-restart; user expected TMS recovery | TMS auto-restart only triggers for PROD + ONLINE_READY jobs. ADHOC jobs require manual restart or re-registration as PROD. Confirm job type via `meta ai.mast-job metadata --name=JOB` and check `job_type` field before troubleshooting TMS. (Learned 2026-05-04: OT autolearn) |
| Job appears stuck — training loop stalls but job does not exit (StuckJobException) | Not a normal FAILED state — check for DPP starvation (dataloader blocked), checkpoint hang (publish deadlock), or rank straggler. Use `meta ai.mast-job error --name=JOB` + `insights` to confirm. Cross-reference with other jobs sharing the same data pipeline or entitlement opened in the same time window. (Learned 2026-05-04: OT autolearn) |
| Training job has high I/O latency / DPP starvation despite healthy data pipeline | Check hardware colocation: data loading and GPU compute may be in different physical locations. Colocation is set at MAST entitlement level — verify entitlement config and coordinate with MAST/capacity team if colocation is missing. (Learned 2026-05-04: OT autolearn) |
| Delta publishing code is correctly configured but job shows 60–70% DPP starvation with MAST status RUNNING — no error visible | Silent JK allowlist fallback: platform `ai_infra/delta_publishing:sparse_enabled_model_entity_ids` returns False, forcing every cycle to FULL_SNAPSHOT in-trainer. Check publish log for `PublishMode.FULL_SNAPSHOT` vs `SPARSE_DELTA`. Fix: add model entity ID to JK sparse allowlist AND register UMM baseline snapshot. (Learned 2026-05-05: OT autolearn) |
| Publisher queue bytes alert fires (>25GB threshold, 100% data points failing) | Hedwig consumer lag: trainer delta output rate exceeds downstream consumer throughput. Check ODS chart to see if queue is draining. If still elevated, follow runbook https://fburl.com/wiki/km3v8nam — pause delta publishing or increase Hedwig consumer capacity. Identify model owner via model ID. (Learned 2026-05-05: OT autolearn) |
| Crashed training job does not auto-restart (stuck DEAD/FAILED, no TMS recovery) | Check job registration type: ADHOC-registered jobs are not eligible for TMS auto-restart. Fix: re-register job as PROD, then TMS will retry automatically. Distinguish from TTL/ML Guardian kills (use `meta ai.mast-job scheduling --name=JOB`). (Learned 2026-05-05: OT autolearn) |
| NCCL timeout / Gloo barrier deadlock across 1000+ events fleet-wide, all delta publish jobs — not a network or straggler issue | Check if `TORCHELASTIC_USE_AGENT_STORE` is inherited by the delta publish subprocess; the subprocess env causes TCPStore fallback and hangs all ranks. Fix: unset the env var before subprocess spawn and restore after (see D103808649). (Learned 2026-05-06: OT autolearn) |
| Full snapshot missing for 5+ hours after job auto-restart / in-place checkpoint reload — weights reload correctly but publish never fires on schedule | In-place checkpoint reload resets model weights but does NOT reset the publisher timer. The stale timer from the pre-reload cycle persists, delaying the next full_snapshot trigger by the full interval. Check publisher timer lifecycle in logs after reload; fix requires explicit timer destroy/reset on checkpoint reload (see D103621691). (Learned 2026-05-06: OT autolearn) |
| ~60% DPP starvation even after EDPP scaling — trainer shows long read/pause cycles; EDPP cannot help because trainer itself is stalled during publishing | Check if the model is on the JK allowlist for sparse/dense delta. If blocked, every publish falls back to FULL_SNAPSHOT in-trainer, stalling training for the full publish window. EDPP scaling assumptions break when the trainer stalls mid-publish. Fix: confirm and update the JK allowlist entry for the model to enable sparse delta. (Learned 2026-05-06: OT autolearn) |
| NCCL timeout recurs across multiple PT2-enabled models (e.g. Threads LSR, Reels IFU MTML) even after restart — same alert fires 3+ consecutive shifts | PT2 recompilation (max_autotune=True) causes rank desynchronization that triggers the timeout. Disable max_autotune=True for the affected OT job and consult pt2_stack_for_recsys_training oncall before re-enabling. Restarting without this change will not hold. (Learned 2026-05-06: OT autolearn) |
| Sparse delta publisher queue (Hedwig ml_realtime_publisher_sparse) saturates to 100% / exceeds 25 GB threshold — T3 publishing halted | Trainer is generating deltas faster than Hedwig consumers can drain. Check current ODS queue metric for the model; if still elevated follow runbook https://fburl.com/wiki/km3v8nam. Do not tune trainer throughput before verifying consumer lag is the bottleneck. (Learned 2026-05-06: OT autolearn) |
| Jobs stuck with TORCHELASTIC events not completing — Gloo deadlock, 1000+ stuck events accumulate over days | Root cause is TORCHELASTIC_USE_AGENT_STORE conflict at subprocess spawn. Check for this flag conflict in job config; track D103808649 for the fleet-level fix. Add a fleet alert for this error pattern so it does not silently accumulate. (Learned 2026-05-06: OT autolearn) |
| PT2 `max_autotune=True` + dynamic tensor shapes → racy rank divergence → NCCL timeout → periodic job restarts (seen on Threads Feed LSR 2129246926 and Reels IFU MTML 883552231) | Set `max_autotune=False` as immediate mitigation; loop in `pt2_stack_for_recsys_training` oncall for a proper fix; do NOT treat as generic NCCL/network issue — root cause is PT2 recompilation divergence, not network or straggler nodes (Learned 2026-05-07: OT autolearn) |
| Publisher subprocess launch causes Gloo barrier deadlock; ~55 stuck training events/day fleet-wide; symptom looks like a hang with no clear error | Check whether `TORCHELASTIC_USE_AGENT_STORE` is unset before subprocess spawn — if not, C++ TCPStore falls back to wrong store type causing the deadlock; fix is D103808649; review related diffs D103808619 and D103162679 as a coordinated stack (Learned 2026-05-07: OT autolearn) |
| All ESR OT models report zero shadow samples for days/weeks; `filter_empty_shadow_label` dropping all shadow rows silently | First check whether an upstream LSR VM change recently modified pselect label output — empty shadow labels are filtered out before training, so the symptom is data starvation with no explicit error; escalate to the LSR team owning the VM change rather than debugging the OT pipeline itself (Learned 2026-05-07: OT autolearn) |
| OT jobs stuck fleet-wide with Gloo barrier deadlock; 1000+ stuck OT events accumulate over days | TORCHELASTIC_USE_AGENT_STORE env var inherited by delta-publish subprocess causes C++ TCPStore wrong-store fallback and deadlock. Check: `meta ai.mast-job error --name=JOB` for barrier/store errors; look for subprocess spawn paths that inherit env. Fix: unset TORCHELASTIC_USE_AGENT_STORE before subprocess spawn (pattern from D103808649). (Learned 2026-05-07: OT autolearn) |
| Job is RUNNING with no errors but SilverTorch full_snapshot gaps of multiple hours after an in-place checkpoint reload | In-place checkpoint reload does not destroy and reset the full publisher timer, so the next snapshot publish is silently delayed. Check GMPP publish log for timer-not-reset signal. Fix: land D103621691-style patch that resets the publisher timer on in-place reload. Do not rely on job health status alone — check snapshot cadence directly. (Learned 2026-05-07: OT autolearn) |
| StuckJobException in backward pass on OT training job with no OOM or NCCL errors, potentially coinciding with a recent config/default change | Triton autotuning deadlock in backward pass under online training workloads (can be silently enabled by a platform default-config change). Workaround: disable triton autotuning in the job config. Confirm whether a recent platform default flipped this on by checking config change log near the onset date. (Learned 2026-05-07: OT autolearn) |
| SilverTorch-integrated model misses full_snapshot after in-place checkpoint reload; subsequent publishes blocked indefinitely | In-place checkpoint reload resets model weights but does NOT reset the full publisher timer in SilverTorch. Check `get_last_n_checkpoint_metadata_tool` for checkpoint timeline and publish logs for timer reset evidence. If timer not reset, escalate to SilverTorch oncall with D103621691 as reference fix. (Learned 2026-05-08: OT autolearn) |
| Fleet-wide DPP stuck-queue / Gloo barrier deadlock affecting 1,000+ events; multiple models stall on data ingestion | Check `TORCHELASTIC_USE_AGENT_STORE` env var — if corrupted/set incorrectly, C++ TCPStore falls back to wrong store causing Gloo barrier deadlock → DPP starvation. Verify D103808649 deployment status. Cross-reference with `meta ai.mast-job error --name=JOB` and DPP starvation % in Scuba. (Learned 2026-05-08: OT autolearn) |
| CPU OOM or full-snapshot publish blocking significant training time (≥60%) immediately after enabling delta streaming | Enabling delta publish without pre-rollout sizing causes immediate stability failures. Before enabling: verify worker CPU allocation, confirm delta diff generation config, and validate publish buffer sizing against expected snapshot delta size. No pre-rollout checklist exists — treat first rollout as high-risk and monitor publishing latency immediately post-enable. (Learned 2026-05-08: OT autolearn) |
| Model age spiking / model growing stale despite OT job running — but root is NOT SV rejections or Conveyor | Check GMPP publish logs for `wait_generate` duration per cycle. If >2 min, root is in-trainer full snapshot publish (gloo, 1500s timeout) blocking the training loop each generate step. Propose decoupled (out-of-trainer) publish. Pattern seen in Feed ESR (S657860), Feed LSR (S659917), Threads LSR (S652695) — 3rd consecutive week. (Learned 2026-05-08: OT autolearn) |
| All snapshots for a model stuck in "creating" phase, blocking inference loading after checkpoint generation | Check UMM snapshot status for the model. If stuck >1h, escalate to model_processing oncall to clear the stuck record. Verify OT job is still generating new checkpoints in parallel (T270362292). (Learned 2026-05-08: OT autolearn) |
| OT job did not auto-resume after MRB (model rebase) — TMS reconciliation silent, no restart triggered | Manually trigger TMS restart for the OT job. Verify TMS reconciliation config explicitly covers post-MRB scenarios for the model family. Document as a gap in OT restart mechanism tracking (S660717). (Learned 2026-05-08: OT autolearn) |
<<<<<<< dest:   65c8834f3c04 - sxhan: [claude-queue] review-monitor pass T269...
| All CPU/GPU partitions for a model drop to streaming.num_messages.success = 0 simultaneously | Simultaneous cross-partition failure = trainer publisher stopped, not serving side. Check GMPP publish stats for the model, then restart OT job if trainer stopped. Don't investigate partitions independently. (Learned 2026-05-12: OT autolearn) |
| 5–10 separate streaming alerts fire at the same time across partitions of the same model | Treat as one incident: single trainer failure causing alert flood. Triage the trainer job, not each partition alert in isolation — no deduplication exists at the model level today. (Learned 2026-05-12: OT autolearn) |
| Job shows RUNNING in MAST but training iterations stop advancing (StuckJobException) | Publish pipeline is likely blocking the main training thread (deadlock). Run `meta ai.mast-job error --name=JOB` (or `get_mast_job_error_tool`) to identify the blocking operation. Add specific model IDs to the tracking task and escalate if unresolved after 48h. (Learned 2026-05-12: OT autolearn) |
| Multiple CPU partitions (e.g., cpu0–cpu5) for the same model all lose streaming enablement simultaneously | Same-time multi-partition failure strongly indicates a **trainer-side publish stop** (job restart, kill-switch) — not independent per-partition runtime failures. Check whether the training job restarted or a publish kill-switch was toggled rather than investigating each partition individually. (Learned 2026-05-12: OT autolearn) |
| Online training NE (normalized entropy) sudden increase task filed but no model ID in the task description | Cannot run `get_online_training_status_info_tool` or inspect tensorboard without a model ID. **First action: ask task assignee to provide the model ID**, then run `get_online_training_status_info_tool` and check NE trends + training QPS to confirm whether the spike is active or resolved. (Learned 2026-05-12: OT autolearn) |
| Oncall alerts CLI exits with code 1 / TLS thread pool error (e.g., due to a Mandoline overload SEV) | Automated alert triage is blocked. **Manually check the monitoring portal** at https://www.internalfb.com/intern/alarms/ for active alerts until the SEV is resolved. Do not assume no alerts exist just because the CLI failed. (Learned 2026-05-12: OT autolearn) |
| Streaming enablement alerts fire with Owner:0 and land in the shared mrs.ot Workplace group for models you don't recognize | Run OT status tool to verify model ownership before acting. If model's oncall is not mrs_online_training, the alert is mis-routed noise — filter and ignore. Do not triage on behalf of another team. (Learned 2026-05-13: OT autolearn) |
| SLICK current-day freshness numbers look alarming or incomplete during oncall shift | This is expected — MUMU limitation means current-day data in SLICK is always partial. Ignore current-day rows; evaluate prior-day freshness instead. No action needed unless prior-day data also shows issues. (Learned 2026-05-13: OT autolearn) |
| `meta alerts --oncall mrs_online_training` exits with code 1 / returns no data | CLI may be disabled due to an active SEV (e.g., S650203 excessive Mandoline load). Check monitoring portal manually for alerts. Automated triage pipelines will also lose alert data — note alert count as unknown and flag in triage report. (Learned 2026-05-13: OT autolearn) |
| Multiple MVAI OT jobs hit StuckJobException simultaneously across models | Pull MAST state transition logs for all affected models at the failure window; look for shared infra root cause rather than per-model cause — concurrent stuck jobs signal platform-level issue, not individual model config (Learned 2026-05-13: OT autolearn) |
| Received OT streaming alert for a model that turned out not to be mrs_online_training | Before investigating, run `get_online_training_status_info_tool` (or check OT registry) to confirm ownership. If model has null OT registry or is owned by a different oncall (e.g., ig_feed_retrieval), close/route rather than debug (Learned 2026-05-13: OT autolearn) |
| SLICK shows same-day data staleness and oncall investigates as a live incident | Same-day SLICK data can lag due to MUMU dependency with no guaranteed ETA. Check MUMU pipeline status before treating as a real reliability regression; add runbook note so future oncall recognizes the pattern (Learned 2026-05-13: OT autolearn) |
| OT job snapshots stuck in "creating" phase, job goes DEAD — pattern recurs across multiple models (T270362292, T222910669, T222684618) | Check job logs for timeout/GMPP errors/snapshot validation failures: `meta ai.mast-job error --name=JOB`. Then inspect full snapshot publishing pipeline and GMPP logs for TimeoutError or generation failures. Common trigger: new model or recent config change. (Learned 2026-05-13: OT autolearn) |
| SLICK "today" freshness data looks stale or shows false negatives during same-day triage | SLICK has a known trailing-data limitation for same-day metrics — use ODS direct queries instead for current-day freshness checks. No ETA from MUMU team on fix. (Learned 2026-05-13: OT autolearn) |
| OT job is DEAD with intended state PAUSED — unclear whether issue was resolved or job was just stopped as a workaround | Distinguish intentional pause from workaround: check task/SEV for explicit "paused pending fix" note. If none, assume workaround — investigate snapshot/config root cause before re-enabling. DEAD+PAUSED without a linked fix task = likely unresolved. (Learned 2026-05-13: OT autolearn) |
| SilverTorch publish snapshot stuck in "creating" state for days with no alert firing | No auto-alert exists for this state — discovered manually. Check SilverTorch snapshot state for the model entity. If stuck in "creating" for >24h, force-expire the snapshot and trigger a fresh publish. Verify the MAST training job is alive and checkpointing normally (Learned 2026-05-13: OT autolearn) |
| Alerts for non-mrs_online_training models (e.g. ig_feed_retrieval, p92_relevance_ranking) landing in mrs.ot oncall group, creating toil | Before investigating a streaming alert, confirm model ownership: `meta ai.model-type metadata --name=<model>` or check OT status tool. If model is not mrs_online_training scope, route back to correct oncall and file a task to fix the alert routing config to remove that model from mrs.ot group (Learned 2026-05-13: OT autolearn) |
| NE (normalized entropy) spikes suddenly — model predictions degraded | Debug in order: (1) check data freshness for training partitions, (2) inspect Tensorboard NE trend to pinpoint exact step/time of divergence, (3) check for upstream label or feature schema changes around that date, (4) inspect checkpoint integrity if spike coincides with a checkpoint load. If NE recovered silently, document root cause and close the task (Learned 2026-05-13: OT autolearn) |
| Multiple models in the **same partition** hit zero streaming messages within minutes of each other (e.g., default_gti96g1: models 2132070936 and 2137783103 fired 48 min apart) | Treat as a shared infrastructure event, not independent model failures. Check for a common MAST restart, gflag change, or platform rollout in that partition window. Query GMPP Scuba for both models together; a single recovery action may resolve all. Escalate to streaming_infra_oncall if MAST is publishing but runtime still receives nothing. (Learned 2026-05-14: OT autolearn) |
| OT snapshot stuck in **"creating" phase** for multiple days (e.g., LSR model T270362292, stuck since May 8) | Check the task for comments since last scan. If no progress, run `OT status check` on the model and inspect MAST job logs for GMPP/SilverTorch timeout, checkpoint write stall, or publish pipeline hang. Do not wait passively — 2+ days with no update needs active escalation to the publish pipeline oncall. (Learned 2026-05-14: OT autolearn) |
| **SLICK today's data is trailing** — oncall notices metrics for the current day look unexpectedly good or data volume is lower than expected | SLICK has a known limitation where today's data lags. Before making a triage call based on today's SLICK data, confirm freshness: check if the dashboard note says "today's data trailing" or ask the MUMU team. Do not dismiss an active alert just because SLICK today shows no anomaly — use yesterday's data or Scuba directly for confirmation. (Learned 2026-05-14: OT autolearn) |
| Multiple models fire streaming zero-messages alerts on the **same partition type** within minutes of each other (e.g., two models on default_gti96g1 within 45 min) | Treat as a shared infrastructure event, not independent trainer failures. Cross-reference all affected model IDs, check for a common partition restart / gflag push / kill-switch change, and file a **joint** investigation ticket with streaming_infra_oncall listing all model IDs. Do not triage each model in isolation. (Learned 2026-05-14: OT autolearn) |
| All snapshots stuck in **CREATING** for days — training/checkpointing is healthy but model never loads to inference | Check GMPP publish log for NaN/GPU-lowering errors or TimeoutError (these silently block snapshot promotion). Also query snapshot validator Scuba for rejection events. If NaN: ban the bad checkpoint and restart OT. This is a recurring pattern (~4× in 12 months); escalate if no clear error in publish logs. (Learned 2026-05-14: OT autolearn) |
| ~70% DPP starvation and model appears to publish every cycle, but training example age is high — no obvious error messages | Check whether model_entity_id is in the JK allowlist for delta publishing. If missing, the DeltaOnlyPublisher silently falls back to FULL_SNAPSHOT every cycle, starving the pipeline. Fix: add model_entity_id to the JK allowlist. Symptom looks like a data or throughput issue but root cause is publish-path config. (Learned 2026-05-14: OT autolearn) |
| Training example age spikes with a periodic read/pause cycle (e.g., 20-min read, 15-min pause); EDPP scaling defeated; multiple secondary SEVs opened | Root cause is often the synchronous in-trainer full-snapshot publish blocking the scribe reader. Instrument first: add publish duration logging, checkpoint save duration, and EDPP starvation counters to confirm pause source. If publish is culprit, apply DeltaOnlyPublisher decoupling to move publish off training critical path. Interim relief: reduce full-snapshot interval or increase --scribe-read-timeout-secs. (Learned 2026-05-14: OT autolearn) |
| 3+ unrelated tier-1 models all fire "streaming update enablement detector" alerts within a few hours on the same day | Treat as systemic infrastructure event, not independent trainer failures. First check GMPP Scuba publish stats for all affected models. If GMPP shows zero publishes across all models → T2 failure (check/restart MAST jobs). If GMPP shows healthy publishes → escalate to streaming_infra_oncall (T4). Investigate platform changes deployed that day before chasing per-model root causes. (Learned 2026-05-14: OT autolearn) |
| LSR OT snapshot stuck in "creating" state for days with no alert or auto-recovery; trainer MAST job still running but snapshot unusable for serving | SilverTorch lacks a watchdog for this hung state. Check SilverTorch logs for the affected model; if no progress, manually cancel the stuck snapshot and trigger fresh creation. If the issue recurs, escalate to the SilverTorch team (possible deadlock or resource exhaustion in the creation pipeline). (Learned 2026-05-14: OT autolearn) |
| Training example age spikes during full-snapshot publish cycles — trainer blocks on scribe read for 20–30 min, starving the GPU of fresh data | Check if trainer runs a read/pause cycle (e.g. 20-min-read / 15-min-pause) that defeats EDPP elastic scaling. Check DPP starvation metrics in EDPP dashboard. Long-term fix: DeltaOnlyPublisher decoupling — run full-snapshot and delta publishes on separate paths so trainer is never blocked. Short-term: reduce pause duration or switch to continuous-read mode. Also verify `dpp_version` is set in app_metadata or pipeline-QPS charts will be blind. (Learned 2026-05-14: OT autolearn) |
| Multiple streaming enablement alerts fire simultaneously for all partitions of a single model (e.g. cpu0–cpu5_t1 all at zero) | This is one root problem, not five. First check GMPP Scuba for the model_id to determine whether the trainer is still publishing. If delta publishes are occurring at the trainer → escalate to streaming_infra_oncall (T4 connectivity issue). If publishes stopped → investigate trainer job status directly. Do not work five tickets independently. (Learned 2026-05-14: OT autolearn) |
| Multiple unrelated models on the same cluster all lose streaming updates around the same time | Suspect a shared upstream cause before investigating each model separately. Check whether they share a trainer host, cluster hardware, or a recent code/config push. Same cluster suffix (e.g. gti96g1) appearing across alerts is a strong signal of cluster-level failure, not per-model regressions. (Learned 2026-05-14: OT autolearn) |
| Snapshots stuck in CREATING state — inference loading blocked, MAST job RUNNING, checkpoints confirmed in MLHub but snapshot never transitions to READY | Query snapshot validator Scuba for the model ID to check NE/quality rejection. If CREATING record is orphaned from a prior training attempt, clear via ip_runtime or UMM admin API. Also check Conveyor pipeline failures blocking snapshot transition. (Learned 2026-05-15: OT autolearn) |
| Training example age (TEA) spikes despite MAST showing RUNNING/0 restarts — in-trainer full-snapshot publish (gloo, 1500s timeout) blocks Scribe reader for ~2–3 min per cycle, causing DPP starvation and GPU underutilization | Check publish logs for FULL_SNAPSHOT mode frequency. Implement DeltaOnlyPublisher decoupling to stop blocking the Scribe reader, or extend full-snapshot interval (30 min → 60 min) as a mitigation. Fix dpp_version='' in app_metadata to restore SLICK pipeline-QPS charts for diagnosis. (Learned 2026-05-15: OT autolearn) |
| Delta publishing configured correctly in model code but publish logs show FULL_SNAPSHOT every cycle — DPP starvation ~70%, GPU SM utilization ~26% (healthy >60%) | Platform JK allowlist (ai_infra/delta_publishing:sparse_enabled_model_entity_ids) may be returning False for the model entity ID, silently overriding the model's delta config. Add the model entity ID to the allowlist (ai_infra/delta_publishing team action) AND register a valid baseline snapshot in UMM. Verify fix by confirming publish logs show PublishMode.SPARSE_DELTA and DPP starvation drops below 20%. (Learned 2026-05-15: OT autolearn) |
| Training example age spikes rhythmically; trainer shows ~57% read time with periodic pauses; EDPP cannot sustain throughput during pause windows, causing ~15 min freshness loss per cycle | Find the trainer config param controlling the read/pause cadence and eliminate or shorten the pause. Verify training example age normalizes in tensorboard after the change. Check EDPP scaling separately — it cannot compensate for trainer-side pauses. (Learned 2026-05-15: OT autolearn) |
| Scribe reader stalls ~2-3 min every 30 min; training example age spikes in sync with full-snapshot publish cycles; model is publishing in blocking mode from inside the trainer | Evaluate DeltaOnlyPublisher to decouple full-snapshot and delta publish paths so scribe reads are not blocked. Also verify dpp_version is set in app_metadata — empty dpp_version blinds scribe-category and pipeline-QPS monitoring. (Learned 2026-05-15: OT autolearn) |
| OT model snapshots stuck in "creating" phase for days; checkpoint exists in storage but inference platform never finalizes serving update | Run `get_last_n_checkpoint_metadata_tool` on the model ID to confirm snapshot state. If OVERALL_TIMEOUT (ServiceRouterError 5400000ms) appears in snapshot transition logs, escalate to IP/model-processing oncall — this is outside trainer scope. (Learned 2026-05-15: OT autolearn) |
| Training shows 15–30 min stall cycles; DPP reader QPS drops to ~1k; scribe reader blocked for 2–3 min periodically | Root cause: in-trainer **full snapshot publish blocks DPP/scribe consumer** during snapshot creation. Check publish logs for FULL_SNAPSHOT events coinciding with QPS drops. Fix: enable `DeltaOnlyPublisher` to decouple snapshot creation from the read path; register model in sparse delta streaming allowlist before new OT launches. (Learned 2026-05-15: OT autolearn) |
| Snapshot stuck in `CREATING` state for hours/days; serving freshness degrading; no alert fired | No existing alert covers this. Diagnose: run `get_last_n_checkpoint_metadata_tool` to confirm state, query snapshot validator Scuba for `OVERALL_TIMEOUT` or transition errors, follow snapshot-stuck runbook. If no error found, escalate to IP/model-processing oncall. **Add alert**: snapshot in CREATING > 2h should page mrs_online_training. (Learned 2026-05-15: OT autolearn) |
| Trainer killed after ~6000s with `DPP_WORKER_STUCK_FULL_OUTPUT_QUEUE`; multiple consecutive versions crash across regions; TMS restart does not fix | Root cause: `_preload_item_pool()` (trainer.py → serving_aware.py) blocks the DPP consumer during checkpoint load, filling the output queue until timeout kills the trainer. TMS restart cannot help because the block recurs on every load. Check `run_config` for item pool size or negative sampling changes; fix the blocking call or reduce pool size before restarting. (Learned 2026-05-15: OT autolearn) |
| Streaming enablement alerts fire on all CPU partitions (cpu0-5) simultaneously at the exact same timestamp | All-at-once multi-partition alerts = trainer-side publish failure, NOT a per-partition serving issue. Do not triage each partition separately. Check MAST job state transitions around the alert time and verify sparse delta is flowing via GMPP Scuba. (Learned 2026-05-15: OT autolearn) |
| OT snapshot stuck in "creating" phase indefinitely; SilverTorch created the snapshot handle but the underlying write never completes | Publish deadlock pattern. Check SilverTorch publish log for the stuck snapshot ID. Verify whether publish timeout patch is landed. If not, manually kill the stuck publish handle and restart the trainer. Track via the corresponding task. (Learned 2026-05-15: OT autolearn) |
| Trainer publishes FULL_SNAPSHOT every cycle instead of sparse delta; DPP pipeline starved 50-70%; no error logged | Silent JK allowlist fallback: when JK allowlist returns False for a model, OnlineStreaming silently falls back to full-snapshot mode with no error. Check if the model ID is in the JK sparse delta allowlist. Fix: add model to JK allowlist AND register UMM snapshot. Startup-time validation does not yet exist for this failure mode. (Learned 2026-05-15: OT autolearn) |
=======
| In-trainer full snapshot stalls Scribe reads ~30 min per cycle, causing DPP starvation (~70%) and model staleness even though the job appears healthy | Switch to DeltaOnlyPublisher; extend snapshot interval from 30 min → hourly; verify `dpp_version` field is not empty (empty means pipeline QPS is hidden). Diagnose with `get_online_training_status_info_tool(<model_id>)` and dai_modelstore Scuba. (Learned 2026-05-16: OT autolearn) |
| Streaming detector fires 8+ partition alerts in bulk days after an upstream outage (e.g., FTMS outage) — models appear to resume but streaming is silently broken post-restart | Do not treat bulk streaming alerts as independent model failures. Check upstream outage history first. Verify streaming recovered via GMPP Scuba (`fburl.com/scuba/gmpp/1qdb275c`); if not recovered, check MAST errors and toggle kill-switch. (Learned 2026-05-16: OT autolearn) |
| ZippyDB degradation → XStream quota exhausted → DPP starvation cascade causes ~19hr model staleness and recurs as exact repeat of a prior incident | When resolving a ZippyDB/XStream/DPP cascade SEV, ensure the XStream quota-increase infra task is closed before closing the SEV — an open quota task guarantees recurrence. Audit open infra tasks on the ZippyDB → XStream path before SEV resolution. (Learned 2026-05-16: OT autolearn) |
| Job shows RUNNING in MAST with no task failures, but training example age spikes and DPP starvation is high (~70%) | In-trainer full-snapshot publishing contends with training data reads (recurring class: S659474, S635390, S652695). Check if full-snapshot publish runs in-trainer (gloo process group). Fix: complete `--scribe-read-timeout-secs 120` patch, switch to DeltaOnlyPublisher to decouple publish from read path, or disable/shorten trainer read/pause cycle. Verify DPP starvation drops below 20% after fix. (Learned 2026-05-16: OT autolearn) |
| Main model and holdout model both fire FULL_SNAPSHOT missing alerts within seconds of each other | Simultaneous failure of main + holdout indicates shared publishing pipeline failure, not model-specific regression. Skip per-model investigation; go straight to SilverTorch/GMPP publish logs for the shared model ID. Verify OT training job is running and check when last FULL_SNAPSHOT was published vs alert fire time. (Learned 2026-05-16: OT autolearn) |
>>>>>>> source: 15ca5ab64d94 - dennyzhang: auto: notes sync 2026-05-17_0000 [...
| Job shows `online_ready` in TMS after a starcart diff lands but no MAST job is launched — user has to restart manually | Check starcart actuator logs around the diff-land time: `meta ai.mast-job scheduling --name=JOB`. Verify starcart config for the model. File a follow-up linking to the restart-gap analysis task if reproduced. (Learned 2026-05-17: OT autolearn) |
| `[Invalid Detector - No Data]` alert fires — looks like a production failure | Not an actual model failure — alert config is pointing to a stale or retired model ID. First check: is the model still active in MAST/TMS? If retired, remove or update the OneDetection alert config and link the finding to the alert-audit task. Do not page a model owner before confirming the model is live. (Learned 2026-05-17: OT autolearn) |
| NaN/NE alert fires on a model that had a prior NaN SEV (bad training data) within the last few weeks | History of data-caused NaN is a strong prior — check the upstream training data pipeline first before tuning LR or reverting code. Steps: verify recent data partitions landed cleanly, check DPP metrics for starvation, then revert+ban bad checkpoints via TMS if NaN is confirmed. Generic LR reduction without data validation will not resolve a data-quality root cause. (Learned 2026-05-17: OT autolearn) |
| OT e2e latency detector fires "[Invalid Detector - No Data]" with no timeseries since model was last seen | Check if the model has an active OT job (`meta ai.mast-job metadata --name=<job>`). If no active job exists, the detector is targeting a retired model — disable it. If job is active, detector config is misconfigured (wrong model ID or metric path). (Learned 2026-05-17: OT autolearn) |
| `dpp_version=""` in TIER_2 job metadata — scribe-category charts and pipeline-QPS tools show no data | Empty `dpp_version` blocks DPP observability tooling. Check job metadata for the `dpp_version` field; if blank, it was not set at launch. Work with model owner to repopulate or relaunch with correct DPP version tag. Cross-reference task 2.51. (Learned 2026-05-17: OT autolearn) |
| Starcart fails to auto-start OT job after a job stop or registry state transition (recurring pattern, seen ~every few months) | Starcart trigger conditions are not reliably met after certain stop/registry transitions. First: identify which job was affected and whether manual restart was required. Then document which trigger condition failed and cross-reference task 2.12 gap analysis. Do not assume starcart will self-recover — manual intervention is likely needed. (Learned 2026-05-17: OT autolearn) |
| Starcart (TMS auto-start) silently failed to restart OT job — job stayed offline with no visible error, no alert until manually noticed | Check TMS auto-start config first (`meta ai.mast-job metadata --name=JOB`, look for starcart/TMS restart settings). Do NOT assume TMS will self-heal; cross-reference T271708262 pattern. If starcart silent failure is suspected, escalate to TMS oncall immediately — every minute offline is training loss. (Learned 2026-05-17: OT autolearn) |
| "[Invalid Detector - No Data]" HIGH alert fired for a model — assumed real publishing failure, escalated unnecessarily | Before investigating as a real incident, confirm with model owner whether the model is configured for sparse delta publishing. If model doesn't produce sparse deltas, the detector timeseries will always be empty — disable or fix the detector, not the model. Pattern tied to alert audit project task. (Learned 2026-05-17: OT autolearn) |
| Scribe `client_lag_in_seconds` HIGH alert fired for OT job — lag means training data arrives stale, degrading freshness | Root cause is upstream of MAST (DPP backlog or Scribe producer slowdown). Check onedetection alert dashboard for current lag level. If still elevated, engage **DPP oncall** (not MAST oncall). Notify model owner. Do not look for MAST-side fixes until upstream Scribe/DPP lag is ruled out. (Learned 2026-05-17: OT autolearn) |
| Starcart diff landed but OT job did not auto-restart (job stays in prior state, user restarts manually) | TMS reconciliation timing race — check TMS state for model is ONLINE_READY, inspect starcart reconciliation log for trigger gap. Manually restart job. Document in T259920238 (recurring since Dec 2025, 4+ instances). (Learned 2026-05-17: OT autolearn) |
| Alert fires with "[Invalid Detector - No Data]" prefix | This is NOT a model issue — the detector itself has no data (stale config). Model was likely refreshed/rebased but the alert detector was not updated. Audit and refresh detector config for that model before investigating training. (Learned 2026-05-17: OT autolearn) |
| NaN in calibration metric (e.g. variable_step_metrics/calibration/global/window/skip_post/train) blocks checkpoint verification gate for a Tier-1 model | Run `mvai trace-doctor analyze -j <mast-job>` to confirm NaN scope. If NaN cleared on its own, no action needed. If sustained, ban the affected checkpoint and restart per standard procedure to unblock serving. (Learned 2026-05-17: OT autolearn) |
| NaN detected in training (calibration/skip_post metric) and ~21h later FULL_SNAPSHOT publishing alerts fire on the same model | Treat as compound failure: NaN is the root cause; FULL_SNAPSHOT missing is a downstream symptom. Resolve NaN first (gradient clipping / model restart), then verify publishing job resumes automatically. Check `meta ai.mast-job error --name=mvai-training-online-<model_id>` and correlate alert timestamps. (Learned 2026-05-18: OT autolearn) |
| `oncall-alerts.json` contains `{"error": "alerts CLI failed (exit=1)"}` | CLI alert source is unavailable — manually check the oncall portal at https://www.internalfb.com/intern/omh/mrs_online_training to catch any alerts not captured in the feed. Do not assume the alert list is complete when CLI exit code is non-zero. (Learned 2026-05-18: OT autolearn) |
| NaN alert (even LOW priority) on Tier-1 prod model was not escalated; 21h later both baseline and holdout stopped publishing FULL_SNAPSHOT due to NaN-corrupted checkpoint | Treat any NaN/NE violation on Tier-1 as P1 regardless of alert severity tag. Immediately run publishing health check (`ot-delta-status.sh <model>`) and verify MAST state. Do not wait for downstream alerts — NaN → checkpoint corruption → publish failure is a known cascade. (Learned 2026-05-18: OT autolearn) |
| Alert flapping every 1–2h for multiple days with owner=0 and zero resolution comments | Unowned alerts will never self-resolve. First step: identify the correct team from tier/component (e.g., T4 Hedwig consumer → Hedwig oncall, T1/T2 Scribe → Scribe oncall). Route immediately and confirm OT action (e.g., verify publish cadence unchanged). Do not leave in carryover across multiple scan cycles. (Learned 2026-05-18: OT autolearn) |
| Holdout shows elevated `scribe_read_proxy` client lag on sparse delta path shortly after a FULL_SNAPSHOT publish began | FULL_SNAPSHOT publish can block the scribe reader — this is a known interaction (ref task 2.51). Check if a FULL_SNAPSHOT publish was in progress at alert onset. Compare holdout vs baseline training QPS. Mitigation: `--scribe-read-timeout-secs 120` on the affected job. (Learned 2026-05-18: OT autolearn) |
| Training example age spikes for OT model; EDPP auto-scaling not kicking in despite elevated age | Suspect trainer read/pause cycle (e.g., 20-min read / 15-min pause) where the in-trainer publish phase blocks incoming data intake, defeating EDPP. Check trainer config for pause patterns; confirm DeltaOnlyPublisher decoupling status (decoupling lets publish happen async so EDPP can scale normally). Also check if `dpp_version=''` in app_metadata is blocking pipeline-QPS charts. (Learned 2026-05-18: OT autolearn) |
| OT job is stopped/stale and no alert fired — Starcart (automated restart) should have restarted it but didn't | Starcart auto-restart has a known reliability gap: jobs can stay stopped indefinitely after Starcart silently fails. Manually identify the stopped OT job, restart it, and log the root cause in T259836090 (restart gap analysis). Don't wait for Starcart to self-recover. (Learned 2026-05-18: OT autolearn) |
| HIGH priority e2e latency alert on sparse delta with no obvious MAST failure | Query GMPP Scuba for `scribe_read_proxy.client_lag_in_seconds` — this is the key metric indicating sparse delta delivery pipeline lag. Also check if the alert has owner=0 (unassigned); if so, route to the team owning the sparse delta channel for that model rather than investigating the training job itself. (Learned 2026-05-18: OT autolearn) |
| `scribe_read_proxy client_lag_in_seconds` spiked, DPP not reading Scribe fast enough, trainer starved — e2e latency from sample to sparse delta jumped | Check `meta ai.mast-job error --name=JOB` + scribe read lag metric; restart job as immediate mitigation, then check if DPP or Scribe had an upstream issue at spike time; monitor e2e latency post-restart to confirm recovery (Learned 2026-05-18: OT autolearn) |
| Model has recurring NaN/NE violation alerts across multiple oncall weeks with no fix landed between waves | Treat repeat NaN waves as a structural problem, not transient; do not just restart — escalate to model owner to land a root-cause fix; track in carryover until a code/config change is confirmed merged (Learned 2026-05-18: OT autolearn) |
| CHC alert fires 7+ times in one week for sparse delta in-order consumption rate dropping below threshold (intermittent drops with recovery then re-drop), zero owner responses on alert posts | Check T3 publisher cadence stability first (publisher congestion shows intermittent pattern); if publisher is stable, escalate to T4 Hedwig consumer team; verify alert routing — repeated no-response suggests T4 CHC alerts may be mis-routed to wrong oncall (Learned 2026-05-18: OT autolearn) |
| NaN alert (LOW severity) fires, then 24-48h later FULL_SNAPSHOT missing (HIGH severity) fires for same model | NaN is the root cause — FULL_SNAPSHOT missing is downstream cascade. When FULL_SNAPSHOT missing fires, immediately look back at NaN/NE violation alerts from the prior 24-48h. Treat NaN as HIGH even if initially classified LOW; unmitigated NaN kills the publish pipeline. (Learned 2026-05-19: OT autolearn) |
| Alert fires 10+ times over multiple days with no owner assigned and no response comments | Likely a T4 (streaming infra) issue beyond OT scope. First check GMPP publish stats for sequence gaps. If clean, escalate to streaming_infra_oncall. File a task to establish ownership — chronic unowned firing = permanent blind spot without explicit triage. (Learned 2026-05-19: OT autolearn) |
| P0 SEV shows no visible progress for 3+ weeks; no new signals in oncall feed | Cross-team coordination gap, not a waiting game. Proactively contact specific named owners to confirm decoupling/fix status. Set explicit EOW deadline and document expected next action in the SEV. Silence ≠ progress on stale P0s. (Learned 2026-05-19: OT autolearn) |
| NaN loss appears at a consistent step number across multiple restarts (e.g., step ~1100), causing a cascade of DEAD model versions before TMS auto-restarts stabilize | Monitor tensorboard through the known failure step. If NaN recurs at the same step, it's a bad data batch or gradient explosion at a fixed training point — add gradient clipping first; if still failing, restore from the last clean checkpoint *before* that step rather than the most recent one. (Learned 2026-05-19: OT autolearn) |
| Same auto-start failure (e.g., Starcart OT job not launching) recurs every few months with no durable fix, each incident resolved ad-hoc and closed without runbook update | When closing a recurring infra failure (same symptom appearing 2+ times), require a runbook update with a step-by-step checklist as part of task resolution. Link the new task to the prior incident task. Escalate to a durable fix before closing. (Learned 2026-05-19: OT autolearn) |
| Alerts (e.g., sparse delta CHC) fire to the model-owner oncall (mrs_online_training) but the underlying infra component (e.g., Hedwig T4) is owned by a different team (streaming_infra_oncall) | Before investigating an alert, verify the alert's infra component owner vs the model owner. If mismatched, immediately route to the correct infra oncall team. Use `meta ai.model-type metadata --name=<model>` and cross-check alert routing config to avoid spending debug time on the wrong side. (Learned 2026-05-19: OT autolearn) |
| Training example age spikes and scribe reader stalls on an OT job doing full-snapshot publishes in-trainer (~30-min interval) | In-trainer full-snapshot publish blocks the scribe reader. Immediate fix: apply DeltaOnlyPublisher pattern (see D105185345) to decouple publish from the training loop. Mitigation diffs: D98861780 / D98079189. Restart job after landing. (Learned 2026-05-19: OT autolearn) |
| Training example age spikes on a job using a 20-min-read / 15-min-pause training pattern; EDPP horizontal scaling appears ineffective | Long pause cycles defeat EDPP scaling triggers — EDPP cannot scale horizontally when training stalls periodically. Mitigation: shorten or eliminate the pause cycle so reads are continuous. Confirm with owner whether a structural EDPP scheduling fix is needed as permanent resolution. (Learned 2026-05-19: OT autolearn) |
| CHC sparse delta in-order rate drops to 46–69% (threshold 90%) and fires repeatedly over multiple days with no clear owner | First run GMPP Scuba (https://fburl.com/scuba/gmpp/1qdb275c) for the model to check T3 publisher delta cadence and sequence numbers. If T3 publish is healthy, escalate to streaming_infra_oncall (root is T4 Hedwig consumer overload). If T3 publish is stalled, check delta publisher logs and upstream pipeline. Assigning an owner early is critical — alert owner field = 0 means no auto-escalation. (Learned 2026-05-19: OT autolearn) |
| OT job did not automatically restart via starcart (auto-start failure, no job running) | Check MAST restart logs for the affected job (`meta ai.mast-job scheduling --name=JOB`). Verify starcart configuration is correct for the model type. This is a T2 infra failure — the auto-restart safety mechanism has silently failed, so the model may be accumulating staleness without any active alert. (Learned 2026-05-19: OT autolearn) |
| EDPP resize operation blocks OT publisher and causes full-snapshot stall (scribe-read-timeout, multi-month SEV) | EDPP resize and DeltaOnlyPublisher must be decoupled — do not let an EDPP resize block the publisher path. Resolution required explicit DeltaOnlyPublisher decoupling diff. When you see scribe-read-timeout coinciding with an EDPP resize event, suspect publisher blocking first before investigating downstream consumers. Expect multiple diffs (9+ in S635390) and weeks of remediation if caught late. (Learned 2026-05-19: OT autolearn) |
| Sparse delta e2e latency alert fires as "[Invalid Detector - No Data]" — no measurements exist for model | Run `ot-delta-status.sh <model_id>` first. If publishing is healthy → detector config is stale, file fix with alert owner. If publishing has stopped → investigate T2 trainer for that model. Do NOT assume monitoring is healthy just because no data is shown. (Learned 2026-05-20: OT autolearn) |
| CHC alerts for sparse delta in-order consumption rate drop (message loss) fire daily at 35–60% loss with no assigned owner | Assign ownership immediately — these don't self-heal. Root cause is often T3 (trainer publishing messages out of order or too fast for Hedwig to consume in sequence), not just T4 (serving). Use runbook https://fburl.com/gdoc/tt269jcg. Check whether trainer publish cadence or ordering guarantees changed recently. (Learned 2026-05-20: OT autolearn) |
| OT training job stopped and starcart (automated restart) failed to auto-start it — model went stale silently | Verify job's current MAST state manually (`meta ai.mast-job metadata --name=<job>`). Confirm whether manual restart was needed. If starcart failure is confirmed, link to T259836090 (restart gap analysis). Starcart failures are silent — always cross-check job MAST state when a job goes quiet. (Learned 2026-05-20: OT autolearn) |
| Sparse delta E2E latency alert fires but shows "[Invalid Detector — No Data]" | May not be a real delivery failure — could be a T3 publishing gap or a monitoring config issue (detector receiving no data). Verify T3 publishing pipeline health and data flow before assuming a real latency violation. If publishing looks healthy, investigate detector/monitoring config. (Learned 2026-05-20: OT autolearn) |
| Calibration metric (e.g., calibration/global/window/skip_post/train) goes NaN while training loss may still appear valid | Distinct from loss NaN — indicates T2 training quality degradation in the calibration path, not the main loss. Check T2 data ingestion for missing or malformed labels and review recent feature/config changes affecting calibration targets. Do not treat as generic loss NaN (different root cause and fix). (Learned 2026-05-20: OT autolearn) |
| OT e2e latency alert fires as "[Invalid Detector - No Data]" — unclear if model stopped publishing or detector is broken | Run `ot-delta-status.sh <model_id>` to check last sparse delta timestamp. If no deltas since alert time → T2/T3 publishing failure, escalate. If deltas are healthy → detector misconfiguration, contact detector owner. Don't spend time debugging training until publishing is confirmed broken. (Learned 2026-05-20: OT autolearn) |
| `ai_infra_training_job_error_context` returns errors but timestamps are months old (job v0 stale data) — doesn't match the current alert | Always check the error timestamp before acting on the result. The tool may return stale v0 errors when current-version errors aren't accessible. Cross-reference error dates against the alert date; if they don't match, treat as "no current error context available" and fall back to `meta ai.mast-job error --name=JOB` CLI instead. (Learned 2026-05-20: OT autolearn) |
| MVAIMetricError (NE or calibration threshold exceeded) causes non-retryable exit code 255 — job halts, needs manual restart | This is the NE guard working as designed, not an infra bug. Before restarting: (1) check if calibration NaN traces to a data source issue (sparse feature divide-by-zero or batch stats); (2) look for upstream feature/data pipeline changes in the 2–4 days before the alert; (3) verify metrics have self-recovered before restart. Restarting without root-cause risks immediate re-trigger. (Learned 2026-05-20: OT autolearn) |
| OT job stops training and Starcart (automated self-healing) does NOT auto-restart it — freshness silently degrades until manual intervention | Verify Starcart is properly configured for the model: check the MAST job config for the starcart restart policy, confirm the model is in the Starcart-managed tier list. If missing, file a task to onboard. Cross-check all Tier-1 OT models proactively, not just the one that failed. (Learned 2026-05-20: OT autolearn) |
| OT e2e latency sparse delta alert fires as `[Invalid Detector - No Data]` — no meaningful signal fires because the detector has nothing to evaluate | This is a broken monitoring detector, not a training failure. Steps: (1) verify the model entity ID in detector config matches the actual model ID, (2) check MAST job status for `mvai-training-online-<model_id>` to confirm job is running, (3) if detector query is wrong, fix it or file a task with the alert owner. Do not triage as a training issue. (Learned 2026-05-20: OT autolearn) |
| Sparse delta CHC alert fires repeatedly (8+ times over multiple days) with no owner assigned and no comments — in-order consumption rate drops to 40–65% in periodic bursts | Chronic unowned alerts indicate a publish-side disruption pattern, not random noise. Debugging shortcut: correlate CHC alert timestamps with OT publisher checkpoint save events or job restart windows — these are the most common triggers for rate spikes on the sparse delta channel. Assign owner immediately; reference runbook https://fburl.com/gdoc/tt269jcg. (Learned 2026-05-20: OT autolearn) |
| FULL_SNAPSHOT publish stuck 4+ hours, blocking all training threads and stalling Scribe reader / EDPP resize | Pull GMPP publish logs for timeout errors (`meta ai.mast-job error --name=JOB`); if full-snapshot frequency is excessive, consider DeltaOnlyPublisher decoupling to decouple publish from training iteration. Pattern: in-trainer full-snapshot generation holds all training threads until complete. (Learned 2026-05-21: OT autolearn) |
| Sparse delta e2e latency elevated and Scribe client lag alert fires after model restart (high restart count) | Check DPP consumer lag and Scribe reader throughput first. Elevated latency immediately post-restart is often post-restart warmup, not an infrastructure failure — confirm whether the alert self-cleared ~30 min after restart. If OT infra is healthy, route to the model's oncall team rather than treating as an OT incident. (Learned 2026-05-21: OT autolearn) |
| FULL_SNAPSHOT stuck for 4+ hours then silently self-resolved with no oncall action documented | Do not close the task until you confirm whether it self-resolved or was manually fixed. Silent recovery is a monitoring gap — document root cause before closing (T272335322 example). (Learned 2026-05-21: OT autolearn) |
| T4 CHC sparse-delta alerts firing repeatedly (4+ times in one week) to a group inbox with Owner: 0, no action taken | Recurring CHC alerts with no assigned owner get silently dropped. When alert Owner is 0, manually assign oncall before clearing. Repeated sparse-delta loss may indicate T3 publishing instability upstream, not just a Hedwig serving blip — escalate after 2+ fires in a week. (Learned 2026-05-21: OT autolearn) |
| OT job did not auto-start after prior run completed (Starcart restart mechanism silent failure) | If an OT job is unexpectedly not RUNNING after its expected restart window, check Starcart auto-restart logs before assuming job failure. Verify whether the gap is one-off or systematic and feed into restart-latency gap tracking. (Learned 2026-05-21: OT autolearn) |
| Oncall group flooded with CHC/alert noise for models owned by other teams (e.g., p92_relevance_growth, instagram_feed_ranking) — team spends time triaging alerts that aren't theirs | Before investigating any alert, verify model ownership first: `meta ai.model-type metadata --name=<model>` and check the `oncall` field. If it doesn't match your team, it's a routing bug — file a task to fix alert routing rather than debugging the model itself. (Learned 2026-05-21: OT autolearn) |
| OT job created via starcart but never auto-started — job sits paused indefinitely, silently degrading model freshness with no visible failure signal | Check the starcart task (T#) for auto-start status. Two root causes: (1) starcart TMS config broken — verify registration and re-register if needed; (2) one-time failure — manually restart. Don't wait for the job to self-heal; starcart auto-start failures require manual intervention. (Learned 2026-05-21: OT autolearn) |
| SEV postmortem left incomplete weeks after mitigation — sevmanager quality score 0.00, 11+ required fields missing, becomes a compliance issue | Complete SEV postmortem in sevmanager at mitigation time, not later. For SEV3+, required fields include: overview, incident_impact, root_cause, time_detected, time_started, detection_narrative, root_cause_areas, escalation, consumer_impact, remediation, prevention. If SEV owner is unresponsive, oncall must follow up directly — stale postmortems are an org debt, not just the SEV owner's problem. (Learned 2026-05-21: OT autolearn) |
| m2m family model training stalls — FULL_SNAPSHOT publish stuck for 4+ hours, blocking data pipeline ingestion (seen on m2130324780, m2130324829) | In-trainer full snapshot generation is a known recurring blocker for ig/threads m2m models. Verify MAST job state first (`meta ai.mast-job metadata --name=JOB`). If stuck >4h, restart the job consistent with S659474 mitigation. Coordinate with Rong Huang — root cause pattern is identical across models. (Learned 2026-05-21: OT autolearn) |
| Starcart mechanism did not auto-restart an OT job after failure — required manual oncall intervention | Check starcart config for the affected model: verify starcart is enabled and the trigger condition matches the failure type. Distinguish config misconfiguration from systemic starcart reliability gaps (track via project task 2.12). Manual restart is the mitigation while root cause is investigated. (Learned 2026-05-21: OT autolearn) |
| T4 serving-layer CHC fires (Hedwig/Sigrid in-order consumption rate) paging OT oncall for sparse delta models — alert recurs daily with no OT-side fix available | This is a serving infra issue, not an OT training issue. When Hedwig/Sigrid in-order consumption rate CHC fires for a model's sparse delta delivery channel, the fix lives in serving infra — OT team has no lever. Route the alert to serving oncall or suppress from OT queue. Do not spend triage time diagnosing training-side causes. (Learned 2026-05-21: OT autolearn) |
| Sparse delta e2e latency HIGH alert fires but job is RUNNING and appears healthy | Check T3 publish rate first: `ot-delta-status.sh <model_id>`. If T3 publisher is healthy, the bottleneck is T4 consumer lag — escalate to T4/Hedwig team, do not tune the training job. (Learned 2026-05-22: OT autolearn) |
| Same alert fires 8+ times in 7 days (daily pattern) | This is a structural issue, not a transient flap. Stop mitigating each instance individually — escalate to the owning team (check alert metadata for T-level ownership) for root cause investigation. File a task to track. (Learned 2026-05-22: OT autolearn) |
| FULL_SNAPSHOT publish job stuck 4+ hours — downstream delta publishing also queued behind it | Check if publish timeout is configured (ref: D96358986). If stuck indefinitely, restart the publisher job for that model. Blocking FULL_SNAPSHOT blocks all downstream delta publishes until resolved. (Learned 2026-05-22: OT autolearn) |
| Streaming enablement detector fires across ALL hardware partitions simultaneously for a model | All-partitions-fail-at-once = training job (publisher) stopped, NOT a per-partition receiver failure. First check: is the training job running and actively publishing? Don't waste time debugging individual partition receivers. (Learned 2026-05-22: OT autolearn) |
| Sparse delta CHC message loss alert fires repeatedly (in-order consumption rate <90%) with no permanent fix | Recurring firings (3+ in one week) with one-off suppression diffs signals unresolved root cause in the training job's delta publishing rate, not a transient Scribe blip. Escalate to a permanent monitoring config fix rather than per-incident suppression diffs. (Learned 2026-05-22: OT autolearn) |
| MAST shows job as RUNNING indefinitely but no training output, GPU burning with zero QPS — main process (light.py) died on CUDA assert but TW sidecars (VipInjector, cert renewal, MetricsServer) keep heartbeats alive | Don't trust RUNNING state alone. Check `mvai_metrics` for sample ingestion: if no samples reported for >30 min on a RUNNING OT job, treat as zombie. Escalate to managed_training_service for main-process exit detection. Interim guard: alert on mvai_metrics staleness >30 min. (Learned 2026-05-22: OT autolearn) |
| FULL_SNAPSHOT publish SLO breach but no new snapshot ever registers — publish-process spawn failed silently (Mode B), no error surfaced, no alert fired | Absence of a new snapshot entry in model_store is the signal for Mode B. For Mode A (zombie snapshots): query for snapshot entries stuck at state=CREATING for >180 min — each correlates with a >180-min SLO gap. Escalate Mode B alerting gap to model_store team; don't assume training is the root cause. (Learned 2026-05-22: OT autolearn) |
| Oncall paged twice for identical SLO breach on same model — two critical alerts, one root cause, wrong team paged | Check for duplicate OneDetection observer registrations: two model_metadata entries sharing the same model_id cause the alerting framework to emit two detectors. Verify with model_store team. Publishing-stability root cause is model_store's domain — re-route paging to the correct owner before investigating. (Learned 2026-05-22: OT autolearn) |
| Sparse delta CHC in-order delivery rate drops below threshold (e.g. 41–65% vs 90% threshold), same alert fires repeatedly with no fix visible | Check streaming channel load and publisher pacing for the affected model. Repeated firings of the same CHC alert without acknowledgment means the underlying cause (channel overload or pacing bug) is unaddressed — escalate to model owner and file a fix task rather than just acknowledging each alert. (Learned 2026-05-22: OT autolearn) |
| OT job publishing multiple sparse deltas per minute despite a longer configured publish_interval | Compare actual publish cadence in GMPP Scuba vs the model's configured `publish_interval`. A mismatch means the interval config is not being respected — identify the config owner and file a fix task. Don't assume it's expected behavior from the delta streaming implementation without verifying. (Learned 2026-05-22: OT autolearn) |
| OT job stuck for hours during FULL_SNAPSHOT publish phase — trainer appears alive (SJD shows liveness) but makes no progress | SJD does NOT monitor publish-pipeline progress, only job liveness. Check publish logs for deadlock/gloo stall. If stuck publish confirmed, restart the job. Track D96358986 (publish timeout guard diff) — landing it adds an automatic timeout to prevent indefinite blocking. (Learned 2026-05-23: OT autolearn) |
| Sparse delta flood: model publishing ~20× more SPARSE_DELTAs than expected (e.g., every 15s instead of every 6 min) | Raw Embedding Streaming (RES) runs a separate OS process (`TrainingPsSchedulerProcess`) at `res_streaming_interval_s=15` independent of `WeightsDeltaPublisher`. Check if RES is active on the job. Confirm with model owner whether the cadence and snapshot type are intentional. If not, adjust `res_streaming_interval_s` or switch snapshot type. Require AI infra teams to notify OT oncall before launching new publish-path features. (Learned 2026-05-23: OT autolearn) |
| Sparse delta latency alert fires with "[Invalid Detector - No Data]" — no actionable signal | Alert config references a stale or wrong metric timeseries path. First validate whether the model is actively publishing sparse deltas (check UMM/GMPP). If publishing is healthy → fix the detector config path. If not publishing → escalate to model owner to investigate why deltas stopped. (Learned 2026-05-23: OT autolearn) |
| OT job appears alive (SJD liveness OK) but model is stale / not publishing | SJD monitors train-loop liveness, NOT publish-pipeline health — a stuck publish is invisible to liveness checks. Check publish-pipeline logs and state transitions separately; see if job is blocked in checkpoint/full-snapshot cycle. Diff D96358986 adds publish timeout as mitigation. (Learned 2026-05-23: OT autolearn) |
| OT training loop very slow or blocked; full-snapshot publish consuming >50% of iteration time | Sparse delta not enabled — all publishes fall back to FULL_SNAPSHOT which blocks the training loop inline. Verify sparse delta is enabled for the model; if not, enable it. Apply publish timeout via D96358986 as immediate relief. Reference: S652695 documents full-snapshot consuming ~60% of training time. (Learned 2026-05-23: OT autolearn) |
| Model generating far more sparse deltas per publish interval than configured (e.g., multiple per minute vs 6-min interval) | Before diagnosing as a bug, check whether AI infra recently enabled a new platform feature (e.g., Raw Embedding Streaming / RES) on the model. RES runs in a separate OS process outside the normal delta publisher and produces additional sparse outputs per training step. Coordinate with the feature POCs to confirm whether the cadence is intentional. (Learned 2026-05-23: OT autolearn) |
| Streaming enabled in control plane but runtime received 0 messages (e.g., 720+ consecutive data points with 0 messages) | First check JK allowlist: verify `ai_infra/delta_publishing` JK allowlist includes the model. If missing, engage streaming_infra_oncall to add it before investigating anything else. (Learned 2026-05-23: OT autolearn) |
| `dpp_version=""` (empty string) in MAST app_metadata on a streaming model | Empty dpp_version blocks scribe-category and pipeline-QPS charts, making streaming debug nearly impossible. Set dpp_version at job launch config; validate it is non-empty as part of streaming onboarding checklist. (Learned 2026-05-23: OT autolearn) |
| ESR/RES job generating delta publishes at unexpected high rate (e.g., 3 sparse deltas/min vs 6-min publish interval) | Raw Embedding Streaming (RES) runs as an independent OS process separate from delta publish config — it produces its own publish cadence regardless of the configured interval. When RES is enabled, expect publish rates higher than the delta publish interval; loop in RES POC (AI Infra) to confirm expected behavior before assuming a bug. (Learned 2026-05-23: OT autolearn) |
| Sparse delta publish frequency unexpectedly high (e.g., multiple per minute vs. expected 1 per 6 min) | Check if Raw Embedding Streaming (RES) is enabled on the model before assuming a trainer bug. RES runs in a separate OS process and creates its own sparse delta entries independently of the standard publish cycle — current monitoring cannot distinguish RES-sourced from trainer-sourced deltas. Confirm expected behavior with RES POCs before escalating. (Learned 2026-05-23: OT autolearn) |
| CHC sparse delta in-order delivery rate alert fires repeatedly (6+ times in 48h) with no root cause identified | First verify trainer publish cadence for the affected model is within expected bounds (`meta ai.mast-job system-metrics --name=JOB`). If trainer side is healthy, escalate immediately to streaming_infra_oncall with alert links. If no response within 24h, file a task against the streaming team. Do not leave repeated MAJOR alerts unactioned. (Learned 2026-05-23: OT autolearn) |
| Same model family hits a second SEV within 5 weeks after prior mitigation | Assume the prior mitigation was incomplete before investigating a new independent root cause. Check if the same EDPP scaling interaction or publish deadlock pattern recurred. Also verify the prior SEV has non-empty root_cause, remediation, and prevention fields — empty fields mean the fix was undocumented and likely not durable. (Learned 2026-05-23: OT autolearn) |
| OT job stuck for hours (not PENDING, FAILED, or DEAD) with no auto-restart triggered | Python agent hung on failure path without propagating exit code to MAST. Check MAST state transitions for publish-phase hangs (`meta ai.mast-job scheduling --name=JOB`). Apply exit code propagation fix from D96502828. Pattern recurs weekly (S628346, S659474, S665454, S667453) — if stuck > 30 min with no transition, this is the first hypothesis. (Learned 2026-05-24: OT autolearn) |
| FULL_SNAPSHOT publish blocks training for 4+ hours; job appears alive but QPS drops to zero | FULL_SNAPSHOT runs in-trainer and serializes training. Apply publish timeout patch (D96358986) and evaluate DeltaOnlyPublisher migration for the model. Recurring deadlock pattern seen on Threads M2M models (S635390, S631731, T272335322). Distinguish from normal publish lag by checking trainer iteration time — zero iterations = blocked in publish. (Learned 2026-05-24: OT autolearn) |
| CRITICAL alert firing but tagged [Invalid Detector - No Data] — unclear if model is actually healthy | Alert is misconfigured; underlying model health is unknown and monitoring is silently broken. Do not assume the model is fine. Independently check delta publish health via GMPP Scuba or `ot-delta-status.sh` for the model ID. Then reconfigure or replace the broken detector. High-priority models with invalid detectors are effectively unmonitored. (Learned 2026-05-24: OT autolearn) |
| OT job stuck non-RUNNING or failed to start during a fleet-wide infra event (e.g., SMC twdns throttle) — no obvious model-specific error | Check active SEVs first; look for STATE_TIMEOUT / SMC_SYNC_TIMEOUT in MAST job logs (`meta ai.mast-job error --name=JOB`); confirm job restarts cleanly after fleet mitigation before digging into model config (Learned 2026-05-24: OT autolearn) |
| SEV filed for age spike / metric anomaly on a model but prod serving looks healthy on manual check | Before escalating, verify Scuba dashboard tenant filter syntax — missing `:` delimiter can mix prod tenant (e.g., `m2130324780`) with test tenant (e.g., `mi350_m2130324780`), causing false spike; run `meta ai.model-type metadata --name=<model>` to confirm actual serving status (Learned 2026-05-24: OT autolearn) |
| ESR OT job generating far more sparse deltas per minute than the configured publish interval (e.g., multiple per minute vs. 6-min interval) | Check if Raw Embedding Streaming (RES) is enabled — RES runs a dedicated OS process that publishes streaming data independently of the standard delta publisher; the two paths are not reconciled; loop in RES POCs or file against AI Infra RES team; audit prod ESR models for same behavior (Learned 2026-05-24: OT autolearn) |
| Multiple OT jobs fail to start simultaneously but no OT-specific alert fires — failure looks like individual model issues | Check for MAST infrastructure blast-radius incidents first (e.g., SMC throttling). Run `meta ai.mast-job scheduling --name=JOB` on affected jobs and look for correlated failure timestamps; cross-reference with MAST oncall SEVs. Detection requires external noise awareness, not OT alerting. (Learned 2026-05-24: OT autolearn) |
| Scuba dashboard shows prod model as stale/unhealthy even though the MAST job is RUNNING and publishing normally | Verify Scuba query tenant filter syntax before escalating. A missing `:` colon in the tenant filter can conflate prod tenant with other tiers (e.g., `mi350`-prefixed hardware tiers), making healthy prod appear stale. Fix the query filter and recheck before investigating the trainer. (Learned 2026-05-24: OT autolearn) |
| Sparse delta CHC in-order consumption rate drops (e.g., ~50%) and the same alert fires repeatedly for the same model within days | Check trainer-side GMPP publish health first (use the Scuba GMPP dashboard for the model). If the trainer is publishing normally, the issue is in the serving-side sparse delta path — escalate to streaming_infra_oncall. Recurring identical alerts for the same model indicate a structural issue, not transient noise. (Learned 2026-05-24: OT autolearn) |
| Scuba dashboard showed model age spike / stale snapshots on prod, but OT MAST job was actually RUNNING healthy — false alarm leading to unnecessary publish pipeline investigation | Verify Scuba dashboard tenant ID filter format first (colon delimiter required in tenant ID filter); test-tier snapshots (e.g., mi350 test tier) can pollute prod view when filter is misconfigured. Run `ot-delta-status.sh <model_id>` to confirm actual delta publishing state before digging into publish pipeline. (Learned 2026-05-24: OT autolearn — S665468/T272335322, fix diffs D105401690 D106021944) |
| Threads Retrieval U2M or M2M OT job gets intermittently stuck with QE degradation; no clear MAST error; mirrors earlier Threads incidents | Check delta cadence via `ot-delta-status.sh` and MAST job errors for scribe timeout during full snapshot window. The 30-min full snapshot interval is a known deadlock risk for Threads Retrieval models (see S635390 pattern: publish stall during snapshot). Cross-reference delta publish interval and scribe timeout logs before escalating. (Learned 2026-05-24: OT autolearn — S665454/S635390) |
| OT job failed to start with no root cause found; all OT-specific investigation is clean; timing overlaps a broad infra window | Check for concurrent MAST SMC throttling events before assuming OT misconfiguration — infra throttling can cause 17%+ MAST startup failure rates with no OT-specific alert firing. Cross-check `meta ai.mast-job scheduling --name=JOB` start time against known infra SEVs; if overlap confirmed, trigger manual OT job restart after infra mitigation. (Learned 2026-05-24: OT autolearn — S667390/S667453) |
| OT job fails to start with no clear MAST error after a nearby SMC throttling window | Check MAST startup logs for "SMC Syncer timed out"; correlate with any active SMC throttling SEVs. Restart the OT job after the throttling window clears before investigating further. (Learned 2026-05-25: OT autolearn) |
| Tier-1 sparse delta in-order consumption rate repeatedly drops (e.g., 50–67%, threshold 90%) but self-recovers each time | Do not treat each drop as an isolated incident. Correlate drop timestamps with FULL_SNAPSHOT publish windows — drops during publish are a known interaction pattern. Assign an alert owner and file a tracking task to catch a trend before it becomes a P0. (Learned 2026-05-25: OT autolearn) |
| Conveyor blocked by Gloo TCP disconnect or DistStoreError timeout during publish after a recent release revision bump | Treat as a regression, not a Gloo config issue. Bisect between the last-known-good revision and the first-bad revision using conveyor re-runs. Do not tune Gloo parameters until the culprit commit is identified. (Learned 2026-05-25: OT autolearn) |
| `online_train_publish` fails with gloo TCPStore DistStoreError: rank 0 waits 30 min for N other ranks that never join (1800s timeout), blocking conveyor trunk | Ranks not joining usually means a worker crashed silently before rendezvous. Check MAST task logs for the non-joining ranks first. Apply a graft workaround to unblock trunk, then bisect between last-good and first-bad revision to find the regressing diff. (Learned 2026-05-25: OT autolearn) |
| Scuba model age spike alert fires for a healthy prod model — appears stale but prod traffic is fine | False alarm from Scuba query matching both the prod model ID and a derivative (e.g. `mi350_m<id>`) due to a missing `:` delimiter in the query filter. Verify by narrowing the Scuba filter to the exact model ID with the `:` delimiter. Land a corrected query before closing the SEV. (Learned 2026-05-25: OT autolearn) |
| OT publish step fails deterministically (DistStoreError or Gloo TCP "connection closed by peer") across models after a Conveyor push | Check if failure started at a specific Conveyor revision — run `meta ai.mast-job error --name=JOB` and look for the first failing revision. DistStoreError = trainer ranks failed to join distributed store; Gloo TCP = process group breakdown during TGIF publish. Both indicate Conveyor regression, not model-specific issue. Coordinate across affected models before reverting. (Learned 2026-05-25: OT autolearn) |
| Model freshness dashboard (IPNext snapshot) shows spike but model seems healthy | Verify Scuba query is not grouping prod + non-prod tenants (e.g., mi350) together — mixed-tenant grouping can show a false freshness spike when prod is actually fine. Narrow the Scuba query to prod-only tenants before escalating. (Learned 2026-05-25: OT autolearn) |
| MAST job appears alive (SJD shows healthy) but OT model is stale and not recovering | SJD monitors job liveness, NOT publish pipeline health. A job can be alive but stuck in the publish phase indefinitely with no auto-recovery. When model age is rising but job is not FAILED/DEAD, check publish step logs directly for deadlock or stuck state. Add manual watchdog or file SEV. (Learned 2026-05-25: OT autolearn) |
| OT job training example age spikes every ~30 min; trainer stalls during full-snapshot publish intervals (EDPP pause pattern) | Root cause: full-snapshot publish occupies the trainer and stalls the Scribe reader. Short-term: verify D101389014 patch is holding; consider extending full-snapshot interval (e.g. 30 → 60 min). Long-term fix: DeltaOnlyPublisher decoupling (separate full+delta publish paths). Check delta publish timestamps via `ot-delta-status.sh` and MAST job state. (Learned 2026-05-25: OT autolearn) |
| Multiple OT jobs fail-to-start simultaneously across models/teams in the same time window | Before debugging individual jobs, check if there is an active SMC throttling SEV (e.g. S667390 pattern: SMC writes throttled → MAST job startup blocked). Run `meta ai.mast-job scheduling --name=JOB` and look for SMC allocation errors. If thousands of jobs are affected, escalate to infra oncall rather than per-job triage. (Learned 2026-05-25: OT autolearn) |
| MAST shows RUNNING but training has silently stopped (e.g., SIGSEGV on one rank causes D-state workers stuck in Manifold checkpoint write; downstream snapshots blocked for hours with no alert) | Do not trust MAST status alone. Check epoch/step progress in TensorBoard — if no new steps for >30min while MAST says RUNNING, treat as zombie. Manual restart is the only mitigation today. File a follow-up task to add a T2 heartbeat or epoch-progress monitor. (Learned 2026-05-26: OT autolearn) |
| Training example age spikes but self-recovers within minutes; pattern recurs every ~20 days across many unrelated models | Likely DPP session rotation at Zeus 20-day TTL — old session expires with no graceful handoff before new session is ready. Confirm via DPP starvation metrics in TensorBoard. Do not page urgently; it self-resolves. Coordinate with DPP team on pre-warming new session before expiry. Consider adding an "expected rotation" annotation to suppress alert noise. (Learned 2026-05-26: OT autolearn) |
| Two or more unrelated models fail at the same pipeline step (e.g., online_train_publish) in the same week with different error messages (e.g., Gloo TCP + DistStoreError) | Treat as probable platform-layer regression, not independent model bugs. Start bisection on the shared platform/release range immediately. If bisection will take >24h, graft a known-good commit as a temporary bypass to unblock conveyor while investigation continues. (Learned 2026-05-26: OT autolearn) |
| OT T3 publish job hangs for 4–8+ hours with no progress (FULL_SNAPSHOT blocked/deadlocked) | Check if model is stuck in FULL_SNAPSHOT publish path: inspect MAST job state transitions (`meta ai.mast-job scheduling --name=JOB`) and publishing logs. This is a known fragile path when checkpointing is involved — escalate to model owner and reference fixes D105401690 / D106021944 as patch pattern. Do not wait >2h before escalating. (Learned 2026-05-26: OT autolearn) |
| Training example age spikes suddenly then self-recovers; job auto-restarts; no data pipeline change | Check if the job is hitting the DPP Zeus session 20-day TTL: look at session rotation timestamp relative to spike time. This is a fleet-wide recurring pattern (140 hits / 90 days / 50+ models) — do not open a P0 SEV if it self-recovers within 30 min. File a DPP pre-warm request if this model has not had one yet. Tune alert threshold to suppress expected rotation spikes. (Learned 2026-05-26: OT autolearn) |
| MAST shows RUNNING but QPS=0, SJD doesn't fire, job never auto-recovers | Zombie job (Linux D-state): main training process died but sidecars keep the container alive. MAST cannot detect the process is dead. Manually kill the hung job — TMS/MAST will auto-restart it. Do NOT wait for SJD or auto-recovery. Root fix is pytorch_distributed_infra light_cli rebuild (D106022977+). (Learned 2026-05-26: OT autolearn) |
| Publish fails deterministically on every attempt while training runs fine; Gloo TCP closed or DistStore timeout starting at a specific code revision | Publish path regression from a recent code push. Bisect by R-number: note the exact revision when failures began, identify diffs landed since the last good revision, bisect to the regressing diff. Block or revert that revision. Cross-check other models publishing around the same time — a single bad push can fail multiple models in the same failure class. (Learned 2026-05-26: OT autolearn) |
| Example age spikes and UBN/SEV alerts fire fleet-wide, then self-resolve within ~5 minutes, recurring on ~20-day cadence | DPP data session auto-rotation cycle. Training pauses ~5 min during restart — not a real training failure. Acknowledge the alert, wait for self-recovery, do not page model owners. File noise ticket with dpp_distributed_data_reading if generating excessive oncall pages (reference issue P2349020385). (Learned 2026-05-26: OT autolearn) |
| Job shows RUNNING in elastic agent / MAST UI but QPS is 0 and job hangs 6-28h | NCCL deadlock in publisher subprocess causes Linux D-state; elastic agent and SJD check OS-level process liveness only — not QPS. Check tensorboard QPS; look for D-state publisher subprocesses. Kill and restart job on healthy nodes. Ping pytorch_distributed_infra for SJD rank-error+alive-process rule. (Learned 2026-05-26: OT autolearn) |
| Checkpoint saves fail silently, job stops without auto-restarting; Manifold returns RESOURCE_BUSY_TIMEOUT | Retry JK `minimal_viable_ai/enable_retryable_error_checkpointing:enable_by_entitlement` may be disabled for this model's entitlement. First check: is retry JK enabled? If not, enable it for the entitlement. Also verify upstream Manifold SEV (e.g. S664460-class) is resolved with model_store oncall. (Learned 2026-05-26: OT autolearn) |
| Example age spike + UBN/SEV alert fires then self-resolves every ~20 days across OT models | DPP restarts data sessions on ~20-day cycle; training pauses briefly, example age spikes, alerts fire and self-resolve (~280 non-actionable alerts per half fleet-wide). Before investigating: check if alert timestamp aligns with a ~20-day rotation boundary. If yes, non-actionable — follow up with dpp_distributed_data_reading on graceful session rotation (P2349022281). (Learned 2026-05-26: OT autolearn) |
| MAST shows RUNNING but trainer is frozen — Python hung, D-state subprocess blocking elastic agent shutdown, sidecar keepalive fools SJD, only manual kill resolves (zombie trainer) | Check for D-state subprocesses on hosts: `mast exec <job> -- ps aux | grep D`. If found, manually kill and restart the job. Do NOT wait for SJD to auto-detect — it cannot. File a rank-error + alive-process liveness rule request with pytorch_distributed_infra as follow-up. (Learned 2026-05-27: OT autolearn) |
| ~280 UBN/SEV alerts firing fleet-wide for example age spikes with training pausing 2–5min and self-recovering — non-actionable noise every ~20 days | Check if the last DPP session restart was ~20 days ago: DPP auto-rotates each OT job session on a ~20-day cycle, which causes a training pause and example age spike that mimics a real incident. Verify by checking DPP session start timestamps. Do not file a ticket until you confirm the dip is persistent (>10 min) or the job does not self-recover. (Learned 2026-05-27: OT autolearn) |
| Cogwheel publish step fails deterministically: rank 0 hangs 30min on gloo TCPStore, other ranks never join (DistStoreError) — started after a package bump | Bisect the fbpkg version to identify the culprit (e.g. `ien.lower.mvai.mi300x`): pin back to last-known-good version as immediate mitigation, then check `meta ai.mast-job error --name=JOB` for DistStoreError + TCPStore in logs. Confirm bisect range using R-number of first failure vs last success. (Learned 2026-05-27: OT autolearn) |
| Job shows RUNNING in MAST for 6–28h with sidecars alive but no training progress (zombie); SJD reports healthy | Trainer has entered d-state (GIL hang, C++ deadlock, storage stall, elastic agent d-state, or SIGABRT+sidecar). SJD is blind to this class of failure. Check MAST logs for stuck subprocess or d-state signal. Only current mitigation is manual kill. Do not wait for SJD auto-recovery — it will not fire. (Learned 2026-05-27: OT autolearn) |
| Job shows RUNNING in MAST, no obvious errors, but checkpoints are not being persisted (silent checkpoint save failure) | Check for Manifold RESOURCE_BUSY_TIMEOUT in trainer logs. Also verify whether the JK retry gate is enabled — if disabled, failed checkpoint saves are silently dropped. Re-enable JK retry gate as temporary mitigation while engaging storage oncall. (Learned 2026-05-27: OT autolearn) |
| Example age spike or UBN alert fires for an OT model, but job recovers on its own within a few minutes | Check MAST logs for: "Session N seconds >1728000. Triggering new attempt." — this is the DPP 20-day session auto-restart. Training pauses briefly, example age spikes, then self-recovers. Non-actionable. Do not escalate unless the job fails to recover after ~5 minutes. (Learned 2026-05-27: OT autolearn) |
| Job shows RUNNING in MAST but makes no training progress — main process dead, only sidecars alive. SJD reports healthy because it checks container liveness, not training progress. | This is the zombie pattern. Don't trust MAST RUNNING status alone. Verify actual training progress via tensorboard QPS or scuba iteration metrics. If main process is dead, SJD cannot self-recover — manually kill and restart. Root cause: D-state subprocess (e.g. publish subprocess) blocks main thread from clean shutdown. Fix stack: lupaul D106022977-D106022978 (process_wrapper shutdown hardening). (Learned 2026-05-27: OT autolearn) |
| Gloo TCPStore timeout during OT publish: rank 0 waits 30+ min for other ranks that never join (DistStoreError). Failure onset aligns with a base-layer fbpkg upgrade. Initial instinct is to revert a recent model diff. | Bisect the fbpkg version, not the model diff. Pin the base-layer package (e.g. ien.lower.mvai.mi300x) to the last known-good version and verify publish succeeds. File a regression report with fbpkg owners. Model code reverts are a false lead when the upgrade timeline matches the failure onset. (Learned 2026-05-27: OT autolearn) |
| Checkpoint saves silently fail with Manifold RESOURCE_BUSY_TIMEOUT; job keeps running indefinitely producing no usable checkpoints. DCP all_reduce save is killed by Manifold throttling and the JK retry gate is disabled for the entitlement. | Check the JK retry gate status for the job's entitlement. A job that runs without saving checkpoints is worse than a failed job — treat as P1 for tier-1 models. Options: re-enable JK retry gate, increase Manifold quota for the entitlement, or configure the job to restart on checkpoint-save failure. (Learned 2026-05-27: OT autolearn) |
| OT job appears RUNNING and healthy to MAST/SJD/TW, SJD kill commands have no effect, but job is completely stuck producing nothing | D-state zombie: publishing subprocess entered uninterruptible sleep (d-state), blocking the main thread. Normal liveness checks can't detect this. Check subprocess state manually; a standard SJD kill won't work. Escalate to pytorch_distributed + MAST/TW oncall with evidence of d-state. Interim: establish manual kill runbook for d-state subprocesses. (Learned 2026-05-28: OT autolearn, S665454, S665478) |
| IFR Watchtower fires "too few delta snapshots published" alert | Before triaging as real incident: (1) check MAST job is RUNNING, (2) query GMPP Scuba for actual delta counts (ot-delta-status.sh or https://fburl.com/scuba/gmpp/1qdb275c), (3) cross-check D98023866 — if the pending threshold raise would have suppressed this alert, treat as likely false positive due to over-sensitive threshold. (Learned 2026-05-28: OT autolearn, model 2143912626, Alert 932083459684694) |
| FULL_SNAPSHOT task stuck for 4+ hours while delta publishing appears ongoing | Different failure mode from general stale-model / SV-rejection path. Check if the full-snapshot MAST subjob is in a hung state vs. queued. Verify the task is actually scheduled and not silently dropped. Owner carryover risk: if no update in 6 days, task may be stale — confirm with assigned owner before assuming resolved. (Learned 2026-05-28: OT autolearn, T272335322, model 2130324780) |
| Job shows MAST RUNNING for 6–28h with no checkpoint progress ("zombie job") — SJD doesn't fire because it only checks process liveness, not training progress | Publishing subprocesses entered D-state (disk wait), blocking elastic agent shutdown. Check `meta ai.mast-job system-metrics --name=JOB` for stalled progress; look for D-state subprocesses. Manual kill + restart is the mitigation. Do NOT wait for SJD to fire — it will not detect this class of hang. (Learned 2026-05-28: OT autolearn) |
| Downstream model has no FULL_SNAPSHOT for 7h+ but its own job appears healthy | Root cause is likely upstream model dependency: the downstream model's FS publish waits on the upstream reranker's checkpoint. Check if the upstream reranker job is hung/zombie first before debugging the downstream model. Set up a >30min checkpoint-stall alert on the upstream model to catch this early. (Learned 2026-05-28: OT autolearn) |
| Recurring MAJOR alert: sparse delta in-order consumption rate drops to 46–64% (threshold 90%), fires multiple times per week on same model | RES (Raw Embedding Streaming) may be publishing SPARSE_DELTA at too short an interval (e.g., 15s with `enable_raw_embedding_streaming=True`), overloading hedwig subscriber buffers. Check RES publish cadence in GMPP Scuba and consult RES POC. Fix: increase RES interval or adjust hedwig subscriber buffer sizing — do not just dismiss the alert as noise. (Learned 2026-05-28: OT autolearn) |
| OT job appears healthy (TorchWorker reports OK) but SJD cannot terminate it; job hangs indefinitely without entering FAILED state | Publishing subprocess entered Linux D-state (uninterruptible sleep). Check for D-state procs on the job host. TorchWorker health masks the blocked subprocess — do not trust it alone. Pattern seen in S665478, S665454, S667567, S668689. Fix requires light_cli base-layer rebuild (lupaul D106022977–D106022978). (Learned 2026-05-28: OT autolearn) |
| Model publishing fails repeatedly (e.g., 11/11 consecutive failures) with Gloo TCP closed or DistStore connection dropped mid-write | Gloo/DistStore publish hang triggered by a regression in a recent runtime/package bump. First: correlate failure start time with recent package changes (R7176 was the culprit in S667668, S667687). Bisect to identify the offending commit. Assess rollback while fix is in flight. Fleet-wide exposure is possible if the package is shared. (Learned 2026-05-28: OT autolearn) |
| Repeated MAJOR alerts for sparse delta in-order consumption drop on a model; model is RUNNING and publishing normally | High-frequency RES (Raw Embedding Streaming) SPARSE_DELTA publishing (e.g., every 15s) overwhelming hedwig subscriber buffers. Check RES publish interval in job config. Even if cadence is intentional, hedwig impact may not have been evaluated. Engage both the model owner (confirm intent) and hedwig oncall (assess buffer capacity). Add a publish rate guardrail. (Learned 2026-05-28: OT autolearn) |
| MAST/TW shows job RUNNING but training and publishing are both stopped (zombie OT job); SJD cannot detect or auto-recover | Check GMPP Scuba for delta publish health on the model — if no deltas published while MAST shows RUNNING, it's the D-state zombie pattern (publishing subprocess in uninterruptible kernel wait). File SEV immediately; do not wait for SJD auto-recovery. Track fix stack: base-layer rebuild is often the blocker. (Learned 2026-05-28: OT autolearn) |
| Tier-1 model running 10+ days with no checkpoint-save success, no alerts fired — discovered only when freshness SLO violated | Silent checkpoint save failure (e.g., Manifold RESOURCE_BUSY_TIMEOUT with JK retry gate disabled) produces no automatic alerting without explicit checkpoint-save success monitoring. For tier-1 models, verify checkpoint-save monitoring is enabled; absence of failure alerts does NOT mean saves are succeeding. (Learned 2026-05-28: OT autolearn) |
| "Too-few-deltas" or sparse delta alert fires; MAST job appears healthy; Conveyor/SV logs show no obvious error | Before deep pipeline investigation, check IFR Scribe quota — quota exhaustion silently blocks OT job submissions and produces the same alert symptom as zombie jobs or publish failures. Confirm with owning team (e.g., IFR) that quota is restored before closing. Recurrence pattern: S625235 → S669133. (Learned 2026-05-28: OT autolearn) |
| MAST shows job RUNNING but training is stopped — publish subprocess in D-state (uninterruptible kernel sleep), SJD health check blind to publish-phase stuckness (S665454, S665478) | Check if publish subprocess is hung (D-state), not just training heartbeat. Manual kill + restart is the immediate fix. Loop in pytorch_distributed oncall to trace I/O trigger (Manifold write? Scribe flush?). Long-term: add publish-progress heartbeat monitoring. (Learned 2026-05-29: OT autolearn) |
| OT publish silently broken after infrastructure package upgrade (ien.lower.mvai.mi300x v93→v99+) — failed for 46 days before becoming hard blocker, no automated regression detection (S667668) | When OT publish breaks without a training code change, check recent GPU toolchain / IEN lower package upgrades. Pin to last-known-good version, verify with canary run. No automated regression detection exists for OT publish in GPU toolchain pipeline — manually bisect package versions. (Learned 2026-05-29: OT autolearn) |
| All new OT job submissions blocked for a model family with no job-level error — Scribe write quota hit limit (S669133 IFR models) | Check Scribe write quota via Scuba: https://fburl.com/scuba/mast_supporting_service_resource_usage_info/amt07dmn. If quota exhausted, reduce OT Scribe write rate or request quota increase. Symptom looks like submission failure, not a job crash — do not chase MAST job error logs. (Learned 2026-05-29: OT autolearn) |
| Example age spike on OT model with 20-min-read/15-min-pause pattern — EDPP autoscaling doesn't help | Scribe reader is stalling during a 30-min full snapshot publish. EDPP cannot fix a publish-blocking stall. Investigate DeltaOnlyPublisher decoupling so snapshot publish does not block delta streaming. (Learned 2026-05-29: OT autolearn) |
| Zero shadow samples suddenly across multiple related models after an infra/serving change | Check if pselect position labels are still populated upstream (e.g. CLIPS_PSELECT_POSITION). An LSR VM or serving-side change can silently stop label generation, causing filter_empty_shadow_label to drop all shadow rows. Mitigation: ban affected checkpoints and restart OT. Root cause: trace the upstream label pipeline change via recent SEVs touching the affected label name. (Learned 2026-05-29: OT autolearn) |
| Job shows MAST RUNNING indefinitely but no training progress — sidecars alive, SJD has no effect | Trainer subprocess is in D-state (uninterruptible sleep) during checkpoint/publish. MAST liveness is sidecar-based, not training-step-based. SJD cannot kill D-state processes. Only fix: manual kill + restart. Check for SIGSEGV, GIL hang, gloo timeout, C++ deadlock, or storage stall in rank logs. (Learned 2026-05-29: OT autolearn) |
| OT publish hangs with DistStoreError: 47/48 ranks fail gloo TCPStore join, 1800s timeout, Conveyor blocked for days | Package regression in IEN lower (e.g., v93→v99+ bump). Pin IEN lower back to last-known-good version (v93). Coordinate with gpu_lowering oncall before re-upgrading. Check if multiple models share the symptom — correlate with package push timestamp. (Learned 2026-05-29: OT autolearn) |
| Checkpoint save fails silently — Manifold returns RESOURCE_BUSY_TIMEOUT, job appears healthy in MAST, no alert fires | JK-gated retry mechanism is disabled, causing all checkpoint write failures to be silently dropped. Job stays RUNNING with no observable error. Check Manifold write success rate in ODS/Scuba rather than relying on job health. Enable JK retry gate or add checkpoint-failure alert. (Learned 2026-05-29: OT autolearn) |
| All ranks hang simultaneously in WAITING_FOR_GPU during backward pass with no NCCL error — job restarts deterministically reproduce the hang (P44/P45 deadlock) | Pull trainer logs via `mast get-logs <job> --use-logarithm`, find py-spy snapshot of frozen Python frame, identify hung CUDA op. If CUDA kernel confirmed, escalate to pytorch_distributed_infra. This is a silent C++ or CUDA-level stall inside the compute graph — distinct from NCCL-timeout class which surfaces a communication error. SJD cannot detect alive-but-frozen trainers; manual oncall kill required until escalation resolves. (Learned 2026-05-29: OT autolearn) |
| Training example age spikes 10–30x above baseline with a fleet-wide ~280-instance dip, then recovers — suspect data starvation | Run `meta ai.mast-job error --name=JOB --no-truncate` and grep for `"Session running for N seconds"` — this is the DPP 20-day session restart signature. If found, wait ~15 min for self-recovery before escalating. Only escalate if age stays elevated past 20 min or Scribe reader lag appears in DPP logs. (Learned 2026-05-29: OT autolearn) |
| OT streaming job was killed or blocked by TMS bot (smart__ai_infra__training_management_system) with no policy documented in the alert | Pull TMS kill event log for the affected job and identify which policy triggered (quota, health check, or policy mismatch). This is distinct from "DEAD → ML Guardian or TTL" — TMS is a separate service. If policy is misconfigured for OT streaming jobs, file bug with managed_training_service and ensure OT streaming jobs have appropriate TMS exemptions. (Learned 2026-05-29: OT autolearn) |
| Job shows MAST=RUNNING for hours with no progress — main process died (SIGSEGV/SIGABRT/D-state subprocess) but container stays alive; SJD cannot detect | Zombie job: SJD is blind to this state. When job appears stuck RUNNING >2h with no progress metrics, check via `meta ai.mast-job insights --name=JOB` for rank-error events with only sidecars alive. If zombie confirmed, manually kill via MAST CLI — do NOT wait for automated detection. (Learned 2026-05-30: OT autolearn) |
| Job continues training but FULL_SNAPSHOT not generated for 7+ hours; deltas may publish OK; Manifold shows RESOURCE_BUSY_TIMEOUT in logs; JK retry gate disabled | Silent checkpoint save failure — job trains but nothing persists. Verify snapshot freshness via `meta ai.mast-job insights --name=JOB`. Check for Manifold errors in logs. If snapshots stale >12h, risk of permanent training loss on restart — restart with manifold retry gate enabled as interim. (Learned 2026-05-30: OT autolearn) |
| OT publish fails with Gloo TCPStore timeout / DistStore hang (100% conveyor failure rate) after a recent IEN lower package upgrade | IEN package regression: bisect IEN lower package versions to find the breaking version. Pin to last-known-good version in conveyor config as interim. File IEN regression report before unpinning. (Learned 2026-05-30: OT autolearn) |
| MAST shows job RUNNING for 6–28h but training has stopped; publishing subprocess in D-state (uninterruptible sleep); SJD does not detect because sidecar containers stay alive | Zombie job pattern — do not trust MAST RUNNING status. Manually kill the affected job. Check for D-state processes in the publish subprocess (`ps aux` + `cat /proc/<pid>/status`). Escalate to pytorch_distributed_infra for the light_cli base-layer fix. (Learned 2026-05-30: OT autolearn) |
| Alerts fire simultaneously across 50+ models with example-age spikes, then self-recover within a few minutes every ~20 days | DPP auto-restarts each OT job's data session every ~20 days, causing transient training pause and alert storms. Suppress or defer escalation for ~5 minutes to confirm self-recovery before opening SEVs. Loop in dpp_distributed_data_reading for graceful session rotation. (Learned 2026-05-30: OT autolearn) |
| Checkpoint saves appear to succeed but FULL_SNAPSHOT never publishes; job open 9+ days; no explicit error surfaced | Manifold RESOURCE_BUSY_TIMEOUT causes silent checkpoint failures. First check: is the JK retry gate enabled? If disabled, re-enable it or contact Manifold oncall for capacity. Do not assume checkpoints are valid just because the training job is running. (Learned 2026-05-30: OT autolearn) |
| DistStore timeout blocks Conveyor publish after a GPU fbpkg version bump (e.g., ien.lower.mvai.mi300x v99+) | Pin fbpkg to last known-good version (e.g., v93) as immediate mitigation to unblock Conveyor. Then track forward fix with GPU lowering team. GPU fbpkg regressions can manifest as DistStore timeouts rather than obvious job crashes. (Learned 2026-05-30: OT autolearn) |
| ~280 non-actionable alert dips per half-shift; example age spikes + UBN/SEV alerts fire then self-recover; pattern repeats every ~20 days | DPP 20-day session restart: DPP auto-restarts each OT job's data session periodically, pausing training for a few minutes. Self-recovers — no action needed. Verify by checking DPP session restart timing against alert timestamp. Suppress/acknowledge without escalating; loop in dpp_distributed_data_reading if graceful rotation not yet deployed. (Learned 2026-05-30: OT autolearn) |
| Training QPS crashes specifically during online training delta publish stage (not during forward/backward); model not in sparse delta allowlist | Publish-stage QPS crash: likely model is doing in-trainer full snapshots but not on sparse delta allowlist (see S652695 platform-allowlist-silent-fallback pattern). Check publish logs for `PublishMode` and JK allowlist flags. If confirmed as full-snapshot mode, engage platform team to add model to sparse delta allowlist. (Learned 2026-05-30: OT autolearn) |

_Last updated: 2026-06-10. Maintainer: dennyzhang._
