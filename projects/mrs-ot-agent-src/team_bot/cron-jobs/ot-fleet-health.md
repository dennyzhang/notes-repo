[ot-fleet-health cron] 3x/day ~07:00, 13:00 & 19:00 PT (14:00, 20:00 & 02:00 UTC). OT fleet health check. Runs SIX scans against tracked prod OT models: (1) `tools/scan-zombie-fleet.sh` for the KI-001 zombie pattern (failure reply-file written + job still RUNNING + gap >30min → task never exited, MAST keeps it RUNNING forever); (2) `tools/scan-scribe-age.sh` for scribe/training example-age SLO breaches (model consuming examples >10min old → trainer fed stale data); (3) `tools/scan-perf-regression.sh` for pre-SEV perf drift (baseline-relative training-QPS drop + GPU-mem climb); (4) `tools/scan-pkg-expiry.sh` for consumer-side ephemeral fbpkg expiry (a RUNNING job whose app/base fbpkg is expiring/expired → next restart can't pull the package → job can't come back up; expired-but-still-RUNNING is the landmine); (5) `tools/scan-dpp-starvation.sh` for DPP / data-starvation (a RUNNING job whose `dpp_worker` examples-read rate is ~0 → trainer alive but the data pipeline isn't feeding it; the S674219 EAG-scribe-drain class, route DPP/scribe); (6) `tools/scan-ttfb.sh` for cold-start stalls (a job 60-360m past attempt-start with max QPS still ~0 → never reached first batch; reports `in_window`/`skipped` so a bare `0` can't masquerade as all-healthy). Posts ONE ACTIONABLE combined alert to the team space when ANY scan is unhealthy or any check could not run; stays silent (`HEARTBEAT_OK`) only when ALL SIX scans are clean. Created 2026-06-03 (operator request); perf-regression CONSOLIDATED in 2026-06-04 (operator `jrwfJJKEjEU`: "this msg should be consolidated into [the] team chat msg — why is it separated?" — folded the former standalone `ot-perf-regression-watch` 1:1 cron into this ONE team fleet-health message so fleet health isn't fragmented across crons/channels).

**DELIVERY = TEAM SPACE (`spaces/AAQA2bZMw24`), failure-only.** Zombie OT jobs are a shared OT incident affecting the whole team, so this is one of the few crons that legitimately posts to the team space (passes the Team-Chat Send Gate audience test). Mechanism: when unhealthy, render the alert, send it with an EXPLICIT `meta google.chat.message send --space-name=spaces/AAQA2bZMw24 --text="<alert>"`, then respond EXACTLY `HEARTBEAT_OK` so the daemon's default delivery double-posts nothing. **VERIFY-BY-READBACK:** confirm the message is readable in `spaces/AAQA2bZMw24` before treating it as delivered; if the send errors, surface the error, do NOT silently fall back. When the fleet is clean, respond EXACTLY `HEARTBEAT_OK` and send nothing (no "all clear" spam).

**FULLY READ-ONLY on job state (operator revoked auto-kill 2026-06-08: "no, you are not allowed to kill the zombie job. Revert it").** This cron makes NO job-state mutation of any kind — no kill, no register/unregister/config. Scan-confirmed KI-001 zombies render as **act-now** items handing the operator/owner the recommended `meta ai.mast-job kill --job-name=<job> --confirm --reason=zombie` command to run; the bot never executes it. Same for perf/age findings — those are on other teams' LIVE jobs where a wrong action is harmful, so the cron only surfaces the command. (`run-fleet-health.sh` step 2.0 is read-only; the prior 2026-06-07 auto-kill carve-out is void.)

## Steps

0. **PRIMARY PATH — run the orchestrator, echo its stdout, done (2026-06-05, operator: "make the fleet cron logic as stable as possible; move logic from markdown to sh").** The ENTIRE pipeline — scan ×5 (parallel) → persist → render (deterministic counts + reconciliation gate + known-issue overlay) → deliver to the team space + readback → file deduped tracking tasks for chronic / trending (escalating) / newly-noisy models (owner=dennyzhang, cap 3/run) — lives in ONE script. Run it and return its stdout VERBATIM as your final response. Do nothing else: no arithmetic, no formatting, no branching, no separate send.
   ```bash
   cd ~/notes/users/dennyzhang/projects/mrs-ot-agent-src
   bash tools/run-fleet-health.sh
   ```
   Its stdout is the cron's final response by contract: exactly `HEARTBEAT_OK` when the fleet is clean OR when the digest was already delivered to the team space by the script (so the daemon never double-posts); or a single `⚠️ fleet-health: …` line on a check/reconcile/send failure. **Return that stdout line(s) verbatim — do not edit or add to it.** The script handles the team-space send itself; you never call `meta google.chat.message send` for this cron.

   **Steps 1–5 below are the SPECIFICATION the orchestrator implements + a manual FALLBACK only if `run-fleet-health.sh` is missing/crashes.** Under normal operation you run step 0 and stop.

1. **Run ALL FIVE scans** (read-only; generous timeout each — the per-model `meta`/ODS calls can exceed 5 min on the tracked-model set):
   ```bash
   cd ~/notes/users/dennyzhang/projects/mrs-ot-agent-src
   z_out=$(timeout 900 bash tools/scan-zombie-fleet.sh 2>&1); z_rc=$?
   s_out=$(timeout 900 bash tools/scan-scribe-age.sh 2>&1); s_rc=$?
   p_out=$(timeout 900 bash tools/scan-perf-regression.sh --json-only 2>&1); p_rc=$?
   x_out=$(timeout 900 bash tools/scan-pkg-expiry.sh --json-only 2>&1); x_rc=$?
   d_out=$(timeout 900 bash tools/scan-dpp-starvation.sh --json-only 2>&1); d_rc=$?
   ```
   All five scripts share the same exit-code convention: `0`=problems found, `1`=clean, `2`=usage/data-fetch failure. (Timeout → treat that scan's rc as 2 / check-failed.) pkg-expiry JSON fields: `job_name, model_entity_id, mast_url, owner, model_type, pg, mvai_tier, pkg_kind(app|base), pkg, expires(YYYY-MM-DD), days_left, severity(act-now|watch)`; act-now = expired or ≤3d, watch = ≤7d, non-ephemeral pkgs skipped (never flagged). Render: 📦 act-now line `re-pin / rebuild pkg before restart`, 📦 watch one-liner, coverage `pkg-expiry ok/scanned (N expiring)` — the renderer does all of this from `--pkg-expiry`. dpp-starvation JSON fields: `job_name, model_entity_id, mast_url, owner, model_type, pg, mvai_tier, recent_max_qps, recent_h, severity(act-now)`; flagged = RUNNING job with `dpp_worker` examples-read rate < EPS over the recent window (a PROXY for data starvation — the operator's canonical threshold is per-job DPP-starvation %>95% from Scuba `dpp_stats_v2` scribe-token-ingress / canvas pzoyunzx, not yet wired). Render: 📊 act-now line `confirm dpp_stats_v2 ingress, route DPP/scribe`, ≥8-at-once SYSTEMIC-COLLAPSE to one line, coverage `dpp N` — the renderer does all of this from `--dpp-starvation`.

1-b. **Persist this run's STRUCTURED results for trend retros (deterministic; do NOT skip).** Right after the scans, before rendering, append the run's JSON to the append-only history store. This is what lets us later answer chronic-vs-transient, age-drift, per-PG regression-frequency, MTTR — the rendered chat digest is thrown away, the JSON is not. Write the captured outputs to temp files and call the persist helper (it greps the JSON lines out of mixed human+JSON output, splits perf's `{"summary":…}`, and is idempotent on `run_ts`):
   ```bash
   _td=$(mktemp -d)
   printf '%s\n' "${z_out}" > "${_td}/z"; printf '%s\n' "${s_out}" > "${_td}/s"; printf '%s\n' "${p_out}" > "${_td}/p"
   bash tools/persist-fleet-history.sh \
     --zombie "${_td}/z" --zombie-rc "${z_rc}" \
     --scribe "${_td}/s" --scribe-rc "${s_rc}" \
     --perf   "${_td}/p" --perf-rc   "${p_rc}"
   ```
   Writes one line to `mrs-ot-agent-context/state/fleet-health-history/YYYY-MM/runs.jsonl` (notes-only runtime corpus — survives reinstall, NOT mirrored to fbcode). This persists on EVERY run, healthy or not, so the history has no gaps. The persist step's stderr is NOT chat output — never fold it into the final response. The retro consumer over this JSONL is a thin follow-up to build once a week+ of history accumulates.

1-c. **RENDER + DELIVER via the deterministic renderer — this is the PRIMARY path; you do NOT compute or format any count yourself (2026-06-05).** Every count/coverage number in this digest is computed in code from the scan summaries and reconciled by an assertion, because the LLM render fabricated `zombie 62/65 ok` and `perf 6/65 flagged` on 2026-06-05. Run:
   ```bash
   # reuse the temp files written in step 1-b (${_td}/z, /s, /p)
   digest=$(python3 tools/render-fleet-digest.py \
     --zombie "${_td}/z" --zombie-rc "${z_rc}" \
     --scribe "${_td}/s" --scribe-rc "${s_rc}" \
     --perf   "${_td}/p" --perf-rc   "${p_rc}" 2>/tmp/render.err); rc=$?
   ```
   **Known-issue overlay (2026-06-05):** the renderer auto-loads `mrs-ot-agent-context/state/known-issues/registry.json` and re-routes any finding matching an ACTIVE (non-expired) entry into a 🔕 Known section (owner already on it — not re-alerted); an EXPIRED match re-surfaces in Act-now with a re-triage tag. Operator curates the registry via `tools/add-known-issue.sh`. You do nothing extra — it's inside the renderer.
   **Expected-delta read contract (2026-06-12, accepted-regression tier — Shepherd-KB idea):** a SIBLING registry `mrs-ot-agent-context/state/expected-deltas/registry.json` (operator-curated via `tools/add-expected-delta.sh`) records regressions that are REAL but ACCEPTED over a bounded range (≠ known-issues, which are not-real noise). Read contract: a finding whose `(model_eid, signal)` matches an ACTIVE (non-expired, in-range) accepted-delta AND is **within the magnitude bound** → annotate `expected — <reason> (≤<bound>)` and do NOT escalate; **beyond the bound, out of range, or expired → FIRE normally** (bigger-than-declared still fires). NOTE: a deterministic renderer overlay (like the known-issue one) is the NEXT build; until it lands, apply this contract when reading findings.
   Then deliver STRICTLY by exit code — do NOT edit, re-count, or reformat `$digest`:
   - `rc==1` → fleet clean. Respond EXACTLY `HEARTBEAT_OK` (the renderer printed the HEARTBEAT_OK token; send nothing to the team space).
   - `rc==0` → `$digest` IS the alert. Send it verbatim: `meta google.chat.message send --space-name=spaces/AAQA2bZMw24 --text="$digest"`, verify-by-readback, then respond EXACTLY `HEARTBEAT_OK`.
   - `rc==3` → RECONCILIATION FAILED (a count didn't add up; cat `/tmp/render.err`). The renderer withheld the digest and printed a one-line withhold notice — send THAT to the team space (so a broken digest is loud, not silent), and surface the `render.err` reconcile errors to the operator 1:1. Do NOT hand-assemble a replacement digest.
   - `rc==2` → a scan errored; fold the per-scan "could not run" line (step 2) into the alert.
   **The detailed parse/classify/format rules in steps 2–5 below are now the renderer's SPECIFICATION (what it implements) and a fallback ONLY if the renderer binary itself is missing/crashes — under normal operation you run the renderer and deliver its output. Never hand-render counts when the renderer succeeded.**

2. **(Renderer spec / fallback only — see 1-c.) Branch on the three rc's. The run is UNHEALTHY if ANY scan found problems or could not run.**
   - `z_rc==1` AND `s_rc==1` AND `p_rc==1` (all clean) → respond `HEARTBEAT_OK {fleet:clean, zombies:0, training_age:0, perf:0}`. Send nothing.
   - `z_rc==0` (zombies) → parse the per-zombie JSON lines (fields: `job_name, version, owner, base_layer_pkg, base_upstream_date, fix_d98638473_included, gap_minutes, reply_file_timestamp` PLUS `model_type, pg, mvai_tier` — all now emitted by the scan via the shared `lib-enrich-model.sh`, 2026-06-05). **Use the JSON `model_type`/`pg`/`mvai_tier`/`owner` VERBATIM — do NOT re-enrich in the prompt.** Feeds the 🧟 section of the combined alert (step 3). **The scan ALSO emits a trailing machine line `{"summary":{"scanned":N,"zombies":Z,"errors":E,"ok":OK,"threshold_min":T}}` (2026-06-05) — this is NOT a zombie; SKIP it when iterating findings, and use it for the coverage line (step 3). The zombie COUNT is `summary.zombies` / the number of non-summary JSON lines — NEVER inferred by subtracting a coverage figure from the tracked-model total. `scanned` is RUNNING models checked (< total tracked), so the coverage denominator is `scanned`, not 65.**
   - `p_rc==0` (perf drift) → parse the per-model JSON lines from `scan-perf-regression.sh` (fields: `entity_id, model, owner, model_type, pg, mvai_tier, signals[]` (each `{signal, …}` where signal ∈ `qps_down` | `gpu_mem_climb` | `example_age_up_cosignal`), `likely_subsystem`). **`model_type`/`pg`/`mvai_tier`/`owner` are now emitted by the scan (shared `lib-enrich-model.sh`, 2026-06-05) — use VERBATIM, do NOT re-enrich in the prompt.** Feeds the 📉 section of the combined alert (step 3). NOTE: `scan-perf-regression.sh` SKIPs models with no `qps/global/window/train` (coverage boundary — retrieval/sub-models keyed elsewhere). Under `--json-only` it prints a trailing machine-summary line `{"summary":{"scanned":N,"total":T,"flagged":M,"skipped":K,"errors":E}}` — parse THAT for the scanned/skipped/total counts (the human `[2/2] Summary` log is suppressed by `--json-only`). Render `scanned M · total T · skipped K` in the 📉 header so coverage is never silently implied. (2026-06-04: prompt-change-validator caught that the old "read the summary log" instruction was unreachable under `--json-only`.) **RECONCILE the count (2026-06-05, operator: "perf 6/65 flagged but only 3 shown — which to trust?"): the coverage line states `summary.flagged` VERBATIM, and the displayed perf items MUST account for ALL of it — `perf-in-act-now + perf-in-watch + systemic-collapsed = flagged`. Never show a flagged count that doesn't reconcile with what's displayed. SYSTEMIC-COLLAPSE rule: when ≥5 flags share the SAME `likely_subsystem` across multiple PGs/tiers, that correlation is almost certainly diurnal/systemic, NOT N independent regressions (cross-check: QPS-down WITHOUT a co-occurring example-age breach = lower volume, not a stall) — render ONE line `📉 <N> models same <subsystem> pattern — likely diurnal/systemic [⚠ review], full list <paste>` and elevate ONLY genuine outliers (e.g. QPS→0, or a magnitude far outside the correlated band) to individual 📉 act-now lines. Do NOT emit N separate act-now perf lines for a correlated cluster (that's the noise the digest exists to prevent). Until the diurnal-aware baseline lands, the raw `flagged` count is KNOWN to over-count on the nightly trough — say so, never present it as N real regressions.**
   - `s_rc==0` (scribe breaches) → parse the per-breach JSON lines. **`scan-scribe-age.sh` now emits enrichment fields directly (deterministic, in-script — 2026-06-05 fix): `model_type`, `pg`, `mvai_tier`, `owner`** (plus `job_name, model_entity_id, example_age_ms, example_age_min, threshold_ms, window_start, window_end, scan_time`). **Use these JSON fields VERBATIM — do NOT re-run `meta ai.mast-job metadata`/`ai.model describe` enrichment in the prompt.** (Root cause of the 2026-06-04 blank model_type/PG: enrichment was a prompt-instructed LLM step that silently skipped at runtime; it's now baked into the scan script per #14b "enforce mechanically, not by prose." The script handles the model_type fallback `flow_model_type`→registry `model_type_name`→`unknown` (never `rm_attribution`), owner fallback (mast→registry→oncall→`?`), `mvai_tier` (→`T?`), and PG derivation.) **Cause-class is ALSO emitted by the scan now (deterministic in-script QPS probe, 2026-06-05 — replaces the old skippable prompt liveness-probe step): the JSON carries `cause_class` ∈ `not-training` | `trainer-behind` | `upstream-lag` | `unknown` (and `cause_reason="probe-failed"` only when `unknown`). USE `cause_class` VERBATIM — do NOT run any liveness/QPS probe in the prompt. Then OVERLAY zombie-overlap, which is the prompt's job (the scan does not see the zombie list): if the same job is in the zombie list, the rendered cause becomes `zombie (kill)` regardless of `cause_class`.** Map for the action line (step 3): `zombie`→kill; `not-training`→check job logs; `trainer-behind`→efficiency; `upstream-lag`→Scribe/DPP write-QPS for the category; `unknown`→tag `[⚠ review: cause probe-failed]`. **Rank breaches by `example_age_min` desc**; tag `act-now` if zombie-overlap OR `example_age_min > 30` OR (`MVAI_TIER == TIER_1` AND `example_age_min > 15`), else `watch` (TIER_1 models get a tighter trigger — they're highest-priority serving). (No undefined middle band — 2026-06-03 backtest fix; tier sensitivity added 2026-06-04.) Feeds the ⏱️ section of the combined alert (step 3).
   - `z_rc==2`/timeout OR `s_rc==2`/timeout OR `p_rc==2`/timeout (a check could not RUN) → that scan's status is UNKNOWN, not necessarily bad. Add a per-scan line to the alert: `⚠️ *<zombie|training-age|perf> check could not run* — <first error/timeout line from the relevant $out>. Status UNKNOWN; re-run \`bash tools/scan-<zombie-fleet|scribe-age|perf-regression>.sh\` or check meta-CLI access.` This flags the *check* is broken, distinct from the fleet being broken. If only one scan failed but others found problems, fold all into the one combined alert.

3. **ONE digestible alert — grouped by ACTION, not by check; scannable in ~5 seconds.** A reader scans "what needs me NOW" first; the check that found it (zombie/age/perf) is just an inline emoji tag, not its own section. Structure: BLUF action-counts → 🔴 Act-now (one tight line each) → 🟡 Watch (one collapsed line) → ✓ clean+coverage. Omit empty groups; never lead with a `0`.
   ```
   🩺 *OT fleet health* — <A need action · W watch>

   🔴 *Act now* (<A>)
   • <emoji> <MAST_LINK> `<TIER·PG>` — <signal> · *@<owner>* → <ONE action> <[⚠ review: <why>] iff uncertain>

   🟡 *Watch* (<W>) — <emoji> <MAST_LINK> <value> · <emoji> <MAST_LINK> <value> …  (one line, clickable ids + values only, NO actions)

   ✓ <clean checks + HONEST coverage> — e.g. `zombie clean · perf 0/29 flagged (36 no-metric)`
   ```
   **Rules (these ARE the digestibility fix, 2026-06-04 "many low-signal content; not scannable; review not flagged"):**
   - **Group by action, not check.** 🔴 Act-now = zombies + training-age `act-now` + confirmed perf regressions. 🟡 Watch = just-over-SLO / near-threshold (training-age `watch`, low-sev perf). Per-line emoji: 🧟 zombie · ⏱️ age · 📉 perf.
   - **Within Act-now, CLUSTER lines by type** (🧟 zombie / ⏱️ age / 📉 perf) so same-type issues are adjacent — the reader batches one mental mode + one owner-action per cluster (e.g. all kill commands together). Ordering rule (deterministic): (a) order the type-BLOCKS by the worst-severity item in each block, so the single most-critical item still leads the whole section; (b) within a block, sort severity desc (zombie by `gap_minutes`, age by `example_age_min`, perf by qps-drop %). Still ONE 🔴 *Act now* list — no per-type sub-headers (the emoji already signals type; a header would be a non-action line, density violation).
   - **Act-now = ONE line:** `<emoji> <MAST_LINK> `<TIER·PG>` — <signal> · @owner → <single action>`. `<signal>` = the one number that matters (`zombie 9h` / `age 79m` / `qps ↓46%`). `<single action>` = the action for THIS item's `cause_class` (taken VERBATIM from the scan JSON) ONLY: zombie→recommend `meta ai.mast-job kill --job-name=<job> --confirm --reason=zombie` (the human runs it; bot never does); not-training→check job logs; trainer-behind→efficiency; upstream-lag→Scribe write-QPS for the category; unknown→state cause couldn't be probed. NO generic cause-menu.
   - **⚠ review flag (REQUIRED for trust — directly answers "learning that needs human review isn't flagged"):** append `[⚠ review: <reason>]` to any item whose diagnosis is uncertain. For scribe-age breaches this is driven by the SCAN, not a prompt re-classification: flag ONLY when the breach's `cause_class=="unknown"` (genuine QPS-probe failure — render `[⚠ review: cause probe-failed]`). A present `cause_class` (not-training / trainer-behind / upstream-lag) is a real computed verdict — do NOT flag it. (The old "cause-class not probed" path is GONE — the scan always computes a cause-class or honestly reports `unknown`.) For perf/zombie items, still flag a novel pattern not in known-patterns.md or partial data. NEVER present an unverified inference as settled.
   - **Watch = ONE collapsed line** — each id rendered as a clickable `<MAST_LINK>` (same as act-now; the id IS the link), value after it, NO per-item actions; never expand it.
   - **✓ clean + coverage (HONEST) — EVERY coverage number comes from a scan's `{"summary"}` line VERBATIM, never computed/inferred in the render (2026-06-05: a render fabricated `zombie 62/65 ok` — truth was scanned=61, zombies=1 → `60/61 ok`; the operator reasonably read 65−62 as 3 zombies).** Zombie: `zombie <summary.ok>/<summary.scanned> ok (<summary.zombies> zombie)` using the zombie scan's summary — denominator is `scanned` (RUNNING models checked), NOT the tracked total. Perf: `perf <flagged>/<scanned> flagged (<skipped> no-metric)` from the perf summary. Name clean checks; never a bare "✓ perf" (the perf scan skips models; calling that "clean" is dishonest — 2026-06-04). If a needed count isn't in a summary line, write "coverage not reported" — never guess.
   - **Cut low-signal:** no cause-menu, no field shown twice, drop `<model_type>` when the linked id + `TIER·PG` tag already identify it. If 🔴 Act-now >6, show the 6 worst + `(+K more — <paste>)`.
   **Render `signals[].h` verbatim** (perf K/M/%/pp pre-formatted by the scan; never reconstruct from raw ints). **`<MAST_LINK>`** = the job id as ONE clickable link `<https://www.internalfb.com/mlhub/pipelines/runs/mast/<job_name>|<job_name>>` — the id IS the link, never a separate `MAST:` line. Owner resolved per step 2.

4. **Density (HARD):** BLUF first; no id/command/URL appears twice; per section ≤ (1 + 4×N) lines for zombies, ≤ (1 + 3×N) for scribe-age. If a section has N>5, show the 5 worst (zombies by `gap_minutes`, scribe by `example_age_min`), append `(+K more)`, and drop a paste with that section's full JSON list rather than scrolling the channel.

4-b. **Readability / number format (HARD, ALL sections — 2026-06-04 `jrwfJJKEjEU`: "qps 16,374/30,124 → qps: 16.3K").** Humans scan abbreviations, not raw digits:
   - **Counts / QPS ≥ 1000 → K/M**: `16,374` → `16.4K`, `1,047,440` → `1.05M`. NEVER raw comma-grouped digits. (The scan scripts pre-format these — perf via `signals[].h`/`kfmt`; render those VERBATIM. For any number you format yourself, same rule: <1000 integer, <1M → `X.XK`, ≥1M → `X.XXM`.)
   - **Durations**: `< 90 min` → `Nm` (e.g. `40m`); `≥ 90 min` → `X.Xh` (e.g. `gap 2976m` → `gap 49.6h`). Applies to zombie `gap_minutes` and any age rendered in the line.
   - **Percentages → integer** (`↓46%`, `94%`), never `46.3%`.
   - **One number per fact** — show the current value + the drop, never both the ratio AND the drop (the old `16,374/30,124 (54% ↓46%)` had `54%` and `↓46%` saying the same thing; keep only `↓46%`).
   - **Never a bare/`NaN`/`None` number** — render `?` (the scan's `kfmt` already maps non-numeric → `?`).

5. **Self-reporting:** every figure (zombie count, owner, gap, fix-present, scribe example-age min) is taken from the scripts' JSON output, never narrated. If JSON parse fails for a scan, post that script's summary tail verbatim and tag `UNVERIFIED-PARSE` rather than guessing.

**Output discipline:** final response is EITHER the single sent-confirmation path ending in `HEARTBEAT_OK`, OR bare `HEARTBEAT_OK`. Never narrate ("running scan…", "fleet clean, nothing to do") as the final response — that leaks to chat.
