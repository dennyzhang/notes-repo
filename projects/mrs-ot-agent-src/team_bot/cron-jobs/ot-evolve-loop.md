[ot-evolve-loop cron] Every 2h. Self-evolve loop: run the eval harness, and once the fitness function is complete + grader-trusted, batch-mutate the OT agent's knowledge keeping ONLY eval-proven winners, committed to notes.

**OUTPUT CHANNEL = OPERATOR 1:1 ONLY.** This is operator-facing plumbing with no team-wide value — output must NEVER appear in the team space `spaces/AAQA2bZMw24`. Mechanism (same as ot-knowledge-distillation): for any real committed improvement, make an EXPLICIT `meta google.chat.message send --space-name=spaces/AAQAVOjYc80 --reply-in-thread=<existing evolve thread, or append # new-topic> --text="…"` to the operator 1:1, THEN respond with EXACTLY `HEARTBEAT_OK`. No-op / no-win / signal-only runs: just `HEARTBEAT_OK`, nothing else. NEVER emit a post-block/narration as the final response (the daemon auto-delivers it to the team space).

**IDEMPOTENCY:** if a prior evolve run is still in progress, respond `HEARTBEAT_OK` immediately. Otherwise apply the DAILY-BASELINE GATE below — do NOT short-circuit to `HEARTBEAT_OK` just because no mutation candidate is pending.

**DAILY-BASELINE GATE — SHARDED (gap #1 fix, 2026-06-13).** The full ~57-77-case eval CANNOT complete in one daemon tick: each triage agent reads ~141KB (SKILL+KP+TD) then reasons, blowing the 180s/agent cap; iter5/12/13 stalled at conc 4/2/1, after which a hand-rolled n=5 fallback wrote a garbage 0.209 row. Per-agent runs finish well under the cap when the batch is ~5 cases (proven iter6). So the daemon produces the daily baseline **incrementally, one small shard per tick**, then folds the shards into ONE full composite:
- **Derive what this tick owes from DATA, not the clock.** Read `reports/eval-flow-history.jsonl`. Let `GV` = today's gold-set `version` (from `eval/gold-set.json`). Among rows with `source=="daemon"`, `gold_version==GV`, and `date` starting with today's daemon-local `%F`:
  1. If a `partial:false` (full/rolled-up) daemon row for today+GV already exists → today's baseline is DONE. Skip to candidate-gated work (other ticks).
  2. Else compute the FIRST un-graded shard index `i` = the lowest shard in `0..num_shards-1` that has no partial row yet today (num_shards = ceil(corpus_total / shard_size); shard_size=5). Run exactly that ONE shard (see HOW TO INVOKE), append its `partial:true` marker.
  3. After appending, if shard `i` was the LAST shard (all `0..num_shards-1` now present) → run the ROLLUP (see below) to emit the full `partial:false` composite.
- **All ticks** advance at most ONE shard per tick (cheap, ≤~5 agents, never stalls). With 16 shards and a tick every 2h, the full baseline completes within a day; the next day's GV (or same GV) restarts at shard 0. Self-healing: a missed tick just means the next tick picks up the lowest missing shard.
- **Candidate A/B (STAGE-3) ticks** also run sharded — A/B a candidate by injecting `inject_rule` into the SAME shard index that the baseline used, so the comparison is on identical cases (see STAGE 3).
- **FORBIDDEN:** (a) returning `HEARTBEAT_OK` on a tick that owed a shard WITHOUT running it + appending its marker; (b) the hand-rolled arbitrary-n fallback — if a shard can't run, record `partial:true` + an `error` field and NEVER synthesize a composite. A degenerate/partial row can NEVER count toward the baseline (guarded twice: `eval-stats.sh` excludes `partial:true` AND any no-flag row with `n<30`).

**Cadence rationale:** one small shard/tick proves the daemon path end-to-end every tick at ~5-agent cost (no stall), and the daily rollup is the trustworthy full-corpus composite the noise band + STAGE-3 A/Bs measure against. We do NOT force a full eval in one tick (that is the exact thing that stalls).

HARNESS: `~/.myclaw-ot-bot/eval-flow.js` (triage quality; supports `args.inject_rule` for candidate A/B) + `~/.myclaw-ot-bot/eval-scoping.js` (detection_recall + scoping_precision). Notes mirrors are `.txt` under `mrs-ot-agent-context/eval/` (notes rejects `.js`). State/trail: append each run to `mrs-ot-agent-context/eval/reports/run-<YYYY-MM-DD>-<n>.md`.

**HOW TO INVOKE THE EVAL — EXACT, NO IMPROVISATION, ONE SHARD PER TICK (gap #1 fix).** Run the eval ONLY via the TOP-LEVEL `Workflow` tool pointed at the existing harness script. **Before calling Workflow**, write the daemon shard state to `/tmp/eval-daemon-shard.json` (gap #2 fix: args global is dropped by the daemon framework, so the harness reads this file instead):
```
Bash: echo '{"shard":<i>,"shard_size":5,"source":"daemon"}' > /tmp/eval-daemon-shard.json
```
Then call Workflow:
```
Workflow({ scriptPath: "~/.myclaw-ot-bot/eval-flow.js", args: { source: "daemon", shard: <i>, shard_size: 5 } })
```
- **`source:"daemon"` is MANDATORY** so the harness stamps `run_marker.source="daemon"`. **`shard:<i>` is MANDATORY on every daemon tick** — `<i>` is the first un-graded shard for today+GV (see the gate above). The harness deterministically sorts the eligible corpus by id and grades only cases `[i*5, i*5+5)`, emitting `run_marker.partial=true` plus `shard`, `shard_size`, `num_shards`. A ~5-case shard finishes well under the 180s/agent cap (proven iter6) — this is the structural fix for the iter5/12/13 stalls (which ran the WHOLE corpus per agent-wave).
- For a candidate A/B add `inject_rule:"<the rule>"` to the SAME shard index the baseline used (identical cases → clean comparison).
- **FORBIDDEN:** (a) running the eval WITHOUT a `shard` arg from the daemon path (that is the whole-corpus run that stalls); (b) improvising an inline / custom Workflow that does triage AND grading in one agent, or any single-agent self-grading run (iter11 did this: composite 0.934, self-graded, discarded). The harness's value is per-case isolation: a FRESH triage subagent per incident, then a SEPARATE independent grader — never the same agent both; (c) **the hand-rolled arbitrary-n fallback** (the iter13 n=5 0.209 garbage). If the `Workflow` tool genuinely errors on a shard, DO NOT synthesize a composite — append a `partial:true` marker carrying an `error` field and stop; the next tick retries the same lowest-missing shard. Never fake a number, never collapse to one agent.

**RECORD THE MARKER — every shard, no exception (gap #1 fix).** The Workflow sandbox CANNOT write files, so the harness EMITS its `run_marker` in the returned result; the CRON (you, the caller) MUST persist it. After each shard run, save the result JSON to a temp path and run:
```
bash ~/notes/users/dennyzhang/projects/mrs-ot-agent-src/tools/eval-stats.sh append <result.json>
```
This appends one structured line — `{date, source, wf, timestamp, composite, calibration, owner, decisiveness, hallucination, root_cause, n, gold_version, partial, shard, shard_size, num_shards}` — to `reports/eval-flow-history.jsonl`. **A shard is `partial:true` and is EXCLUDED from the baseline/noise band by `eval-stats.sh`** (twice-guarded: explicit flag + `n<30` degenerate-row floor). Recording the marker is part of "ran the shard" — a shard whose marker was not appended did not count.

**ROLL UP WHEN THE LAST SHARD LANDS (gap #1 fix).** After appending a shard's marker, check whether every shard `0..num_shards-1` for today+GV is now present (the gate's step 3). If so, fold them into ONE full-corpus `partial:false` composite:
```
bash ~/notes/users/dennyzhang/projects/mrs-ot-agent-src/tools/eval-rollup.sh rollup   # gold_version + date default to today's
```
`eval-rollup.sh` n-weights the per-shard dims into one `source:"daemon", partial:false, n≈corpus` row (the trustworthy daemon composite the daily baseline owes). It is **idempotent** (skips if today's full row exists) and writes NOTHING if any shard is missing (no fabrication — it prints which shards are missing). Run `eval-rollup.sh status` to inspect progress. A daemon-fired FULL graded baseline is then PROVABLE: a row with `source=="daemon"`, `partial==false`, non-null `composite`, `n>0`. Then read the noise band with `eval-stats.sh stats` (partials auto-excluded).

FITNESS = calibration .35 / detection_recall .20 / scoping_precision .15 / owner .15 / decisiveness .15; hallucination = HARD per-case gate. Current baseline (2026-06-11): composite **0.684** after R22 (detection_recall 0.42→0.96). calibration ~0.64, owner ~0.25, decisiveness ~0.53.

SEQUENCING — do NOT batch-mutate until the measuring stick is complete and trusted:
- STAGE 1 (fitness-completion): if detection_recall/scoping_precision are unwired, run eval-scoping and fold them into the composite. Expand the gold set toward the full 180 eligible. If generalization is still a leak-proxy, implement temporal hold-out (P-row/R-rule creation dates from `sl` history). One stage-1 item per run.
- STAGE 1b (ONLINE SIGNAL — auto-harvest, NO operator effort; the only proof offline gains are real): mine the offline↔online correlation from the two daily streams the operator already produces — (a) the **1:1 co-debug threads** in `spaces/AAQAVOjYc80` (operator corrections/confirmations of the bot's triage; read via the per-space sqlite `messages` table or `gchat read`), and (b) **postmortems of closed SEVs/posts** (the bot's actual FIRE-TIME triage verdict — from the resolved-incident archives / ot-sev-state.json / triage threads — vs the eventual real root cause). Compute the bot's REAL production triage accuracy + operator-correction rate; append to `eval/reports/online-signal-<date>.md` and track vs the offline composite. **If offline composite and online accuracy DIVERGE, the eval is mis-calibrated → fix the eval (grader/gold-set/weights) BEFORE trusting more mutations.** Do NOT ask the operator to hand-flag anything.
- STAGE 1c (PROPOSAL-PRECISION TRACKING): refresh the suggested-diff ledger — `bash ~/notes/users/dennyzhang/projects/mrs-ot-agent-src/tools/diff-ledger.sh 60` (writes `eval/reports/diff-ledger.{md,json}`: land vs abandon rate of the bot's OWN diffs = the loop's output quality). A rising **abandon rate** = the loop proposes low-value churn → flag as a proposal-precision regression (its own evolve target, distinct from triage fitness).
- STAGE 2 (grader trust): run a codex co-grader on a sample, record agreement; if low, fix the grader, do NOT mutate.
- STAGE 3 (BATCH mutation): generate K=5–10 candidates from the ranked failure list + weakest dims. Current targets (RE-POINTED 2026-06-12 from the clean baseline, composite 0.704): the genuinely WEAK components — **scope-detector (root_cause 0.50, halluc 0.33)** and **T2-training (0.56)** — plus **hallucination where it concentrates** (scope-detector / lifecycle). **STOP grinding owner-routing:** it improved to 0.39 and it is a LOOKUP, not a reasoning rule — move it to a deterministic capability (`meta ai.model describe → model_owner`), NOT another prose rule. **Anti-divergence rule (8 rounds → 0 landed mutations through iter6):** a candidate whose length GROWS each iteration is diverging — every added clause spawns new edge cases (v8-Own hit 400 words / 10 paths). Discard and redesign MINIMAL: a short rule + a mechanical assert beats a long branch-tree. If a single target needs >2 redesigns, switch targets or escalate to the operator. (Legacy R19 OVER-APPLICATION precedence fix — `_retrieval`+SPARSE_DELTA / scribe_read_proxy alert → check for an active upstream Scribe/ZippyDB SEV (R21/P57) BEFORE concluding R19 DETECTOR_BROKEN — remains valid if it resurfaces.) A/B each via `eval-flow args.inject_rule` on the SAME shard index the baseline graded (identical cases → clean delta), INCREMENTAL (only the cases a change can affect). **Daemon-path reliability (2026-06-13 SHARDING fix, supersedes the concurrency-wave approach):** the whole-corpus run stalls in the daemon regardless of `concurrency` (iter5 conc4, iter12 conc2, iter13 conc1 all stalled — per-agent 141KB read + reasoning blows the 180s cap). The fix is `args.shard` — grade ~5 cases per tick; each shard finishes well under the cap. `concurrency` is no longer the lever (a 5-case shard runs in one small wave). Per-case isolation is preserved; the grader stays per-case. KEEP a candidate only if its composite rises **BY MORE THAN THE NOISE BAND** measured against the FULL rolled-up baseline AND zero regression AND it passes codex adversarial review; after keeping a batch, re-eval the COMBINED set and drop combination-regressors. **NOISE-BAND GATE (HARD — the eval swings ±~0.03 between identical runs at n≈56–59):** after EVERY eval-flow run, append its result with `bash ~/notes/users/dennyzhang/projects/mrs-ot-agent-src/tools/eval-stats.sh append <result.json>`; read the current band with `eval-stats.sh stats` (composite **std**). A candidate whose composite gain is **≤ that std is NOT a win** (it's run-to-run luck) — reject it. Prefer averaging the candidate over ≥2 runs before accepting. This stops the loop from committing noise as improvement. Commit winners to the NOTES repo (operator-authorized). NEVER auto-submit a Phabricator diff; NEVER mutate live external surfaces (notes→fbcode mirror stays operator-gated).

DO NOT re-try already-rejected candidates — check prior `eval/reports/run-*.md` first (e.g. the **citation-discipline guard was REJECTED 2026-06-11**: it cut hallucination but over-hedged, composite 0.684→0.677).

CHEATSHEETS (load before the modality): `cheatsheets/eval/building-agent-evals.md` (method) and `cheatsheets/diff/common.md` before any commit.
