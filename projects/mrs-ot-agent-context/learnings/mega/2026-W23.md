# Mega Learnings — 2026-W23 (Jun 1–7)

_Auto-generated by ot-knowledge-curation cron. Each entry = a cross-incident pattern distilled from the postmortem corpus._

---

## 2026-06-02 23:00 PT — SPARSE_DELTA monitoring conflates structurally distinct publish paths (N=2, validator confirmed)

- **Pattern:** Two IG Reels models fired SPARSE_DELTA anomalies that monitoring couldn't distinguish by cause: one was a 13-day gap from GPU starvation (entitlement oversubscription, 7 competing jobs → 0 allocated tasks); the other was flooding (3.3 delta/min vs expected 1/6min) that was intentional RES behavior. (N=2, post corpus — both resolved 2026-06-02)
- **Surface:** POST-1314267290126950 (ig_reels_starsearch_t2i_retrieval model 2145491885, GPU starvation → SPARSE_DELTA gap 13d), POST-1332204528874290 (ig_reels_tab_esr_ttsn model 2122065668, enable_raw_embedding_streaming=True → SPARSE_DELTA at 15s vs expected 6min)
- **Root mechanism:** SPARSE_DELTA metrics don't carry a path tag distinguishing (a) training-schedule-triggered deltas, (b) RES-path deltas (enable_raw_embedding_streaming=True at res_streaming_interval_s=15s), and (c) gap caused by resource starvation masking as publisher crashes (Gloo timeout, port orphan). Monitoring fires on volume thresholds blind to cause. POST-1332204528874290 resolution note: "monitoring conflated RES-path deltas with training-path deltas."
- **Detection gap:** No P-row covers GPU entitlement starvation → SPARSE_DELTA gap (misread as publish failure). No alert differentiates RES-path vs training-path delta rate. Oncall must distinguish manually from crash logs.
- **Generalization:** SPARSE_DELTA monitoring needs path-type metadata. Candidate monitoring improvements: (1) annotate delta events with source path (RES vs training) in TMS metadata; (2) add entitlement utilization check to the SPARSE_DELTA gap runbook (check reels_retrieval_main_online_qe utilization before assuming publish failure); (3) Gloo timeout + port-orphan crash = GPU starvation signature, not a publish bug — add to CL-018 fast-path checks.

---

## 2026-06-02 23:00 PT — S668542 scribe quota exhaustion: cascading alert duration anomaly across model families (N=2, validator confirmed)

- **Pattern:** S668542 (Feed scribe quota exhaustion, 2.797 > 2.66 TB/s, In Progress since 2026-05-27) is driving alerts across two distinct models and two distinct alert types — scribe_read_proxy.client_lag_in_seconds AGG (P57) on ig_organic_feed_mtml (model 878102693) and dpp_worker.scribe_example_age_ms >10m (P04 candidate) on facebook_ifr_main_mtml_main (model 2143912626). The IFR example-age alert persisted for 27 days — anomalous, possibly a continuous threshold breach or alert system artifact. (N=2, alert corpus)
- **Surface:** A1421314379760011 (ig_organic_feed_mtml 878102693, 2026-06-01, P57, ~27h alert), A2076342857098875 (facebook_ifr_main_mtml_main 2143912626, originally fired 2026-05-06, P04 candidate, ~27-day alert duration)
- **Root mechanism:** S668542 is a persistent, long-duration Scribe infrastructure SEV (>6 days In Progress as of 2026-06-02). Unlike transient spikes, quota exhaustion at sustained throughput causes repeated alert fires across dependent model families. The 27-day alert duration on A2076342857098875 indicates either: (a) scribe throttling persisted since May 6 with S668542 as the latest manifestation, or (b) the alert was not properly closed after initial resolution and re-triggered. Both are covered by existing P-rows (P57, P04) and require no OT action — but long-duration Scribe SEVs need a distinct monitoring escalation path.
- **Detection gap:** No alert-management rule triggers escalation when an UPSTREAM_INFRA alert persists >7 days without resolution. A 27-day alert with no assigned responder is invisible until it auto-clears. Neither P04 nor P57 has a duration-threshold escalation step.
- **Generalization:** Long-duration Scribe infrastructure outages (>7d) should auto-escalate through a separate Scribe-capacity-SEV tracking path rather than re-firing individual model alerts. P04/P57 should add: "if alert duration >7d with no assigned responder → file follow-up task to Scribe oncall + OT oncall; do not leave as passive MONITOR."

---

## 2026-06-03 23:00 PT — Fast-clearing alerts purge before triage metadata extraction (N=3, validator confirmed)

- **Pattern:** 3 FULL_SNAPSHOT alerts (A1455336899399360, A1558388828479222, A902257976006165) all stamped "ods/scuba fetch skipped — alert purged from feed before metadata extraction." Alerts for publish stability misses auto-clear within <12h (often within one cron cycle). By triage time, the alert is gone from the feed, making entity-link extraction impossible. (N=3, alert corpus — Jun 2–3 2026)
- **Surface:** A1455336899399360 (ig_textpost_feed_m2m_retrieval 2130324780, Jun 3), A1558388828479222 (facebook_cfr_video_mtml 919957162, Jun 3), A902257976006165 (facebook_reels_vdd_hstu_v0 877766818, Jun 3)
- **Root mechanism:** ot-alert-monitor cron lacks a "metadata-snap-first" step. All metadata queries run at analysis time (step 7), not at detection time (step 2). Fast-clearing alerts purge between detection and analysis. Step i-0c and entity-link extraction both return empty — forcing inference from title regex only, degrading confidence to 0.3–0.4.
- **Detection gap:** No pre-detection metadata cache in the cron. A batch metadata snap after step 2 (before filter/triage) would capture entity links, model_id, signal_class, assigned_user even if the alert subsequently purges.
- **Generalization:** Any alert class with <1h duration (FULL_SNAPSHOT miss, AGG auto-clear, retrieval SPARSE_DELTA oscillation) is susceptible. Metadata-snap-first pattern is the fix: snap all alert IDs at detection, store in-memory, fall back to snap during triage if feed returns empty. **Diff:** committed to ot-alert-monitor.md (step 2.5 added — notes commit `9afe7aadf069`).

---

## 2026-06-03 23:00 PT — D98638473 (light_cli fix) confirmed root cause for 14h SJD-invisible trainer hang (N=1+prior, validator confirmed)

- **Pattern:** W1332046782223398 (Threads U2M retrieval model 2124122280, trainer stuck 14h 9m May 16, no alert) confirms D98638473 as the root-cause fix 12 days after the incident. Prior incident W1326387856122624 (Max Kaplan, OT job logged error + hung 11h, same root cause) was already in distilled state — this is a confirmed RECURRENCE with verified fix. (N=1 new + prior recurrence evidence)
- **Surface:** W1332046782223398 (Denny Zhang, ig_textpost_feed_u2m_retrieval 2124122280, 2026-05-22 post); author comment 2026-06-03: "Root cause is found. Found a bug in light cli. Mitigation diff: D98638473"
- **Root mechanism:** Bug in mvai/light_cli — elastic agent ChildFailedError creates a non-daemon C++ thread that keeps the process alive at OS level while Python interpreter stops progressing. MAST reports RUNNING. SJD checks process liveness (not training liveness). Result: mvai_metrics gap for 14h+ before a human notices.
- **Detection gap:** SJD checks if Python process is alive, not if it is training. mvai_metrics staleness check absent from SJD/TMS. D98638473 is the fix — confirm landing/propagation to all affected models. Caveat: archive confirms D98638473 was cited but does not verify the diff landed.
- **Generalization:** Add mvai_metrics staleness check to SJD/TMS monitoring (post author's explicit ask). Propagate D98638473 awareness across OT oncall. If mvai_metrics gap >30min AND MAST RUNNING → treat as P44/CL-012 candidate immediately, not at human-spotted-it time.

---

## 2026-06-03 23:00 PT — Trainer-bound throughput cap → peak-traffic example-age spike (CL-013 #3, no P-row, validator confirmed)

- **Pattern:** MC12 arm3 model 2123944781 showed DPP starvation=0% but training example age >1 hour (target <2min). Root: trainer at 10x8 GPUs can't keep up with peak Scribe data rate. (N=1, post corpus, Jun 3 2026)
- **Surface:** W1336024098492333 (Hao Sha, MC12 arm3 2123944781, 2026-05-27 post; confirmed by Paul Lu + MVAI bot)
- **Root mechanism:** DPP is not starving (0% starvation, trainer-bound). Trainer throughput cap at peak creates Scribe backlog → example age spikes from minutes to >1h during peak periods. CL-013 sub-mechanism #3 (peak capacity shortfall) — no dedicated P-row.
- **Detection gap:** Current triage flow: "example_age high" → check DPP starvation → 0% → confusion. Missing branch: if starvation=0% AND example_age_high → trainer is the bottleneck, not DPP. Oncall frequently investigates DPP when the fix is trainer scaling or data subsampling.
- **Generalization:** Add P-row for CL-013 sub-mechanism #3: "DPP starvation=0% + example_age_high = trainer throughput cap." Mitigation: scale trainers (10x8→16x8/20x8) OR subsample to reduce effective input rate at peak. Paul Lu's diagnosis is the canonical runbook source.

---

## 2026-06-03 23:00 PT — R17 MODEL-ID GUARD creates triage blindspot for plural-title SEVs (N=1, validator partially confirmed)

- **Pattern:** S665478 (Reels LSR MB9 OT jobs hanging, 2026-05-18→2026-06-02, 15 days) was never fully triaged by the bot because plural title + no explicit model IDs → R17 AWAITING_CONTEXT. Bot triage was blocked at time of analysis; the SEV had already run 15 days by the time it was archived. (N=1, sev corpus, Jun 2 2026)
- **Surface:** SEV-S665478-2026-06-02.md (Reels LSR MB9, 15d hang, xingjia / ig_rec_modeling_lsr, OOM + MB9 pool suspected)
- **Root mechanism:** R17 correctly refuses to page on inferred model IDs — the 2026-05-18 S665478 live triage had two concurrent sessions infer different model sets (one nearly recommended kill on a healthy 96-GPU trainer). Guard is correct. Gap: no follow-up mechanism for AWAITING_CONTEXT cases — bot stops, takes no further action.
- **Detection gap:** AWAITING_CONTEXT verdict has no built-in escalation: no auto-task asking owner to add model IDs, no duration threshold (SEV still AWAITING_CONTEXT at 24h → no alert), no cluster-level triage by signal class.
- **Generalization:** For R17 AWAITING_CONTEXT blocks: (1) file a follow-up task tagged to the SEV asking owner to add explicit model IDs; (2) if AWAITING_CONTEXT for >24h → escalate to operator 1:1; (3) triage at signal-class level (OOM/capacity/NCCL) without model_id to provide directional guidance without risky page. Validator note: "for entire duration" claim not directly supported — R17 blocked bot triage at analysis time; 15-day duration was prior to bot triage.

---

## 2026-06-03 23:00 PT — P23/kClientShardSizeThrottled 2nd recurrence in 48h = quota unaddressed (N=1, validator confirmed)

- **Pattern:** Alert A902257976006165 (facebook_reels_vdd_hstu_v0 model 877766818) is a 2nd recurrence of Manifold per-shard size throttle (P23-adjacent) within 48h of first resolution. Prior alert (Jun 2) auto-resolved via v7 restart with no quota action taken. (N=1 new recurrence, alert corpus, Jun 3 2026)
- **Surface:** A902257976006165 (charlesz, facebook_reels_vdd_hstu_v0 877766818, 2026-06-03); prior: high-2026-06-02-A978986517964596 (same model, P23/kClientShardSizeThrottled, auto-resolved)
- **Root mechanism:** Manifold per-shard size quota throttle (kClientShardSizeThrottled) for training model 877766932 FULL_SNAPSHOT checkpoint writes. Auto-recovery (v7/v8 restart) clears the alert temporarily but doesn't increase quota headroom. 48h later, same throttle recurs.
- **Detection gap:** P23 mitigation currently says "wait for auto-recovery." No rule: "≥2 P23 occurrences within 7d → file Manifold quota task; auto-recovery is insufficient." Validator note: correctly labeled "P23-adjacent" — kClientShardSizeThrottled is shard-size quota (distinct from P23's Manifold key collision).
- **Generalization:** Add P23 recurrence rule: if same model fires kClientShardSizeThrottled (or FULL_SNAPSHOT miss with P23 evidence) ≥2× within 7 days → auto-create task to Manifold storage oncall for quota review. Auto-recovery via restart is a bandage; quota increase or checkpoint path migration is the fix. Monitor facebook_reels_vdd_hstu_v0 (877766818 / training 877766932) for 3rd recurrence.

---

## 2026-06-04 23:00 PT — OT zombie job (ChildFailedError + NCCL join block) confirmed 2nd occurrence → P61 added (N=3 archives, 2 distinct SEVs, validator confirmed)

- **Pattern:** light.py main() lacks try/except on ChildFailedError error path; Python shutdown joins NCCL threads stuck waiting for the crashed rank → zombie job, MAST never retries despite 100 retries available. Confirmed in S628346 (prior) and S665454 (2nd occurrence, Threads U2M model 2124793203, 19-day zombie). (N=3 archives: L3-2026-05-16-S665454, L3-2026-06-04-S665454, SEV-S665454-2026-06-04)
- **Surface:** SEV-S665454-2026-06-04.md, L3-2026-06-04-S665454.md, L3-2026-05-16-S665454.md (all S665454 — cross-ref S628346 prior occurrence already in corpus)
- **Root mechanism:** SIGABRT on rank 0 (CUDACachingAllocator free_block assertion) → EA raises ChildFailedError to light.py main() → no try/except → Python threading._shutdown() → NCCL thread join blocks (dead rank gone, survivors waiting) → process zombie → MAST never reads reply file → no new attempt.
- **Detection gap:** SJD checks process liveness (OS-level), not training liveness. Zombie process is alive at OS level → no SJD kill, no NCCL watchdog (Python-side hang). mvai_metrics gap is the earliest signal, found by humans at 14h+ in both occurrences. D107459272 ([MVAI_EXIT] logging) now enables ~1-min disambiguation.
- **Generalization:** P61 added to known-patterns.md. Audit all OT models with base layer upstream < 2026-05-21 (D98638473 landed 2026-05-21) — vulnerable until upgraded. mvai_metrics staleness (>5 min gap + MAST RUNNING) is the mandatory first probe for any "trainer looks stuck" symptom.
- **Diff:** notes commit `224e6bc77826` (P61 added to known-patterns.md)

---

## 2026-06-04 23:00 PT — Self-mitigating SEVs with no documented root cause leave zero learning signal (N=2, SEV corpus, ⚠️ [validator: not checked for this candidate])

- **Pattern:** Two SEVs (S670347, S670795) self-mitigated within 33h and 29h respectively with NO root cause documented in SEV body, GChat, or bot triage record. Bot reached NEEDS_INVESTIGATION for both. Postmortem archives carry empty root_cause fields. (N=2, SEV corpus, Jun 3 2026)
- **Surface:** SEV-S670347-2026-06-03.md (ig_feed_voice_translation_retrieval_snapshot pipeline delay, 33h, self-mitigated), SEV-S670795-2026-06-03.md (low sparse streaming success rate multiple IGR T2I models, 29h, self-mitigated, plural-title → R17 blocked triage)
- **Root mechanism:** Two distinct triage blockers: (1) S670347 — no investigation context in SEV GChat or body at bot-triage time; (2) S670795 — R17 plural-model-title guard refused model IDs → AWAITING_CONTEXT, SEV self-resolved before context arrived. Both closed with zero diagnosis.
- **Detection gap:** No escalation path for self-mitigating SEVs that close as NEEDS_INVESTIGATION. Postmortem cron writes the archive but the knowledge loop ends there — no follow-up task, no owner ask, no postmortem quality metric.
- **Generalization:** For any SEV closing as self-mitigated with NEEDS_INVESTIGATION: (1) auto-file follow-up task asking SEV owner to document root cause; (2) for R17 AWAITING_CONTEXT blocks >24h → escalate to operator 1:1. Track "undocumented self-mitigating SEVs per month" as a postmortem-quality signal.

---

## 2026-06-04 23:00 PT — Transient SPARSE_DELTA/DENSE_DELTA alerts (<15 min, trainer alive) need a fast-path NO_ACTION rule (N=2, alert corpus, ⚠️ [validator: not checked for this candidate])

- **Pattern:** Two alerts (A1840391773334834, A990003026746054) fired for SPARSE_DELTA or SPARSE+DENSE_DELTA publishing instability, auto-cleared within 8 and 13 minutes respectively. Trainer confirmed alive via mvai_metrics throughout. Both classified TRANSIENT_NOISE/NO_ACTION by ot-alert-monitor with no human action. (N=2, alert corpus, Jun 4 2026)
- **Surface:** A1840391773334834 (ESR 2141728943, SPARSE_DELTA, 8 min, Jun 4), A990003026746054 (VDD HSTU 2130289862, SPARSE_DELTA+DENSE_DELTA, 13 min, Jun 4)
- **Root mechanism:** Brief checkpoint publishing delay or GMPP/Hedwig queue transient. Trainer alive throughout; no MAST error; no SEV. Alert fires on SPARSE_DELTA gap > threshold; clears when next delta publishes. First known occurrence for both model IDs.
- **Detection gap:** No P-row fast-path for "trainer alive + SPARSE/DENSE_DELTA + auto-clears < 15 min." Triage time is spent confirming what is already predictable from the duration signature. P57 covers scribe_read_proxy latency during ZippyDB SEV (different detector/mechanism). CL-003 covers scribe lag specifically.
- **Generalization:** Add fast-path rule: trainer alive (mvai_metrics fresh) + alert duration < 15 min + no concurrent MAST FAILED + no upstream infra SEV → TRANSIENT_NOISE/NO_ACTION, skip deep triage. Flag if either model (2141728943, 2130289862) fires again within 7 days — second occurrence converts from one-off to pattern requiring P-row.

---
