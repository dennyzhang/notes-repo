# Mega-Learnings Digest

Weekly curated patterns distilled from postmortem archives (resolved SEVs, posts, alerts).
Each entry = a cluster that crossed multiple incidents or carries novel signal.
Source cron: ot-knowledge-curation (23:00 PT nightly).

---

## 2026-06-17 23:00 PT — P57 multi-model cascade auto-clears while upstream SEV remains In Progress: ACKED_ONLY, not RESOLVED

- **Pattern:** P57 (scribe_read_proxy.client_lag_in_seconds) fired simultaneously on 2 IG models during S669133 ("Tenant root/Facebook/Feed over scribe quota"). Alert A979061878273128 (ig_organic_feed_mtml 878102693, ~28h) and A1572394467643789 (ig_reels_tab_ss_omni_retrieval 2144816217, ~12h) both auto-cleared via AGG rule timeout while S669133 remained In Progress (time_mitigated=empty at 22:05 PT 2026-06-17). (N=2, alerts corpus, CL-003)
- **Surface:** A979061878273128 + A1572394467643789 (2026-06-17)
- **Root mechanism:** S669133 scribe quota exhaustion degraded scribe_read_proxy latency across multiple IG training models simultaneously. OT publish continued normally for both throughout — scribe_read_proxy is a non-gating path. AGG rule timeouts cleared the alerts independently of whether the upstream SEV resolved. Both models remain at risk of re-fire until S669133 closes.
- **Detection gap:** P57 says "auto-resolves when upstream ZippyDB/Scribe SEV mitigates" — but these alerts cleared via AGG timeout BEFORE upstream mitigation. The classification MONITOR is correct; the resolution_signal label should be ACKED_ONLY (not RESOLVED) when the upstream SEV is still In Progress at alert clear time. Without this distinction, the cron's state shows "resolved" while the root cause remains live.
- **Generalization:** Add to P57 resolution guidance: when a P57 alert auto-clears while the upstream SEV remains In Progress (time_mitigated=empty), mark resolution_signal=ACKED_ONLY, not RESOLVED. Re-check the upstream SEV status in the next triage cycle. If S669133 is still active, expect re-fire. Confirm Scribe/ZippyDB oncall has active ownership before treating cascade as closed.
- **Validator:** ✓ confirmed — P57 signal verified in both archives; AGG-timeout clear + S669133 In Progress at clear time confirmed; ACKED_ONLY vs RESOLVED distinction is non-duplicate (P57 row has no resolution_signal guidance for this case).

---
