# Evolve run — 2026-06-12 iter9 (R25 CL-013 trigger + T2 baseline replays)

**Outcome: NO COMMIT. R25 (CL-013 trigger) adversarial ACCEPT + targeted A/B win on Case B, but expected composite gain ≈ 0.006 — below HARD noise band ±0.029. T2-training P44/P61 correctly handled by baseline. Root cause of T2 gap isolated to W1342971631130913 (no-P-row delta_publish_config case).**

---

## Stage sequencing

| Stage | Status |
|---|---|
| 1 (fitness-completion) | ✅ Done |
| 1b (online signal) | ✅ Done |
| 1c (diff-ledger) | ✅ Done |
| 2 (grader trust) | ✅ Done (90% codex agreement) |
| 3 (batch mutation — iter1) | ✅ R19 trigger committed |
| 3 (batch mutation — iter2) | ✅ GRADE schema committed; OwnerRouting v2 REJECTED |
| 3 (batch mutation — iter3–6) | ⚠️ All REJECTED (Dec v3–v7, Own v3–v8) |
| 3 (batch mutation — iter7) | 🚨 ESCALATION: Dec v8 + Own v9 REJECTED; 8+ consecutive — SWITCHED TARGETS |
| 3 (batch mutation — iter8) | 🔍 DIAGNOSTIC: scope-detector hallucination source identified as CL-013 knowledge gap |
| 3 (batch mutation — iter9) | ⚠️ R25 ACCEPT at adversarial, targeted A/B win, but below noise band — NO COMMIT |

---

## Work done this run

### R25 adversarial pre-check — ACCEPT (with warnings)

**Design**: When `signal_class=checkpoint_training_data_age_mins` AND alert staleness ≥10× MAST ODS actual age → THRESHOLD_MISFIT/CL-013 (Scuba formula timezone mismatch in `_resolve_ts_now()`, D75703936). NO_ACTION. Do NOT use generic "stale reference timestamp [INFERRED]".

**Adversarial result: ACCEPT — no fatals.**

Warnings (non-blocking):
1. R23/R24 numbering gap — structural context gap, not a flaw in R25
2. 10× threshold not anchored to unit/source — concrete example (3000 vs 2.5 min) makes this clear in practice
3. "Recurring fire" fallback is too loose (undefined recurrence threshold) — minor; primary trigger is ratio-based
4. NO_ACTION not conditioned on D75703936 fix status — worth noting; add to rule when committed

No conflict with R16, R18–R22.

---

### Targeted A/B — Case B (ALERT-1011200521237714)

Case B input: `checkpoint_training_data_age_mins` alert, ~3000 min staleness, MAST ODS actual ~2.5 min, facebook_reels_vdd_hstu_v0 877766932 RUNNING 11+ days, 2nd recurrence.

| Metric | Baseline (no R25) | Treatment (R25) |
|---|---|---|
| root_cause | DETECTOR_BROKEN / generic (or via archive: CL-013 only if archives loaded) | CL-013 Scuba formula timezone mismatch in `_resolve_ts_now()` (D75703936) ✓ |
| p_row | R16 (partial) / no CL-013 | R25 / CL-013 ✓ |
| verdict | NO_ACTION (direction correct) | NO_ACTION ✓ |
| confidence | 0.92 (via archive) / 0.5 (no archive) | 0.93 ✓ |
| hallucination | Clean | Clean |

**Eval-flow context (critical):** The eval-flow agent reads SKILL+KP+TD only — no incident archives. Without archives, baseline produces "stale reference timestamp [INFERRED]" (confirmed in iter8 replays). Treatment correctly applies CL-013 from R25 alone. Targeted A/B is a clear win.

---

### Noise band gate — FAIL

R25 affects only `checkpoint_training_data_age_mins` cases in the gold set:
- ALERT-1011200521237714 (eligible, R25 directly fixes) — **1 case**
- POST-1321547686606641 (checkpoint_training_data_age_mins, HEURISTIC_RESOLVED verdict) — marginal

Gold set: 75 eligible total. R25 affects at most 2 cases.

**Expected composite gain:** Δcalib ≈ +0.6 per case × 2 cases / 75 × calibration weight (0.35) ≈ **0.006**.  
**Noise band:** ±0.029 (from eval-stats.sh, runs=2).  
**Gate:** 0.006 < 0.029 → **BELOW NOISE BAND → NO COMMIT.**

This is structural: R25 is evidence-based and deterministic (not noise), but the current gold set has too few `checkpoint_training_data_age_mins` cases to give it statistical power.

---

### T2-training baseline replays

**ALERT-1533487445064477 (P44 GIL hang):**
- GT: P44 Python GIL hang, mvai_metrics gap ≥3h, MAST v3 RUNNING 42h no checkpoint progress. REAL_OT_FAILURE. PAGE mingchao.
- Baseline: Correctly applies R17 → P44. root_cause_match: 1, p_row: 1, verdict: 1, owner: 0.5 (needs model-metadata lookup for unixname).
- **Baseline CORRECT** — P44 case is NOT a gap.

**W1347152497379493 (P61 zombie):**
- GT: P61 zombie trainer, CUDACachingAllocator assertion, pre-D98638473 base layer no-clean-exit, NCCL threading._shutdown() block. REAL_OT_FAILURE. Yubo Wang.
- Baseline: Correctly applies P61. root_cause_match: 1, p_row: 1, verdict: 1, owner: 1.
- **Baseline CORRECT** — P61 zombie case is NOT a gap.

**T2-training gap root cause:** The T2 composite being ~0.56 is NOT from P44/P61 failures. The structural gap is **W1342971631130913 (delta_publish_config case — no P-row for duplicate sparse streaming config)**. This case has no existing P-row to apply; the agent must reason from first principles and likely fails. This is a data-side gap (no P-row = harder for the agent) that may require a new P-row (P-row for "duplicate sparse streaming config in trainer_config + delta_publish_config = torch.cat() on empty tensors").

---

## Workflow eval attempt

Launched `Workflow` with `workflow({scriptPath: eval-flow.js})` — failed: `workflow()` requires `{scriptPath:...}` object format, not string. Fell back to Agent fan-out (per task guidance). Full eval skipped based on noise-band analysis (gain << std). Workflow path confirmed unreliable for nested scriptPath invocations; use top-level `Workflow({scriptPath: ...})` in future.

---

## Anti-divergence status

| Target | Iterations | Current status |
|---|---|---|
| Decisiveness | v1→v8 (8 rejections) | STOPPED — switch targets |
| OwnerRouting | v1→v9 (9 rejections) | STOPPED — move to deterministic capability |
| Scope-detector (CL-013) | iter8 diagnostic + iter9 A/B | R25 ACCEPT adversarial, targeted A/B win. Blocked by gold set size (2 affected cases < noise band). |
| T2-training gap | iter9 diagnosis | NOT P44/P61. Root cause: delta_publish_config (W1342971631130913) has no P-row. |

---

## Rejected candidates registry (complete through iter9)

| Candidate | Date | Reason | Status |
|---|---|---|---|
| Citation-discipline guard | 2026-06-11 iter2 | Composite 0.684→0.677 | REJECTED — do not retry |
| Decisiveness v1–v8 | iter1–iter7 | Various — 8 fatals | REJECTED — switched targets |
| OwnerRouting v1–v9 | iter1–iter7 | Various — 9 fatals | REJECTED — move to capability |
| R24 v1 (scope body-anchor) | 2026-06-12 iter8 | Suppresses correct CL-013/R23 P-row details | REJECTED |
| R24 v2 (scope SEV-ID anchor) | 2026-06-12 iter8 | ACCEPTED adversarial but wrong failure mode | NOT COMMITTED — wrong target |
| R25 (CL-013 trigger) | 2026-06-12 iter9 | ACCEPT adversarial, A/B win on Case B, but Δcomposite ≈ 0.006 < noise band ±0.029 | PENDING — hold for gold set expansion |

---

## Next iteration (iter10) plan

**Priority 1 — Gold set expansion for R25 to become statistically significant:**
- ALERT-1011200521237714 has 3 historical fires (2026-05-09, 2026-05-18, 2026-06-12) on the same model
- Add ALERT-1011200521237714 2026-05-09 and 2026-05-18 instances as separate gold-set cases (if not already present)
- Also search resolved-incident archives for OTHER models that hit `checkpoint_training_data_age_mins` false alarms (CL-013 family)
- Target: ≥5 `checkpoint_training_data_age_mins` cases to give R25 enough statistical power (5/75 × 0.6 Δ × 0.35 weight ≈ 0.014 — still < 0.029, so may need ≥10 cases)

**Priority 2 — delta_publish_config P-row for T2 gap:**
- Design a new P-row for "duplicate sparse streaming config (trainer_config + delta_publish_config) → torch.cat() on empty tensor list"
- Source: W1342971631130913 (ig_textpost_feed_esr, D107578770 fix)
- This is a genuine knowledge gap; adding it as P-row + gold-set case would directly improve T2-training score
- Requires: 3-source threshold (currently 1 source). Defer to knowledge-distillation unless other instances found.

**Priority 3 — Commit R25 once gold set expanded:**
- If iter10 adds ≥3 more `checkpoint_training_data_age_mins` cases, run A/B again
- Expected composite gain with 5 cases: ~0.014 (still below noise band but directional)
- With 10 cases: ~0.028 (at noise band threshold)
- Alternative: accept targeted evidence as sufficient per iter-by-iter precedent (R19 iter1 had delta=0.00 on all test cases but was committed for logical consistency)
