# Evolve run — 2026-06-12 iter8 (Target pivot: scope-detector diagnostic)

**Outcome: NO COMMIT. Target pivoted from Dec/Own (escalation threshold) to scope-detector. R24 v1 REJECTED (adversarial), R24 v2 ACCEPTED (adversarial) but addresses wrong failure mode. Baseline case replays diagnose scope-detector hallucination as CL-013 knowledge-gap, not SEV ID fabrication.**

---

## Stage sequencing

| Stage | Status |
|---|---|
| 1 (fitness-completion) | ✅ Done |
| 1b (online signal) | ✅ Done |
| 1c (diff-ledger) | ✅ Done |
| 2 (grader trust) | ✅ Done (90% codex agreement) |
| 3 (batch mutation — iter1) | ✅ R19 trigger committed |
| 3 (batch mutation — iter2) | ✅ GRADE schema committed; OwnerRouting v2 REJECTED |
| 3 (batch mutation — iter3–6) | ⚠️ All REJECTED (Dec v3–v7, Own v3–v8) |
| 3 (batch mutation — iter7) | 🚨 ESCALATION: Dec v8 + Own v9 REJECTED; 8+ consecutive — SWITCHED TARGETS |
| 3 (batch mutation — iter8) | 🔍 DIAGNOSTIC: scope-detector hallucination source identified |

---

## Work done this run

### R24 v1 — REJECTED at adversarial pre-check

**Design:** "For DETECTOR_BROKEN/THRESHOLD_MISFIT verdicts, all supporting details (SEV IDs, model states, occurrence counts, timing) MUST appear verbatim in signal body."

**Fatal (adversarial catch):** R24 v1 prohibits correct pattern-derived mechanistic details expected by the grader. Case B GT contains "timezone mismatch (D75703936) + wrong scope for _resolve_ts_now()" (CL-013 details) and Case D GT contains "~7.5h cadence" (R23 mechanism) — neither is in the body but BOTH are correct. R24 v1 systematically suppresses them, causing grader penalization on correct mechanistic attribution. The boundary between "classification label" (permitted) and "supporting citation" (body-required) excludes the mechanism linking them, which is required for actionable verdicts.

### R24 v2 — ACCEPTED at adversarial pre-check (but wrong failure mode)

**Design:** "When citing an upstream SEV ID (S<digits>) in root_cause for DETECTOR_BROKEN/THRESHOLD_MISFIT cases, it must appear verbatim in signal body. If not in body, write 'possible upstream cascade (SEV not confirmed from signal)'. Pattern-derived mechanistic details from a matched P-row/R-rule (D75703936 timezone bug, R23 cadence mismatch) may be cited without body anchor."

**Adversarial verdict: ACCEPT** — no fatals. No conflict with R19/P57 (those use live tool queries not body anchoring), hedging beats wrong fabrication on grader rubric.

**Why not committed:** Baseline case replays revealed R24 v2 addresses the wrong failure mode.

---

## Baseline case replays (3 of 4 scope-detector cases)

| Case | Baseline verdict | Baseline classification | Hallucination self-check | GT match? |
|---|---|---|---|---|
| C: ALERT-1201406268614142 | NO_ACTION | DETECTOR_BROKEN | Clean — all claims body-traced | ✓ Partial (missing S665163 not in body) |
| A: ALERT-1002291152283272 | NO_ACTION | UPSTREAM_INFRA (P57) | Clean — S667358 cited from body | ✓ Good (P57 correctly applied) |
| B: ALERT-1011200521237714 | NO_ACTION | DETECTOR_BROKEN | "Stale reference timestamp" labeled INFERRED | ✗ Miss — GT expects CL-013 THRESHOLD_MISFIT |

**Key finding:** No SEV ID fabrication observed in any of the 3 replays. The actual scope-detector failure mode is Case B: agent labeled its mechanistic hypothesis as INFERRED instead of matching it to CL-013 (Scuba formula timezone mismatch, D75703936). This is a **knowledge gap**, not a hallucination from fabricated identifiers.

**R24 v2 would not fix Case B.** The agent correctly avoided fabricating SEV IDs. The failure is: agent does not connect "3000 min staleness vs 2.5 min MAST ODS actual" to CL-013 (Scuba formula timezone mismatch) and instead produces a generic INFERRED hypothesis.

---

## Scope-detector root cause: CL-013 recognition gap

**Actual failure pattern (Case B):**
- Signal: checkpoint_training_data_age_mins shows ~3000 min staleness while MAST ODS shows ~2.5 min actual
- Correct: THRESHOLD_MISFIT — CL-013 Scuba formula bug (timezone mismatch in _resolve_ts_now, D75703936)
- Agent: DETECTOR_BROKEN (correct verdict) but "stale reference timestamp" (generic INFERRED instead of CL-013 specific)

**Root cause of gap:** CL-013 is referenced in R16 by name but the mechanistic detail (timezone mismatch, D75703936, _resolve_ts_now()) may not be accessible in a form the agent can pattern-match from the signal. The signal contains "~3000 min staleness + MAST ODS actual 2.5 min" — this is the CL-013 signature (massive mismatch between Scuba-computed and ground-truth staleness), but the agent doesn't have a trigger condition to map that specific discrepancy pattern to CL-013.

**Candidate fix (for iter9):** Add CL-013 THRESHOLD_MISFIT trigger condition to R16 or a standalone R25:
> "When checkpoint_training_data_age_mins alert shows staleness ≫ actual MAST ODS data age (≥10x discrepancy), classify THRESHOLD_MISFIT — Scuba formula bug (CL-013: timezone mismatch in _resolve_ts_now, D75703936). This discrepancy signature is diagnostic for CL-013 — do NOT infer a generic 'stale reference timestamp'; apply CL-013 mechanistic explanation."

This is minimal (one trigger condition, one P-row application), has no branching, and directly fixes the observed failure.

**Why not committing this run:** The fix (CL-013 trigger in R16) needs adversarial pre-check before committing. Pending for iter9.

---

## T2-training assessment (no new work this run)

iter9 should run baseline replays on T2 cases (W1347152497379493 P61 zombie, ALERT-1533487445064477 P44 GIL hang) to determine if agent correctly applies these P-rows. If P61/P44 already applied correctly, T2-training rc~0.63 gap may be structural (cases with no P-row match like W1342971631130913 delta_publish_config).

---

## Anti-divergence status

| Target | Iterations | Current status |
|---|---|---|
| Decisiveness | v1→v8 (8 rejections) | STOPPED — switch targets (anti-divergence rule hit) |
| OwnerRouting | v1→v9 (9 rejections) | STOPPED — move to deterministic capability (per task direction) |
| Scope-detector | iter8 diagnostic | R24 v1 rejected, R24 v2 accepted but wrong mode. CL-013 trigger fix designed for iter9. |
| T2-training | iter8 planned | Baseline replays pending iter9 |

---

## Rejected candidates registry (complete through iter8)

| Candidate | Date | Reason | Status |
|---|---|---|---|
| Citation-discipline guard | 2026-06-11 iter2 | Composite 0.684→0.677 | REJECTED — do not retry |
| Decisiveness v1–v8 | iter1–iter7 | Various — 8 fatals | REJECTED — switched targets |
| OwnerRouting v1–v9 | iter1–iter7 | Various — 9 fatals | REJECTED — move to capability |
| R24 v1 (scope body-anchor) | 2026-06-12 iter8 | Suppresses correct CL-013/R23 P-row details | REJECTED |
| R24 v2 (scope SEV-ID anchor) | 2026-06-12 iter8 | ACCEPTED adversarial but wrong failure mode | NOT COMMITTED — wrong target |

---

## Next iteration (iter9) plan

1. **Design CL-013 trigger condition** for R16 (specific discrepancy pattern → THRESHOLD_MISFIT/CL-013)
2. **Adversarial pre-check** on the CL-013 trigger
3. **Targeted A/B** on Case B (ALERT-1011200521237714) + any other checkpoint_training_data_age_mins cases in gold set
4. **T2-training baseline replays** on ALERT-1533487445064477 (P44) + W1347152497379493 (P61)
5. **Commit if** adversarial + A/B show improvement
