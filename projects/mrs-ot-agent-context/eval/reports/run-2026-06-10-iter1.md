# Evolve run — 2026-06-10 iter1 (FIRST real improvement)

**composite 0.576 → 0.684 (+0.108)** — driven entirely by detection_recall.

## What landed
- **R22** added to `triage-discipline.md` (live-read; changes triage next run).
  "ENGAGE OT failure-symptom classes regardless of alarm status."
- detection_recall **0.423 → 0.962** (+0.539); scoping_precision **held at 1.0**, zero new leaks.

## Stage 2 — grader trust (codex co-grader)
- codex headless works. n=8, agreement 0.75 (6/8), mean_claude=mean_codex=0.875.
- **0/8 cases where codex inflated a wrong answer to MATCH** → grader is not a rubber-stamp (failed a clearly-wrong candidate, credited mechanism-level matches). Trust: OK to mutate.
- caveat: n=8, single run, gold-set has no raw inputs (triage worked from IDs).

## Stage 3 — A/B (4 candidates × 50-case scoping corpus, objective labels)
| cand | rule | recall | precision | new_leaks | winner |
|---|---|---|---|---|---|
| R-DR1 | broad umbrella (all families) | **0.962** | 1.0 | 0 | ✅ committed as R22 |
| R-DR2 | liveness-halt family | 0.885 | 1.0 | 0 | ✅ (subsumed by R22) |
| R-DR3 | snapshot/staleness family | 0.885 | 1.0 | 0 | ✅ (subsumed) |
| R-DR4 | streaming/publish family | 0.846 | 1.0 | 0 | ✅ (subsumed) |

All 4 won; committed R-DR1 (umbrella, covers all families) with a downstream-deference clause (engage = admit to triage; R16/R18/R19 still apply) to bound over-engagement.

## Why this is trustworthy (red-team)
- A/B is on **objective engage/drop labels**, not graded judgment → independent of grader subjectivity.
- precision held on the **hard near-miss negatives** (Ads/DPA SEVs with OT keywords) → org/title hard-exclude still fires; not naive "engage all".

## Open caveat → next Stage-1 item
- precision verified on **out-of-org negatives only**. R22 is the rule most likely to leak on **in-MRS-non-OT** negatives (e.g. serving SEVs with "model stale"). Add that negative class to the scoping corpus and re-validate R22's precision before trusting the 0.684 fully.

## Remaining targets (next iterations)
- owner 0.25 (routing) · 30% R-rule confabulation (citation guard) · calibration 0.64 · temporal hold-out for true generalization.
