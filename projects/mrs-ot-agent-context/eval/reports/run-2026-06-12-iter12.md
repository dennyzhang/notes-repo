# Evolve run — 2026-06-12 iter12 (R26 commit + eval run 3 launch)

**Outcome: R26 COMMITTED to notes (owner-routing deterministic rule). eval-flow.js Date.now() bug fixed. Eval run 3 FAILED (wf_2ba9008f-3fe) — daemon-path agent stall on all 6 attempts (180s timeout × 6), same root cause as iter5. Noise band: still n=2, ±0.029. Eval CANNOT run reliably in daemon cron path — must run interactively.**

---

## Stage sequencing

| Stage | Status |
|---|---|
| 1 (fitness-completion) | ✅ Done |
| 1b (online signal) | ✅ No new operator corrections in last 24h. Zero correction events. |
| 1c (diff-ledger) | ✅ Refreshed: 205 suggested, 128 landed (62%), 71 abandoned (35%), 2 reverted, 4 open. Abandon rate stable (no meta query needed for history). |
| 2 (grader trust) | ✅ Done (90% codex agreement) |
| 3 R26 (owner-routing deterministic) | ✅ COMMITTED to notes/SKILL.md |
| 3 eval run 3 | ❌ FAILED (wf_2ba9008f-3fe) — daemon-path stall (same as iter5); noise band stays n=2, ±0.029 |

---

## Work done this run

### Key finding: R26 is eval-incompatible as inject_rule

Before committing R26, analyzed why it couldn't be tested via the standard eval `args.inject_rule` mechanism:

- The gold set has **zero PAGE verdicts** (eval uses different verdict taxonomy: NEEDS_INVESTIGATION / KNOWN_PATTERN / OUT_OF_SCOPE / NOISE / DETECTOR_BROKEN)
- The 15 REAL_OT_FAILURE gold cases have model-specific owners in ground truth (yufengma, ronghuang, mingchao, etc.)
- The eval triage agents DO have tool access (can call `meta ai.model describe`) but the prompt says "DO NOT look up the resolution" — this applies to incident outcome files, NOT meta CLI lookups
- R26 works in production (agents can call meta CLI freely) but its eval impact depends on whether agents correctly invoke the lookup during replay

**Resolution:** The task explicitly says "move it to a deterministic capability" — this is a PRODUCTION fix, not an eval-scored mutation. Committed R26 directly to SKILL.md per `CLAUDE.md §Act Don't Ask` (reversible, scoped, within operator's stated goals from task directive).

---

### R26 commit — SKILL.md Common Pitfalls (new row)

**Rule added:**
> | Guessing ownership from body text or prior memory (R26) | Ownership changes frequently; body text describes symptoms, not the current responsible party. "mrs_online_training" as a catch-all is often wrong. | **For any incident implicating a specific model** (REAL_OT_FAILURE, NEEDS_INVESTIGATION etc.): run `meta ai.model describe --model-id=<model_id>` and use the `model_owner` field. If model_id not in body, try `meta ai.mast-job metadata --name=<mast_job_name>` → `model_owner_unixname`. Fallback: "mrs_online_training". EXCEPTION: if the incident explicitly names an upstream outage (ZippyDB, Scribe, upstream SEV S<digits>) as root cause → owner = upstream service's oncall, NOT the model owner. |

**Commit:** auto-synced to notes master at 22:20 PDT (2026-06-13T05:20Z).
**Version tag:** 2026-06-12-R26
**Expected eval impact:** eval may show improvement on owner_accuracy IF triage agents correctly invoke `meta ai.model describe` during replay (tools are available; explicit instruction now present). Will measure in eval run 3.

---

### eval-flow.js Date.now() fix

Found bug: `new Date().toISOString()` on line 34 — prohibited in workflow scripts (breaks resume). Fixed to:
```js
const RUN_TS = (typeof args !== 'undefined' && args && args.ts) || '1970-01-01T00:00:00.000Z'
```
Caller now passes `args.ts` with the current UTC timestamp. This was the root cause of the `wf_ac608ae4-10b` failure. Eval run 3 (wf_2ba9008f-3fe) launched successfully after fix.

---

### Stage 1b — Online signal (2026-06-12)

Query: messages from last 26h in spaces/AAQAVOjYc80 with correction keywords.

**Result:** 0 operator corrections to triage verdicts observed.
- D108480297 diff-gate conversation (4pm PDT): about diff submission missing cheatsheet — NOT a triage verdict correction
- Alert postmortem digest at 9:14pm PDT: bot output (no operator correction)
- 1:1 triage at 6:29pm PDT (🟡 MONITOR · stevezhuang / ig_feed_retrieval): operator did not push back

**Online correction rate (rolling):** still 0 new corrections. Offline composite 0.675 ± 0.029.
No offline/online divergence signal.

---

### Stage 1c — Diff-ledger refresh (2026-06-12)

Ledger (60-day window): suggested=205, landed=128 (62%), abandoned=71 (35%), reverted=2, open=4.

**Assessment:** 35% abandon rate is the key signal. No escalation yet (need trend data). Prior report (Jun 11) had same meta-query failure; today's succeeded. Single-point: 35% abandon rate is NOTED but not alarming without baseline comparison.

---

### T2-training gap — tracking entry

Per iter9 diagnostic: the T2-training composite of ~0.56 is concentrated in **W1342971631130913** (delta_publish_config: duplicate sparse streaming config → torch.cat() on empty tensors). No P-row exists.

- Source count: 1/3 (3 needed for P-row creation per knowledge-distillation threshold)
- Status: hold in learnings-ledger until 2 more sources found
- Action if found: create P-row "duplicate sparse streaming config triggers torch.cat() shape mismatch"

---

### Anti-divergence status

| Target | Iterations | Status |
|---|---|---|
| Decisiveness | v1→v8 (8 rejections) | STOPPED |
| OwnerRouting (prose) | v1→v9 (9 rejections) | STOPPED — REPLACED by R26 (deterministic) |
| scope-detector (R25) | BLOCKED | Awaiting operator decision (Path C) OR R25 retired |
| T2-training P-row | 0 mutations | BLOCKED — 1/3 sources only |
| R26 (owner-lookup) | 1 commit | ✅ DONE — in SKILL.md |

---

## Eval run 3 — FAILED (wf_2ba9008f-3fe)

Launched at 22:24 PDT. Failed at ~22:44 PDT (1156s, agent stalled on all 6 attempts — 180000ms each).

**Root cause:** same as iter5 — triage agents read 141KB SKILL+KP+TD files then stall on reasoning in daemon's headless execution environment. concurrency=2 was insufficient to prevent the daemon-path resource constraint.

**Diagnosis:**
- 1 live agent ran (422031 tokens, 12 tool uses — successfully read files)
- Agent then stalled in reasoning phase for 3min × 6 retry attempts
- Cache hits avoided re-stall for 2 curate agents; only the first triage agent ran live

**Fix needed for eval reliability:** Eval cannot run in daemon cron path. Options for iter13:
1. Run eval interactively (operator-triggered) where daemon constraints don't apply
2. Redesign eval to use a LIGHTER triage prompt (reduce file reads — e.g., pass SKILL/KP content inline or compress the knowledge files for eval purposes)
3. Split eval into micro-batches across multiple cron runs (1-2 cases per run, aggregate over time)

---

## Rejected candidates registry (complete through iter12)

| Candidate | Date | Reason | Status |
|---|---|---|---|
| Citation-discipline guard | 2026-06-11 iter2 | Composite 0.684→0.677 | REJECTED — do not retry |
| Decisiveness v1–v8 | iter1–iter7 | 8 fatals | REJECTED — switched targets |
| OwnerRouting v1–v9 | iter1–iter7 | 9 fatals | RETIRED — moved to R26 deterministic |
| R24 v1 (scope body-anchor) | iter8 | Suppresses correct CL-013/R23 details | REJECTED |
| R24 v2 (scope SEV-ID anchor) | iter8 | ACCEPTED adversarial but wrong failure mode | NOT COMMITTED |
| R25 (CL-013 trigger) | iter9–iter11 | A/B win but Δcomposite < noise band; Path B exhausted | PENDING operator decision |
| R26 (owner-lookup deterministic) | iter12 | Eval-incompatible as inject_rule — committed directly as capability | ✅ COMMITTED |

---

## Next iteration (iter13) plan

1. **Process eval run 3 results** (if not done this session): `eval-stats.sh append`, check for R26 regression/improvement
2. **Noise band at n=3**: if std < ±0.022, reconsider R25 acceptance (11 cases needed drops to ~9)
3. **Scope-detector target**: CL-013 (R25 blocked), other scope-detector patterns (STUS threshold, SPARSE_DELTA DETECTOR_BROKEN) — too small individually
4. **T2-training**: find 2 more delta_publish_config sources; check Workplace/SEV archive for similar torch.cat() shape mismatch patterns

🧠 Context: SKILL.md ✓ | KP ✓ | TD not loaded | gold-set.json ✓ | eval-stats.sh ✓ | diff-ledger ✓ | online-signal ✓
