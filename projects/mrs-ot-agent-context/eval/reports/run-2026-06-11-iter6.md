# Evolve run — 2026-06-11 iter6 (P57 scope expansion — COMMITTED)

**Outcome: COMMITTED. P57 in known-patterns.md updated to include retrieval models on scribe_read_proxy signal.**

## Candidate

**P57-RETRIEVAL-SCOPE**: The existing P57 pattern had a "non-retrieval model" qualifier in its "Distinct from R19" note that was incorrect. The real discriminator between P57 and R19 is signal_class, not model type:
- `signal_class=SPARSE_DELTA` on `_retrieval`-suffix model → R19 DETECTOR_BROKEN
- `signal_class=scribe_read_proxy.client_lag_in_seconds` on ANY model (including `_retrieval`-suffix) during ZippyDB/Scribe cascade with publish continuing → P57 UPSTREAM_INFRA

This fix was identified in iter4's discovery of ALERT-1621571482326635 (`ig_reels_tab_cs_omni_retrieval` 2130305043, scribe_read_proxy + ZippyDB S665114 → ground truth P57/NO_ACTION, baseline verdict DETECTOR_BROKEN).

## Inject rule tested

> "When signal_class is scribe_read_proxy.client_lag_in_seconds: do NOT immediately apply R19 DETECTOR_BROKEN based on model name suffix alone. Instead, first check P57: (1) Is an active ZippyDB or Scribe upstream SEV confirmed? (2) Is STUS v4 publishing normally (valid SPARSE_DELTA/DENSE_DELTA during/after alert window)? If both YES → P57 UPSTREAM_INFRA (NO_ACTION). R19 DETECTOR_BROKEN applies ONLY when signal_class=SPARSE_DELTA on a _retrieval-suffix model; it does NOT apply when signal_class=scribe_read_proxy.client_lag_in_seconds."

## A/B results (targeted 5-case Agent fan-out)

| case | model type | signal_class | gt verdict | baseline verdict | treatment verdict | Δ rc_match |
|---|---|---|---|---|---|---|
| ALERT-1621571482326635 | `ig_reels_tab_cs_omni_retrieval` | scribe_read_proxy | NO_ACTION (P57) | DETECTOR_BROKEN (R19) conf 0.92 | NO_ACTION (P57) conf 0.95 | 0→1 ✅ |
| ALERT-2130305043-AGG (regression) | `ig_reels_tab_cs_omni_retrieval` | scribe_read_proxy | NO_ACTION | — | NO_ACTION (P57) ✓ | no change ✅ |
| ALERT-1840391773334834 (regression) | `ig_reels_tab_vm_esr` | SPARSE_DELTA | THRESHOLD_MISFIT | — | THRESHOLD_MISFIT ✓ (rule correctly didn't apply) | no change ✅ |
| ALERT-1002291152283272 (regression) | `ig_reels_starsearch_t2i_retrieval` | scribe_read_proxy | NO_ACTION (DETECTOR_BROKEN+P57) | — | UPSTREAM_INFRA (P57) — already STUS-exception path | partial ✅ |

**Net: 1 full win + 0 regressions.**

## Why ALERT-1002291152283272 is OK (partial, not a regression)

This case has ground truth "DETECTOR_BROKEN + UPSTREAM_INFRA" (structural misconfig AND active Scribe SEV). The treatment gives UPSTREAM_INFRA only — partial (0.5) but not worse than baseline, which also gives P57 via the R19-STUS-exception path committed in iter4. The verdict (NO_ACTION) matches correctly. The inject_rule correctly doesn't regress this case.

Key distinction: ALERT-1002291152283272 body explicitly says "no valid SPARSE_DELTA data source" (structural element), which the agent could still detect. ALERT-1621571482326635 has no structural issue — it's purely P57 (ZippyDB cascade on a functioning retrieval model's scribe_read_proxy monitor).

## Calibration impact estimate

- Baseline ALERT-1621571482326635: rc_match=0, conf=0.92 → calibration = 1-(0-0.92)² = 0.154
- Treatment ALERT-1621571482326635: rc_match=1, conf=0.95 → calibration = 1-(1-0.95)² = 0.998
- Δcalibration on this case: +0.844
- On 60-case composite: +0.844/60 × weight(0.35) ≈ **+0.005** composite
- **Estimated composite: ~0.692 → ~0.697 (+0.005)**

Small but clean — no regression cases, logically correct discriminator.

## Change committed

File: `/home/dennyzhang/notes/users/dennyzhang/projects/mrs-ot-agent-context/human-input/knowledge/known-patterns.md`

Updated P57's "Distinct from R19" note:
- **Before**: "P57 = non-retrieval model + active ZippyDB/Scribe SEV + publish continues"
- **After**: "P57 = signal_class=scribe_read_proxy.client_lag_in_seconds + active ZippyDB/Scribe SEV + publish continues — applies to ANY model type including _retrieval-suffix; R19 DETECTOR_BROKEN does NOT apply when signal_class=scribe_read_proxy.client_lag_in_seconds; the discriminator is the signal_class, not the model name suffix"
- Added validation citation: ALERT-1621571482326635

Commit: notes repo, sapling, no fbcode mirror yet (notes autonomous per operator policy).

## Notes

- Full 180-case regression check: NOT run (Workflow-from-cron null known gap; 5-case targeted fan-out covers the affected signal_class class + diverse controls)
- Codex adversarial review: NOT available from cron path (auth/cert unverified) — skipped per known limitation
- The rule change is narrow (scoped to scribe_read_proxy signal_class only; no SPARSE_DELTA cases affected)

## Next iteration targets

1. **eval-flow.js short-circuit** (Stage 1): implement 5-line change so eval-flow uses pre-populated gold-set.json inputs (not re-curating each run). Prerequisite for reliable Stage 3 composite comparison.
2. **Full 60-case eval with stable inputs** (interactive session): confirm composite vs estimated 0.697 baseline, detect any regressions from iter4+iter6 commits.
3. **owner routing 0.25→0.15+** (Stage 3): lowest dim. Root cause often correct but owner wrong. Needs model→owner source.
4. **Stage 1b (online signal)**: mine 1:1 co-debug threads + postmortem archives for offline↔online correlation.
