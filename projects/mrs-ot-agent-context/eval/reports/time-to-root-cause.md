# Time-to-root-cause — metric #1 (2026-03-16 → 2026-06-14, 90d)

_Generated by `tools/time-to-root-cause.sh` from the `triage_events` substrate. Elapsed from first-notified (`ts_notified`) to root-cause-identified (`ts_root_cause`). The headline cut is **recurring / known-pattern** hits (signal cites a P-row/R-rule) — those must reach root cause in **≤5 min** via P-row lookup (Problem #1).

> **Wiring status:** `ts_root_cause` is captured via `record-triage-event.sh --root-cause-at`. To fully wire this metric the three monitor crons (`ot-sev-monitor`, `ot-alert-monitor`, `ot-post-monitor`) must pass `--root-cause-at "$(date '+%F %T')"` at the moment they reach a confident verdict / P-row match. (Not edited in this pass.)

## Metric

- triage events in window: **190** · with root-cause timestamp: **18** · known-pattern (recurring) hits: **49**

### Recurring / known-pattern hits (the ≤5-min target population)

- n = **8**
- median = **0.0 min** · p90 = **0.0 min**
- within ≤5-min bar = **8/8 (100%)**

### All measured triages

- n = **18**
- median = **0.0 min** · p90 = **0.0 min**
- within ≤5-min bar = **17/18 (94%)**

**Verdict (known-pattern ≤5-min bar): PASS**

