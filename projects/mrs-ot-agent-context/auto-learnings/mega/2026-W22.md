# Mega Learnings — 2026-W22 (May 18–25)

Auto-generated by `ot-knowledge-curation` cron. Validator unavailable (cron context limitation). All entries marked ⚠️.

---

## 2026-05-28 23:00 PT — Persistent upstream Scribe/ZippyDB SEVs drive repeated OT false alerts with no escalation gate (N=4)

⚠️ [validator unavailable]

- **Pattern:** Four alerts today (A938817701854850, A1020303910488347, A2449443538836650, A1002291152283272) all traced to the same long-running upstream SEVs — S667358 (IG T20 H100 Scribe, In Progress since 2026-05-22=6d), S659235 (IG Feed scribe, >23d), S654082 (ZippyDB AI training, >35d), S668542 (Feed scribe quota). Bot correctly classified each as CL-003/P57 MONITOR but has no gate that escalates when the upstream SEV is persistently old. (N=4, alert corpus)
- **Surface:** A938817701854850, A1020303910488347, A2449443538836650, A1002291152283272 — all 2026-05-28 alerts
- **Root mechanism:** Upstream Scribe/ZippyDB SEVs stay In Progress for weeks while OT monitors keep re-triaging the same downstream false alerts from the same root cause each day. Each triage correctly lands MONITOR but the loop never closes — no rule triggers escalation of the upstream SEV when it ages past a threshold.
- **Detection gap:** CL-003/P57 triage procedure has no step that checks upstream_sev_age. A 35-day-old SEV generating daily OT false alerts should produce an escalation recommendation, not just another MONITOR verdict. The gap is in the triage flowchart, not the classification logic.
- **Generalization:** When upstream infra SEV driving P57/CL-003 OT false alerts has been In Progress >7 days AND same-model alert fires again: shift verdict from MONITOR to ESCALATE_UPSTREAM — surface the SEV age and route to scribe/ZippyDB oncall for expedited resolution. Candidate new R-rule.

---

## 2026-05-28 23:00 PT — scribe_read_proxy P57 false-alarm cluster reinforced across 5 IG models (N=5, 49-day holdout alert)

⚠️ [validator unavailable]

- **Pattern:** Five alerts today on 5 distinct IG models (ig_reels_starsearch_t2i, ig_reels_tab_mtml, ig_organic_feed_mtml, ig_reels_tab_ss_omni, ig_reels_tab_cs_omni) all classified P57 scribe_read_proxy latency false alarm. One alert (A950082977383608 on model 2132766001) had been open 49 days with a 36% chronic noise rate. Threshold tuning has been "in flight" for weeks but not landed. (N=5, alert corpus)
- **Surface:** A1574093140800697, A950082977383608, A1020303910488347, A2449443538836650, A1002291152283272
- **Root mechanism:** scribe_read_proxy.client_lag_in_seconds alert threshold is too tight for holdout-class models and fires during any ZippyDB/Scribe instability window regardless of OT training pipeline health. OT publish continues normally throughout — the alert monitors a non-gating path.
- **Detection gap:** No escalation path from P57 3rd+ recurrence to "alert needs threshold tuning task". Bot correctly classifies each instance as THRESHOLD_MISFIT/CL-003 but never auto-drafts the threshold-tuning follow-up task.
- **Generalization:** P57 match on 3rd+ recurrence for the same model/detector should auto-draft a task to the detector owner (yiqliu per A950082977383608) requesting threshold tuning. Standing MONITOR alone is insufficient when alert opens for 49 days.

---

## 2026-05-28 23:00 PT — BOOST_PP_LIMIT_SEQ=256 overflow blocks aarch64 fbpkg conveyor (N=1 novel)

⚠️ [validator unavailable]

- **Pattern:** S668720 (2026-05-27): D104413329 added the 257th entry to `__DISABLED_FEATURE_SWITCHES` in AgentFeatureSwitch.h → BOOST_PP_LIMIT_SEQ=256 exceeded → BOOST_PP_SEQ_FOR_EACH corrupts macro expansion on aarch64 strict mode → mvai/light_cli fbpkg blocked → no trunk releases for 3.3h. Fixed via D106439798 (trevormathisen). Note: x86 currently has 1 slot remaining before same overflow. (N=1, sev corpus)
- **Surface:** S668720 (2026-05-27 14:03–17:22 PT), model mvai/light_cli conveyor
- **Root mechanism:** BOOST_PP preprocessor macros have a hard sequence count limit of 256. No lint or CI guard counted entries in `__DISABLED_FEATURE_SWITCHES` before commit. aarch64 strict-mode compilation exposes the corruption that x86 may silently tolerate.
- **Detection gap:** No presubmit check counts entries in macro-expanded sequence lists. The limit was unknown until it broke. "1 slot remaining on x86" is a live impending recurrence with no alerting.
- **Generalization:** Preprocessor macro count limits (BOOST_PP, X-macros, enum lists) need automated CI guards that warn when approaching the limit. Distinct from P41 (fbpkg version-slot exhaustion) and P52 (TorchScript annotation mismatch). Candidate new P-row in class mvai_publish_pipeline.

---

## 2026-05-28 23:00 PT — TMS auto-restart bootstrap stall causes transient delta gap (N=1, pattern reinforcement)

⚠️ [validator unavailable]

- **Pattern:** ALERT-1473457624222843 (2026-05-26): facebook_reels_ifu_mtml_v0 (model 883552231) — TMS auto-restart at 04:30 PDT caused ~30-min bootstrap stall; trainer produced 1–2 snapshots then stalled briefly; both SPARSE_DELTA and DENSE_DELTA resumed valid by 05:47 PT; total gap ~75 min. Auto-resolved with no operator action. (N=1, alert corpus)
- **Surface:** ALERT-1473457624222843 + sibling alert 1799789414004939 (same model, baseline + holdout)
- **Root mechanism:** TMS restart triggers a new trainer process boot; during bootstrap (model load, checkpoint restore, DPP warm-up, first-cycle setup), the trainer is not yet producing snapshots. Detectors fire because the gap exceeds their threshold, even though recovery is guaranteed.
- **Detection gap:** TMS restart events are not correlated with alert fire timestamps. If alert.fire_time is within 2× expected_bootstrap_window of the last TMS restart_time for that job, it should be auto-classified as TRANSIENT_BOOTSTRAP and suppressed.
- **Generalization:** Add TMS-restart correlation step to alert triage: check `meta ai.mast-job metadata --name=mvai-training-online-<id>` for last restart timestamp; if alert.fire_time < restart_time + 2h → TRANSIENT_BOOTSTRAP, monitor only. Reduces false-positive alert burden during routine TMS operations.

---

## 2026-05-28 23:00 PT — DPP xregion enforcement + empty tetrisArgs → Hive colocation failure (N=1 novel, first-time)

⚠️ [validator unavailable]

- **Pattern:** S657606 (2026-04-30 → 2026-05-14, 14 days): ig_textpost_feed_u2m_retrieval (model 2124793203) was on the DPP xregion enforcement online allowlist AND misreporting `table_name_in_tetris_args` as empty → scheduler placed job on hardware across regions without required Hive data → training blocked. Mitigation was a job-level workaround; D103756077 (remove from allowlist) was attempted as dry-run then abandoned. (N=1, sev corpus)
- **Surface:** S657606 (2026-04-30, L4, 14d to mitigate)
- **Root mechanism:** Two compounding gaps: (1) xregion allowlist let DPP skip colocation enforcement; (2) empty tetrisArgs gave scheduler no colocation signal anyway. Either gap alone might be tolerated; together they caused silent scheduling to non-collocated hardware where the required Hive table was absent.
- **Detection gap:** No automatic check scans OT jobs for the (xregion_allowlist=true AND tetrisArgs_empty) compound condition. The condition creates silent misconfiguration only surfacing when scheduling happens cross-region.
- **Generalization:** New pattern candidate (P58 / Hive-xregion-enforcement): flag OT jobs that have `table_name_in_tetris_args` empty AND are on the DPP xregion enforcement allowlist for DPP team audit. Source: S657606 (first known instance on ig_textpost_feed_u2m family). Distinct from P30 (DPP pkg version change) and P16 (DPP starvation).

---

## 2026-05-27 23:00 PT — CONVEYOR_REGRESSION: trunk library API/behavior advance breaks cached compiled artifact (N=2)

⚠️ [validator unavailable]

- **Pattern:** Two same-day SEVs (S668029, S668033) share the same root mechanism: an upstream library or framework changes its API or internal behavior on trunk, while a cached compiled artifact (Rust binary, IEN lowering package) was built against the old API/behavior — artifact becomes incompatible → CONVEYOR_REGRESSION class fires. Fix paths differ by artifact type: patch call sites (Rust, S668029) or rebuild package against new trunk (MI300x IEN lowering, S668033). (N=2, sev corpus)
- **Surface:** S668029 (justknobs Rust `Option<bool>`→`bool` API change → CONVEYOR_REGRESSION, ~1h), S668033 (PyTorch Inductor cat indexing D105916880 → IEN lowering `ien.lower.mvai.mi300x:102` incompatible → AOTI AssertionError, ~3h)
- **Root mechanism:** Trunk advances are not gated on downstream artifact compatibility. Rust call sites using `.unwrap_or()` on `Option<bool>` stop compiling when justknobs changes the return type; multisect found SANITY_FAILURE (no single culprit diff) because the API change ITSELF is the cause. IEN lowering package built 2026-05-22 for MI300x encoded Inductor cat indexing semantics that were changed by D105916880; package was not rebuilt when trunk advanced. Both failed with CONVEYOR_REGRESSION class.
- **Detection gap:** No pre-merge CI gate verifies downstream artifact (Rust binary, compiled lowering package) compatibility when an upstream library changes API/behavior. Multisect SANITY_FAILURE is the signal — no single-culprit diff found because the API change itself is the root. Earlier detection: automated compatibility test on cached artifacts before trunk API changes land.
- **Generalization:** CONVEYOR_REGRESSION from trunk API advancement is a recurring class distinct from P52 (TorchScript annotation mismatch) and P37 (stale prod-tag size). General form: trunk advances API/behavior → cached compiled artifact built against old API fails → rebuild or patch. When multisect yields SANITY_FAILURE, check upstream library API changes in the past 24h. Two fix paths: (a) patch call sites to new API; (b) rebuild artifact against current trunk. Candidate for a new general P-row.

---

## 2026-05-27 23:00 PT — R18 re-violation: FBLearner batch model PAGEd as OT failure, self-corrected 4h later

⚠️ [validator unavailable]

- **Pattern:** Bot initially diagnosed TIFU ESR Primary + Secondary (m2129445831 + m2144714031) as REAL_OT_FAILURE and PAGE'd p92_relevance_growth at 15:59 PDT. Re-evaluation 4h later correctly applied R18: both models are FBLearner batch with NO MAST OT job → T4 serving-stage only → out of OT scope. Actual root: S668184 (TW Controller Timeouts) blocking IPNext releases. (N=1 self-correction)
- **Surface:** S668181 (TIFU ESR Primary & Secondary, 2026-05-26), models m2129445831 + m2144714031, self-correction at 19:56 PDT
- **Root mechanism:** Model names `ig_textpost_tifu_esr` and `ig_textpost_tifu_m2m_retrieval` contain OT-suggestive keywords and were tagged `mvai-online-training`. Bot formed initial REAL_OT_FAILURE hypothesis from tag alone without first verifying MAST OT job existence. R18 mandates checking for a live `mvai-training-online-<id>` MAST job BEFORE publishing any OT diagnosis. Verification was done 4h after initial PAGE — wrong oncall was paged.
- **Detection gap:** R18 check was not executed at classification time. Confidence was elevated to PAGE-worthy before `meta ai.mast-job describe --name mvai-training-online-<id>` confirmed or denied a live OT job. Classification confidence must stay ≤ LOW for any model with OT-suggestive keywords until MAST job existence is verified.
- **Generalization:** R18 compliance must be pre-emptive, not corrective. Gate pattern: if symptom is snapshot-load or serving-side (TW Controller Timeout, IPNext blockage), FIRST check `meta ai.mast-job metadata --name mvai-training-online-<model_id>` → if no job found, classify T4/serving immediately, zero PAGE. A second recent R18 violation (S668181 joins S661843 from 2026-05-13) — pattern recurrence suggests an explicit early-exit rule should be added to the sev-monitor triage chain.

---

## 2026-05-27 23:00 PT — Model 2132070936 recurrence pressure: 8th documented incident, upstream stall pattern unresolved

⚠️ [validator unavailable]

- **Pattern:** Alert A27534791649462484 (critical, 2026-05-27, FULL_SNAPSHOT missing on facebook_reels_ifu_i2i model 2132070936) is the 8th documented incident in the archive corpus referencing this model's FULL_SNAPSHOT blockage. Prior: S667567 (mitigated 2026-05-24), plus 6 post/alert archives. Root pattern: upstream reranker 2125081901 stalls (elastic-agent D-state via SIGSEGV) → downstream model 2132070936 FULL_SNAPSHOT pipeline blocked. (N=1 new alert + 7 prior cross-corpus archives)
- **Surface:** critical-2026-05-27-A27534791649462484.md, S667567, 6 prior post/alert archives on model 2132070936
- **Root mechanism:** MAST v70 RUNNING but 0 FULL_SNAPSHOT in 20 most recent instances at archive time. Alert closed via ack only (underlying signal not confirmed resolved). R20 sweep cites 7 prior incident hits across SEV, post, and alert corpora. Structural mechanism: model 2132070936 depends on upstream reranker 2125081901 publishing before it can produce its own FULL_SNAPSHOT. When 2125081901 hangs (D-state zombie after SIGSEGV in ServiceRouter stats callback), 2132070936 is transitively blocked.
- **Detection gap:** After 8 incidents with the same pattern, no structural fix applied. S667567 was mitigated by manually killing the zombie (fengzhang1), but no task filed for the SIGSEGV root cause or automated zombie detection. Alert cleared via ack — underlying publish gap unresolved per archive.
- **Generalization:** ≥5 cross-corpus incidents on the same model with the same pattern = chronic model, not recurring transient. Threshold trigger: file a "recurrence-pressure escalation" task to upstream owner (model 2125081901 / SIGSEGV crash site / TorchElastic team) requiring root fix within N sprints. Immediate ask: Feng Zhang's post-S667567 request for automated zombie recovery (kill + restart without manual intervention) was explicitly raised but not actioned — should be filed as a tracked task now.

---

## 2026-05-27 23:00 PT — CL-013 throttle threshold miscalibration: under-throttle causes QPS drop and example age alarm

⚠️ [validator unavailable]

- **Pattern:** Model 2132766001 (ig_reels_tab_mtml holdout) training example age climbed over alert threshold because the pre-proc throttle was miscalibrated at 145k — below actual steady-state QPS floor — causing under-throttle → QPS drop → example age degradation. Trainer v11 was healthy throughout; failure was entirely in the throttling config layer. CL-013 pattern confirmed. (N=1: S666880)
- **Surface:** S666880 (2026-05-26, model 2132766001), mitigated by jakubbester (p92_relevance_growth oncall)
- **Root mechanism:** Throttle threshold set below actual steady-state pre-proc QPS → throttle suppresses QPS below training consumption rate → artificial backpressure → fewer examples delivered → example age climbs. Prod trainer also affected (per GChat context). Miscalibration was at the throttle configuration, not DPP pipeline or Scribe layer.
- **Detection gap:** Under-throttle variant (threshold too low → QPS suppressed) has different diagnostic signatures than over-throttle (threshold too high → backpressure from excess QPS). Standard triage first suspects DPP starvation (P16) or Scribe volume (P04/P12). Fast CL-013 check: compare throttle threshold config value against ODS-observed steady-state pre-proc QPS — if threshold < p10 of observed QPS, under-throttle is likely.
- **Generalization:** Throttle thresholds set at model launch may drift below production steady-state as traffic scales. Periodic re-validation (quarterly or after traffic profile changes) of throttle config values against observed pre-proc QPS histograms would catch this class before it pages. CL-013 under-throttle recurrences should be tracked as a cohort.

---

## 2026-05-25 23:00 PT — SIGSEGV D-state elastic-agent zombie: distinct from P44, no auto-detection

⚠️ [validator unavailable]

- **Pattern:** SIGSEGV on a C++ service callback (ServiceRouter stats) on a single rank → D-state uninterruptible sleep → elastic-agent/TorchElastic supervisor blocked → zombie job (MAST RUNNING, no self-recovery, no NCCL watchdog trip) (N=3: S667567, POST-W1332867342141342 ×2)
- **Surface:** S667567, POST-W1332867342141342 (2026-05-23), POST-W1332867342141342 (2026-05-24)
- **Root mechanism:** SIGSEGV in C++ extension code puts a rank into D-state (uninterruptible wait on kernel); TorchElastic cannot exit cleanly because D-state ranks don't yield; elastic-agent supervisor therefore hangs indefinitely without firing the NCCL watchdog. Result: job appears RUNNING, metrics stop flowing, checkpoint stuck CREATING, downstream FULL_SNAPSHOT blocked.
- **Detection gap:** Bot initially misclassified as P44 (Python GIL hang) — same outer symptom signature (RUNNING + empty error + zero mvai_metrics samples + FS CREATING). Disambiguated only via deeper SEV investigation. No automated zombie-detection for SIGSEGV + D-state pattern; manual kill is the only recovery path, which doesn't scale (Feng Zhang, 2026-05-23).
- **Generalization:** Any C++ signal handler or service callback that raises SIGSEGV on a single rank can silently kill the job without triggering standard watchdogs. Detection requires per-rank `/proc/<pid>/status` monitoring (D-state for >N min) OR rank-level heartbeat probing in TorchElastic — neither exists today. P44 disambiguation requires py-spy; SIGSEGV detection requires process-state monitoring.

---

## 2026-05-25 23:00 PT — CL-003 upstream SEV causing 5+ repeated alert firings with no holddown

⚠️ [validator unavailable]

- **Pattern:** Active upstream infrastructure SEV (ZippyDB/Scribe) causes `scribe_read_proxy.client_lag_in_seconds` alert to fire repeatedly on the same model, each time as a separate pager event, with no suppression while the upstream SEV remains In Progress (N=4: ALERT-1480195820275950-2026-05-24, ALERT-1670185680650606-2026-05-23, ALERT-977255094865118-2026-05-24, A1021144657237695-2026-05-25)
- **Surface:** ALERT-1480195820275950-2026-05-24 (5th fire in 8 days), ALERT-1670185680650606-2026-05-23, ALERT-977255094865118-2026-05-24, A1021144657237695-2026-05-25
- **Root mechanism:** S667358 (IG Relevance T20 H100 Scribe Over Quota) and S654082/S665114 (ZippyDB AI training overload) cause transient scribe reader backpressure; `client_lag_in_seconds` spikes, alert fires, alert auto-clears when lag drops; cycle repeats without oncall having any OT-side action. CL-003 is a known cluster in failure-patterns.md but no holddown mechanism exists during active upstream SEVs.
- **Detection gap:** Bot correctly diagnoses UPSTREAM_INFRA / CL-003 each time and recommends NO_ACTION. But the alert still creates notification fatigue (5 firings in 8 days for model 2144816217 alone). No mechanism suppresses alerts when a causally-linked upstream SEV is In Progress. 
- **Generalization:** Alert suppression during known-upstream-incident windows is a missing capability. Pattern: if an `infra_sev_ids` field were attached to alerts whose root cause is confirmed-upstream, the alert system could auto-suppress re-fires while the upstream SEV is open. This would reduce OT oncall alert fatigue significantly — CL-003 is the most common alert class in May (6 of 56 alerts).

---

## 2026-05-25 23:00 PT — THRESHOLD_MISFIT false alarm recurring on same model (2132070936)

⚠️ [validator unavailable]

- **Pattern:** Model 2132070936 (facebook_reels_ifu_i2i) has incorrect expected publish types in OneDetection — detector fires "missing FULL_SNAPSHOT" and "missing DENSE_DELTA" repeatedly even though this model never produces those types. Same model triggered false FULL_SNAPSHOT alert 2026-05-23 AND false DENSE_DELTA alert 2026-05-08 — same misconfiguration unaddressed for 15+ days (N=1 new archive, but references 2 separate detector firings)
- **Surface:** ALERT-1955974545038771 (2026-05-23), prior DENSE_DELTA false alarm 2026-05-08 (from archive text)
- **Root mechanism:** OneDetection alert configured with `expected_types = [FULL_SNAPSHOT, DENSE_DELTA]` for a model whose publish path only produces ITEM_EMB_DELTA and SPARSE_DELTA. Alert fires (and pages) every time the "missing" type isn't published in the expected window — which is always, because the model never publishes those types.
- **Detection gap:** THRESHOLD_MISFIT is correctly identified by the bot, but the follow-up action (re-tune detector config) is never filed as a tracked task. No mechanism closes the loop from "false alarm identified" to "detector reconfigured." Result: repeated paging for the same misconfiguration across multiple weeks.
- **Generalization:** False-alarm identification without downstream task creation is waste. Bot should auto-file a task (or at minimum, surface a persistent alert-to-fix gap report) when the same alert fires with THRESHOLD_MISFIT verdict more than once in 30 days on the same model.

---

## 2026-05-25 23:00 PT — STUS bootstrap gap from trainer restart: 4th+ recurrence, no threshold

⚠️ [validator unavailable]

- **Pattern:** STUS model's SPARSE_DELTA publishing pauses for ~40 minutes every time the root trainer restarts a new version, due to bootstrap re-initialization gap. Alert fires on the gap. Recurring pattern: 4th+ firing in 30 days for model 2130305043 (ig_reels_tab_cs_omni_retrieval baseline) (N=1 new archive, strong recurrence signal)
- **Surface:** ALERT-1621571482326635 (2026-05-24)
- **Root mechanism:** When the root trainer (2130305053) restarts a new version, STUS requires re-bootstrapping before resuming SPARSE_DELTA publishing. The bootstrap window (~40 min) consistently falls within the alert detection window. Alert is TRANSIENT_NOISE but fires every time the trainer version bumps.
- **Detection gap:** No post-restart alert suppression window for expected bootstrap gaps. Alert threshold not raised for models with known frequent trainer restarts. Each firing requires oncall investigation to confirm it's expected.
- **Generalization:** Trainers that restart frequently (e.g., due to regular version bumps or scheduled maintenance) need STUS alert thresholds calibrated to `expected_bootstrap_time + safety_margin` rather than a fixed global threshold. A per-model `post_restart_mute_window_min` config would eliminate this class of false positive.

---

## 2026-05-25 23:00 PT — DPP Zeus TTL 20-day session rotation → transient example age spike

⚠️ [validator unavailable]

- **Pattern:** DPP Zeus TTL 20-day session rotation causes a periodic, predictable ~3.6h gap in DPP example delivery (training example age spike to 25min) on IFR holdout model. Pattern is fully transient and auto-resolves with no OT intervention — but triggers L3 SEV detection (N=1 new archive: S667544)
- **Surface:** SEV-S667544 (2026-05-23)
- **Root mechanism:** Zeus credential/session has a 20-day TTL. When the session renews, there is a transient gap in DPP example delivery; training example age climbs. Once renewal completes, example age normalizes. The 20-day cadence is predictable but not yet correlated with alert suppression or pre-announced maintenance windows.
- **Detection gap:** Alert and SEV fired appropriately (model age degraded), but the root mechanism was a scheduled credential renewal — not a real OT failure. If Zeus session renewals were announced as maintenance windows, the alert could be suppressed. No mechanism today maps periodic Zeus TTL rotations to expected transient gaps.
- **Generalization:** Periodic, predictable infrastructure credential rotations (Zeus TTL, Kerberos TGT, etc.) that cause transient training data gaps should be treated as scheduled maintenance — not novel OT failures. A calendar of upcoming TTL renewals for OT-critical data paths would allow pre-suppression and prevent L3-SEV-level responses to known-scheduled events.

---

## 2026-05-26 23:00 PT — Cross-corpus: dependency failure → snapshot blocked → manual restart (S667355 + W1332867342141342)

⚠️ [validator unavailable]

- **Pattern:** Two distinct failure classes (APS package staleness, elastic-agent D-state zombie) both result in identical recovery workflow: (1) diagnose root dependency failure, (2) fix/pin the dependency, (3) manually restart/kill the OT job to restore FULL_SNAPSHOT publishing. Cluster spans sev + post corpora. (N=2: S667355, W1332867342141342-final)
- **Surface:** S667355 (APS aps_ads_vm pkg stale → SV validator timeout, 2026-05-22–26), W1332867342141342 (SIGSEGV → elastic-agent D-state → zombie, 2026-05-23–25)
- **Root mechanism:** Two structurally different upstream failures both sever the checkpoint→snapshot publication pipeline and do not self-recover: (a) stale package version prevents the SV validator from accepting new snapshots; (b) D-state zombie process prevents the MAST job from generating new checkpoints. In both cases, the bot/oncall must identify the upstream cause, resolve it externally, and then manually trigger the restart.
- **Detection gap:** Bot treats each failure class independently (CL-019 for pkg staleness, CL-unknown for elastic-agent). The common workflow pattern — "upstream fix + manual restart required" — is not abstracted into a triage meta-rule. An oncall who identifies FULL_SNAPSHOT gap has no guided workflow to check both dependency freshness AND process liveness before choosing the recovery path.
- **Generalization:** A "snapshot publication gap recovery flowchart" would help oncall: (1) is the OT job alive? (process liveness check) → if zombie: kill + restart. (2) Is the serving/validation dependency version current? → if stale: pin + restart. Separating these two gates would reduce mean-time-to-action when FULL_SNAPSHOT has been missing >2h.

---

## 2026-05-26 23:00 PT — APS/QRT OT: stale aps_ads_vm pkg → SV validator timeout (P57 candidate)

⚠️ [validator unavailable]

- **Pattern:** APS QRT online-training models (IG CTR combo version, aps_ctr_cvr_fm family) fail SV snapshot validation after a package-dependent diff lands, because the running OT model still holds an old version of `aps_ads_vm` that cannot parse the new snapshot format. Manifests as SV validator timeout (not a packaging error) — a subtler failure mode. (N=1: S667355)
- **Surface:** S667355 (2026-05-22 to 2026-05-26), model_id=1054821984, `aps_ads_vm` needed ≥605 for new snapshot format; running version was 605-equivalent pre-pin
- **Root mechanism:** APS domain OT models pin `aps_ads_vm` via Flight Deck / conveyor hotfix path (distinct from MVAI's `light_cli` / `mvai_app` pipeline). When a new snapshot format diff lands without a coordinated package pin update, the SV validator (running updated code) cannot parse snapshots produced by the model (running old code). Validator times out rather than returning a parse error — making diagnosis non-obvious.
- **Detection gap:** No pattern in known-patterns.md for APS-domain package staleness affecting SV validation. Triage bot would fall back to general "snapshot validation failure" without a P-row to guide the APS-specific recovery path (Flight Deck hotfix + model restart via TIS debugger). Proposed as P57 in S667355 archive.
- **Generalization:** APS QRT models have a distinct release coordination path (Flight Deck hotfix for `aps_ads_vm`) compared to standard MVAI OT models. Any diff that changes snapshot format for APS models must be accompanied by a coordinated `aps_ads_vm` pin bump via conveyor BEFORE the snapshot format change reaches SV validators. This is a cross-team release coordination gap (APS infra + aps_ads_production_training oncall).

---

## 2026-05-26 23:00 PT — Elastic-agent zombie: "manual kill doesn't scale" — untracked follow-up

⚠️ [validator unavailable]

- **Pattern:** After S667567 mitigated (elastic-agent D-state zombie on model 2125081901), model owner Feng Zhang explicitly asked "is there a way to let model restart automatically when stuck without manual kill?" Denny answered "Not an easy fix — follow up after long weekend." No task filed as of resolution (W1332867342141342 final resolved 2026-05-25 21:40 PT). Follow-up untracked. (N=1: W1332867342141342-final)
- **Surface:** W1332867342141342 (final resolution archive, 2026-05-25), model_id=2125081901, comment thread 2026-05-23 17:28–19:18 PT
- **Root mechanism:** The elastic-agent hang (SIGSEGV → D-state) requires a manual `kill v<N>` to recover. TMS auto-restart only fires when MAST reports a FAILED attempt — but zombie jobs stay in RUNNING state indefinitely, so TMS never triggers auto-restart. Feng Zhang's ask identifies the correct gap: no automated zombie detection + recovery loop in TorchElastic/TMS for D-state processes.
- **Detection gap:** Incident identified as a scaling concern by the model owner, but the concern was not converted into a tracked task. The `ot-knowledge-curation` cron sees this because the final resolution archive explicitly notes "no task filed as of resolution."
- **Generalization:** When operator or model owner raises a "doesn't scale" concern during incident mitigation and no task is filed, that gap WILL recur. Bot should surface untracked follow-up asks from resolution archives as a standing prompt to operator. Minimum viable task: `task create --title "Auto-recovery for elastic-agent D-state zombie OT jobs (model 2125081901)" --owner fengzhang1 --ref S667567`.

---

## 2026-05-26 23:00 PT — STUS FULL_SNAPSHOT alert: 2nd fire without owner ack → escalation rule not in known-patterns.md

⚠️ [validator unavailable]

- **Pattern:** Alert 1455336899399360 on STUS model 2130324780 (ig_textpost_feed_m2m_retrieval) fired identically twice in 2 days (2026-05-17 + 2026-05-19) with THRESHOLD_MISFIT verdict both times. R23 and CL-008 cover STUS disambiguation, but the escalation rule for repeated fires ("passive THRESHOLD_MISFIT → explicit escalation to model owner for alert retuning") was added as an annotation to the archive — NOT codified in known-patterns.md where oncall can find it during triage. (N=1: A1455336899399360-2026-05-18 upsert)
- **Surface:** A1455336899399360, model_id=2130324780 (ig_textpost_feed_m2m_retrieval), owner=ronghuang, 2nd fire confirmed by R20 sweep
- **Root mechanism:** CL-008/R23 covers STUS FULL_SNAPSHOT mismatches and calls for NO_ACTION. But NO_ACTION on the 2nd identical fire implicitly accepts the alert mis-tuning as permanent. The escalation rule — "if same FS-missing alert fires a 2nd+ time on same model without owner acknowledgment → escalate from NO_ACTION to explicit recommendation: suppress or retune alert 1455336899399360 for STUS-role model 2130324780" — lives in the archive text but not in the pattern DB.
- **Detection gap:** No P-row encodes the 2nd-fire escalation rule for STUS threshold misfits. Oncall triaging the 3rd fire would re-diagnose THRESHOLD_MISFIT, say NO_ACTION, and not escalate — missing the opportunity to permanently fix the alert.
- **Generalization:** Patterns that are THRESHOLD_MISFIT on 2nd+ fire should trigger an escalation path, not just NO_ACTION. This is a meta-pattern: any alert with ≥2 THRESHOLD_MISFIT verdicts on the same model+detector pair within 30 days → auto-file an ask to retune. Could be added to R23 as a sub-rule: "R23.1: on 2nd+ THRESHOLD_MISFIT fire: escalate to model owner for detector retuning within 5 business days."
