# Mega-Learnings — 2026-W20 (May 11–17)

_Auto-generated by `ot-knowledge-curation` cron. Append-only; do not edit entries._

---

## 2026-05-16 00:00 PT — Snapshot-stuck-CREATING is a heterogeneous symptom, not a pattern

- **Pattern:** "Snapshot stuck in CREATING phase" co-presented across 3 distinct root causes in the same week (N=3)
- **Surface:** S652049, W1320805680014175, W1324729222955154
- **Root mechanism:** Three different roots all produce the same D7 symptom: (a) NCCL ALLGATHER timeout + TMS non-restart (P40); (b) NCCL timeout + stale base layer incompatibility; (c) `_preload_item_pool()` blocking DPP consumer at startup (`DPP_WORKER_STUCK_FULL_OUTPUT_QUEUE`). The stuck-CREATING snapshot is a downstream consequence in all three — NOT the root.
- **Detection gap:** The cron and model owners initially investigated publish-pipeline health (T3 stage) before checking trainer-side liveness. The `mvai_metrics` sample-freshness probe (R17/P44 runbook) was not applied as the first check in all 3 cases.
- **Generalization:** When ≥2 incidents in a short window show "snapshot stuck CREATING," DO NOT assume a shared publish-pipeline root. Always run trainer-liveness probe (R17) first; each case may have a distinct A-class or scheduling root. The catalog D7 entry already codifies this — the gap is in applying it consistently across oncall handoffs.

---

## 2026-05-16 00:00 PT — Mitigation over-reset causes secondary training regression ⚠️ [validator unavailable]

- **Pattern:** "Mitigation action reset more job state than intended → secondary NE regression or metric degradation" appeared in 2 distinct posts in the same week (N=2)
- **Surface:** W1324729222955154, W1320816783346398
- **Root mechanism:** (a) Clearing retryable info.json for model 2135033479 caused the next job startup to treat as cold-start → loaded from a stale recurring checkpoint instead of the pinned version → NE regression 15–25%, IFU click metric degrading. (b) Killing the OT job + re-enabling recurring training for AMD arms (W1320816783346398) without verifying STUS snapshot continuity → source rate still 15–25% below prod parity 3 days later.
- **Detection gap:** Operators and the bot proceeded with "standard restart/clear" mitigations without explicitly auditing what state would be lost. Li Lu's comment in W1324729222955154 ("clearing = cold start") is key context that wasn't surfaced in the mitigation recommendation.
- **Generalization:** Before any mitigation that resets job state (info.json clear, kill + re-enable, unregister + re-register), explicitly enumerate: (1) which checkpoint the next attempt will load from; (2) whether STUS flows need to be separately restarted. If the checkpoint source is ambiguous → set it explicitly via `mvai online-training-mgr -m <id> --checkpoint-version <N>`. This should be a pre-mitigation checklist item in the OT oncall runbook.

---

## 2026-05-16 00:00 PT — Upstream service failures cascade to OT training data quality at scale ⚠️ [validator unavailable]

- **Pattern:** Non-OT upstream service outage caused large-scale OT training data quality degradation requiring coordinated revert-and-ban operations (N=2)
- **Surface:** S659243, S635390
- **Root mechanism:** (a) S659167 Shots tier outage (2h22m, caused by QE combinatorial explosion) → TIXU OT jobs consumed corrupted serving data → out-of-range calibration on 61 IG/FB models → manual revert-ban of 20 TMS models + 41 ESR models. (b) S660329 fleet-wide EDPP resize blocked by koski high-filtering cascade → 115 OT jobs unable to scale up → Threads U2M example age spike.
- **Detection gap:** Both SEVs lasted multi-day before mitigation. The shared pattern — "upstream tier failure poisons OT data quality" — has no automated detection or alerting. The bot doesn't monitor for "did a non-OT SEV just affect OT training data?" cross-SEV correlation.
- **Generalization:** OT training data quality is a function of the full upstream data pipeline health, not just DPP/Scribe. When a high-severity upstream SEV fires on any Shots/EDPP/serving-tier component, the OT oncall should proactively audit training data collection windows and calibration metrics — not wait for model-quality alerts. Add a cross-SEV probe to `ot-sev-monitor`: when any co-active L1/L2 SEV affects serving or data tiers, emit a "check OT training data integrity" advisory.

---

## 2026-05-16 00:00 PT — Conveyor publish pipeline is fragile to upstream code regressions without pre-validation ⚠️ [validator unavailable]

- **Pattern:** Three SEVs in one week blocked the conveyor publish pipeline via upstream code regressions (N=3), with combined 200+ engineer-hours of publish downtime
- **Surface:** S664494, S664365, S661936
- **Root mechanism:** Three distinct regression sub-classes all blocked trunk publish: (a) PyTorch TorchScript return-type annotation mismatch (P52 — `torch.unique_consecutive`, 44h block); (b) Transitive C++ dependency compile break (`common/db/tests:dbdef`, class name mismatch `Connection` vs `DBConnection`, 2.6h block); (c) TGIF export timeout → cogwheel SIGKILL (157h block). None had pre-submit validation catching the break before trunk.
- **Detection gap:** P52 is documented and caught post-hoc. The transitive-dep compile break (S664365) is not covered by any P-row. The TGIF timeout (S661936) postmortem is incomplete (0.04 quality score). The 3-week clustering is a signal that the conveyor's pre-submit CI gates have systemic gaps.
- **Generalization:** A new P-row is needed for "transitive C++ dependency compile break blocking Build Node" (S664365 sub-class, distinct from P52's TorchScript). The meta-principle: every conveyor-blocking failure mode should have a CI gate that catches it pre-merge. Current gaps: TorchScript annotation lint (P52), transitive-dep compile (S664365 class), TGIF timeout budget (S661936). Add to the systemic-gap backlog.

---

## 2026-05-16 00:00 PT — Delta publishing activation exposes two uncharted failure modes

- **Pattern:** Two posts in one week reported distinct delta publishing failures not covered by existing P-rows (N=2)
- **Surface:** W1320070000087743, W1319356410159102
- **Root mechanism:** (a) Enabling delta publishing on Facebook IFR QE model → Hedwig publish backlog pile-up consumed RAM (75% → OOM); `TORCHINDUCTOR_COMPILE_THREADS=1` — useful in earlier MCs — became net-harmful in current MC (hurts QPS, worsens example age, raises memory). Mitigation: move tables to HBM + remove the flag. (b) Adding an embedding table (10 → 11) without triggering FULL_SNAPSHOT first → predictor's `model_param_config` in Manifold still maps 10 FQNs → delta update fails: "Not all sparse embeddings are mapped. Expected 11, got 10."
- **Detection gap:** (a) No pre-onboarding delta-publish memory headroom check in the OT onboarding guide. (b) No guard or warning when embedding table count changes that FULL_SNAPSHOT must precede delta re-enable.
- **Generalization:** Two additions to the OT onboarding checklist are warranted: (1) "Before enabling delta publishing, verify CPU memory headroom ≥ 15% under full publish load; move memory-heavy tables to HBM proactively." (2) "After any embedding table addition (architecture change), disable delta-only mode, trigger FULL_SNAPSHOT first, verify predictor model_param_config updated, then re-enable delta." Both are pre-activation gates, not post-failure mitigation.

---

## 2026-05-16 11:03 PT — Systemic gap: OT job auto-start has no end-to-end watchdog; silent stalls require human discovery ⭐ novel ⚠️ [validator unavailable]

- **Pattern:** Two distinct failure modes in 3 days both produce the same observable symptom — "OT job stops launching, no alert fires, model owner discovers it manually." (N=2)
- **Surface:** S664106 (Threads Feed teacher model 2128461099, MVAI expired-job class, mitigated 2026-05-14 12:12 PT); Renqin Cai mrs.ot post 1326836659411077 (model 2124118880 chronos_secgrp_explore_ranking_engineers, starcart non-restart after diff land, manually restarted 2026-05-16 ~midnight PT).
- **Root mechanisms (DIFFERENT roots, SAME symptom):**
  - (a) **MVAI manual expiration** (S664106). Per-job `expiration_date` stored in MVAI managed_training. Default ~1 year out, never visible after onboarding. Expiry hits → MVAI silently flips job out of `online_ready` → starcart stops launching → no MAST failure event, no alert. Owner (Haoyu) noticed next morning via QPS drop.
  - (b) **Recurring flow disabled** (Renqin's post — INFERRED; owner not yet confirmed). Starcart depends on an FBLearner recurring flow firing on a schedule. If `is_enabled=false` on that flow → no workflow runs emitted → starcart never invoked even after a diff lands → state stays `online_ready`. Owner discovered after waiting for auto-restart that never came.
- **What's missing (the systemic gap):** **There is no end-to-end watchdog that fires when "model intent = train, but no MAST job started/restarted within expected window."** Every existing layer monitors its own piece:
  - MAST watchdog → fires only on running-job process death, NOT on "job never started" (mega-learning #1 covers the in-process variant)
  - Starcart → assumes upstream layers will trigger it; doesn't alert when it sits idle
  - Recurring flow / MVAI managed_training → no aging alarm on "intent stale relative to last actual launch"
  - OT alert detectors → measure snapshot/delta cadence on running jobs; a job that *never launches* has no snapshot history to threshold against, so detectors stay silent
- **Why this is N=2 from a 1-week window:** S664106 and Renqin's post share the gap class (auto-start failure, silent, owner-discovered) despite distinct root mechanisms. This is the cluster definition — different roots, shared detection gap = systemic miss.
- **Generalization (proposed control):** A new probe — *"OT auto-start liveness"* — that periodically checks each registered OT model and emits an alert if `(now - last_mast_attempt_start) > 2 × expected_launch_interval` AND `intent = active`. Logic:
  - Query 1: list models with `managed_training.intent == active` (TMS API) OR `is_enabled == true` recurring flow.
  - Query 2: for each, fetch latest MAST job attempt start time.
  - If gap > 2× expected interval → page model owner. The threshold is loose enough to tolerate normal restart windows but tight enough that the S664106 (~24h gap) and Renqin's case (overnight gap) would both fire.
  - Owner: This should live in the same detection plane as `ot-alert-monitor` — propose adding a new periodic check, OR adding an inverse-detector in OneDetection that fires on "missing instances" not "stale instances".
- **Documentation control:** Until the probe lands, the OT onboarding runbook must surface the manual-expiration footgun (Li Lu owes Jamey this update per S664106 follow-up) AND document the recurring-flow enablement requirement explicitly.
- **Why this matters:** Both incidents required HUMAN discovery via downstream symptoms (QPS drop, missing snapshots). The OT detection stack is symptom-focused (cadence breaks, threshold breaches) rather than intent-focused (intent vs. actual launch state). This is a category of gap, not a single bug.
- **Cross-ref:** P59 (proposed) covers the (a) mechanism specifically (MVAI expired-job). This mega-learning is the LAYER ABOVE — the gap exists regardless of which specific root mechanism caused the stall.
- **Source threads:** spaces/AAQAVOjYc80 `8LLIVF1l7Yw` (2026-05-16 starcart triage) + cross-space trigger spaces/AAQA2bZMw24 (2026-05-16 10:45-10:50 S664106 triage).

<!-- Force re-index 2026-05-16T18:12Z: internalfb code browser was caching old content for this file -->

---

## 2026-05-16 23:00 PT — Shampoo optimizer NaN is a distinct failure class from P22/P06 ⚠️ [validator unavailable]

- **Pattern:** Shampoo second-moment-matrix NaN corruption → LOSS=NaN training signal → metrics_violation alert → model unusable; appeared across 2 corpora in the corpus window (N=2)
- **Surface:** S665135 (sev, rlett, QE Model TIFU ESR PNUA), A1703030847735006 (alert, P2, CFR MainPredictor baseline model 2134801434)
- **Root mechanism:** NaN enters Shampoo optimizer's second moment matrix (from exploding gradients, corrupted data, or accumulated numerical error in Shampoo preconditioning). Once the second moment has NaN, all gradient updates compute NaN → training LOSS=NaN → metrics_violation alert fires. Fix: revert to last good snapshot + ban bad checkpoints; apply optimizer NaN-stability patch (prod model 2128701656 had the patch; QE model did not).
- **Detection gap:** No P-row distinguishes Shampoo-optimizer-NaN from P22 (NaN in concurrent model merge) or P06 (checkpoint NE regression at serving). Both P22 and P06 focus on post-training artifacts; P06's verify step checks serving NE, not training LOSS. Shampoo optimizer state health is not in any verify checklist. Cross-corpus detection (alert signal_class=metrics_violation + SEV root_cause=NaN optimizer) fired as two separate tickets with no correlation.
- **Generalization:** When LOSS=NaN appears during training (not just at serving): (1) check optimizer state BEFORE assuming bad training data; (2) for Shampoo models, explicitly inspect second moment matrix for NaN/Inf via optimizer health check; (3) ensure the NaN-stability patch is present in the model's app layer before prod graduation. Propose new P-row: "Shampoo optimizer state NaN — training LOSS=NaN, metrics_violation alert, revert+ban+patch."

---

## 2026-05-16 23:00 PT — AGG alerts are triage dead-ends: 14–27h duration with zero pattern signal ⚠️ [validator unavailable]

- **Pattern:** AGG (aggregation-rule) alert type produces zero triage-actionable information; both instances persisted many hours with no assignee and no pattern match (N=2)
- **Surface:** A2126294138 (model 2126294138 ig_feedrec_esr_ttsn, ~27h, AGG), A2130305043 (model 2130305043 ig_reels_tab_cs_omni_retrieval, ~14h, AGG)
- **Root mechanism:** OneDetection `entity=grouped_time_series_entity` AGG alerts bundle sub-alerts (SPARSE_DELTA, FULL_SNAPSHOT, etc.) under one umbrella ID. The AGG entry's metadata does NOT expose sub-alert types or individual entity keys. ODS query on the AGG entity fails (non-queryable). The bot archives the AGG, pattern-matches nothing (no P-row applies to grouped_time_series_entity directly), marks confidence=0.3–0.35, and closes. Root cause of the underlying 14–27h issue is permanently unresolved in the archive.
- **Detection gap:** No AGG-specific handling in the alert triage procedure. No fallback to sub-alert drilling via OneDetection API or model-owner page for long-duration AGGs. A 27h unassigned AGG that clears silently should be an escalation trigger, not a silent archive.
- **Generalization:** Add two rules to triage-discipline: (1) When AGG duration > 4h with no assignee → emit model-owner advisory (page or notify); (2) When archiving AGGs, attempt to enumerate sub-alerts via OneDetection API query filtered by `model_id` + `alert_time window` to extract sub-alert types for pattern matching. Until the drill-down is implemented, add to the R-rules: "AGG alert with duration > 4h = manual follow-up required with model owner."

---

## 2026-05-16 23:00 PT — Paired baseline+holdout FULL_SNAPSHOT co-failure signals shared root, not independent failures ⚠️ [validator unavailable]

- **Pattern:** FULL_SNAPSHOT alert firing simultaneously on baseline and holdout variants of the same model is a fast diagnostic for shared root cause (N=2 alerts, same model 878858380)
- **Surface:** A4366891846955592 (facebook_cfr_main_mtml baseline, model 878858380, fired 11:59 PDT, cleared 18:30 PDT), A898952803114953 (facebook_cfr_main_mtml [holdout], model 878858380, fired 11:59 PDT, cleared 18:15 PDT)
- **Root mechanism:** Both baseline and holdout variants share the same root training job (`mvai-training-online-878858380`), the same fbpkg, and the same Boxcar maintenance window exposure. When the shared upstream fails (P17 fbpkg expiry OR P38 Boxcar preemption), both variants lose FULL_SNAPSHOT generation simultaneously. Both self-resolved ~6h later, consistent with P38 auto-restart or P17 un-expiry.
- **Detection gap:** Bot archived the two alerts independently without correlating the <1-min fire-time gap and identical model_id. The shared-root signal (same model_id, identical fire time) is visible in the metadata but no triage rule promotes it to a fast-path diagnosis.
- **Generalization:** Add fast-path rule: "When FULL_SNAPSHOT alerts fire on two variants (baseline vs [holdout]) of the same model within <5 min of each other, treat as ONE incident with shared root. Immediately check P17 (fbpkg expired) and P38 (Boxcar preemption) on the root training job. Do not investigate variants independently." This halves triage time for this class.

---

## 2026-05-17 23:00 PT — Recurring-training alert design causes systematic false positives on OT jobs ⚠️ [validator unavailable]

- **Pattern:** Two distinct mechanisms (metric formula bug + threshold misconfiguration) both produce false-positive alerts because the alert was designed for recurring training, not OT jobs (N=3, cross-corpus: 1 post + 2 alerts)
- **Surface:** W1321547686606641 (checkpoint_training_data_age_mins formula false positive, model 877766932), A708481502288258 (fb_reels_ifu_mtml_v0 prod post-FS delta pause threshold), A2382297228909438 (fb_reels_ifu_mtml_v0 holdout companion alert)
- **Root mechanism:** (a) `checkpoint_training_data_age_mins` Scuba column has UTC-vs-TZ mismatch (known bug D75703936) + uses `train_end_ts` architecturally wrong for OT's `_resolve_ts_now()` hour-alignment. Healthy OT model 877766932 reads ~3000 min when ODS shows 2.5 min actual. Denny (operator, oncall) self-diagnosed: ODS `dpp_worker.scribe_example_age_ms.avg.60` is the correct OT ground truth. (b) Post-FS delta pause threshold (3.0× = 30 min) fires during model-specific ~33-min FS→delta gap that is normal for fb_reels_ifu_mtml_v0; holdout variant fires a companion alert simultaneously doubling noise.
- **Detection gap:** Neither alert was labeled "OT-inappropriate" at fire time. D75703936 has been open but no P-row documents the full OT false-positive pattern for `checkpoint_training_data_age_mins`. P57 covers post-FS threshold (mechanism b) but not the holdout-companion doubling. Fleet-wide OT-applicability audit of recurring-training alerts has never been done.
- **Generalization:** All recurring-training alerts using `checkpoint_training_data_age_mins` should be suppressed or annotated for OT jobs; use ODS `dpp_worker.scribe_example_age_ms.avg.60` instead. P57 threshold tuning should extend to holdout variants so both prod and holdout are suppressed together. Broader: every alert inherited from recurring training needs an OT-applicability review before deployment to OT oncall.

---

## 2026-05-17 23:00 PT — Cold-start backfill stubs = 20+ unlearned OT-hours from 4 pre-launch SEVs ⚠️ [validator unavailable]

- **Pattern:** 4 of 7 new SEVs in the 7-day corpus are metadata-only backfill stubs with empty root cause/mitigation — the knowledge lives in gchat threads not yet read (N=4)
- **Surface:** S662719 (VM MTML model 2128024482, ~4.5h), S663027 (ig_feedrec_esr_ttsn SilverTorch QPS, ~4.7h), S664106 (Threads teacher model 2128461099, ~2.3h), S664296 (Threads Permalink DPP oscillation, ~20h)
- **Root mechanism:** Bot cold-started 2026-05-15; these SEVs resolved 2026-05-12–14 before the bot was running. Backfill cron generated stubs from `meta sevmanager.sev metadata` only — does not read gchat threads. Root cause and mitigation (which ARE in the gchat threads per References in each stub) are absent from the archive. S664296 is highest-value: a 20h Threads Permalink DPP oscillation with no root cause documented.
- **Detection gap:** Sibling cron `ot-daily-learning-mitigated-sevs` has no enrichment step for backfill stubs. R2c (triage-discipline: "Read SEV's live GChat — MANDATORY when channel exists") is not applied during backfill. Permanent knowledge gap for first 2 weeks of OT history.
- **Generalization:** Propose enrichment-pass cron: for each backfill stub with `postmortem field empty`, (1) extract GChat space URL from References, (2) read thread via `meta google.chat`, (3) extract root cause + mitigation from comments, (4) update stub. Priority: longest-duration L3 SEVs first. Without enrichment, postmortem corpus structurally underrepresents pre-launch history and curation cron cannot cluster from those incidents.

---

## 2026-05-17 23:00 PT — P58 ZippyDB cascade generates 3 alert records for 1 root event — suppression needed

- **Pattern:** A single ZippyDB SEV (S665163) generated 3 separate OneDetection alert records for model 878102693 over ~21h, each requiring independent triage lookup (N=3, alert corpus)
- **Surface:** A2387001468469120 (individual SPARSE_DELTA, 02:08 PDT May 17), A878102693-413 (AGG urgency=4 :413, ~11:24 PDT May 16 – 08:19 PDT May 17), A878102693-417 (AGG urgency=4 :417, ~04:03 – 08:19 PDT May 17)
- **Root mechanism:** OneDetection creates individual alert for initial scribe_read_proxy spike, then creates AGG instances :413 and :417 as ZippyDB degradation continues producing additional sub-alerts. AGG metadata is opaque (sub-alert types hidden); each required separate ZippyDB SEV lookup to classify. All three correctly resolve as P58 but each was filed independently.
- **Detection gap:** `ot-alert-monitor` has no dedup logic for "same model + same active upstream SEV = already triaged." AGG instances :413 and :417 were each filed as NEEDS_INVESTIGATION despite being the same root as already-closed A2387001468469120. AGG urgency=4 sub-class has no P-row verify step to auto-inherit upstream SEV correlation.
- **Generalization:** Add suppression rule to ot-alert-monitor: "If P58 ZippyDB SEV is already noted for model M, new alerts for model M in same SEV window → auto-suppress as duplicate, note `[P58 dedup: SEV S<N>]`, skip NEEDS_INVESTIGATION." Reduces per-SEV alert triage from N tickets to 1. Extend P58 verify step: after confirming ZippyDB SEV active, immediately check open alerts for same model and mark all as P58-correlated.

---

## 2026-05-17 23:00 PT — _preload_item_pool() startup deadlock is an uncharted A-class failure mode — propose P59 ⚠️ [validator unavailable]

- **Pattern:** OT job enters persistent crash-restart loop from startup-phase deadlock: preload_item_pool() blocks DPP consumer → GetDataTimeoutError 6000s (N=1, high-quality MVAI bot + operator diagnosis)
- **Surface:** W1324864999608243 (UserLCE + VelV Threads M2M OT, models 2124038092/2124047632/2123351801/2123352958, ~43h duration)
- **Root mechanism:** `ig_retrieval/trainer.py:332` → `serving_aware.py:278` calls `_preload_item_pool()` during checkpoint loading startup, blocking the DPP consumer thread. DPP workers fill output queues with no consumer draining → `GetDataTimeoutError` after 6000s → crash-restart loop. Reproducible across 2 regions (vll, ncg) and 9 restart attempts over 21h. Distinct from P44 (GIL — Python holds GIL itself), P48 (blocking RPC — network socket), P46 (storage syscall stall — state D). Reducing `--preload-batch-size` (tried by Rudra, did NOT resolve). D104469704 speculated as related but Lan Xue analysis suggests different mechanism from P43.
- **Detection gap:** No P-row covers startup-phase preload deadlock. Bot classifies as REAL_OT_FAILURE/NEEDS_INVESTIGATION with no structured fix path. Cannot distinguish from other A-class hangs without `DPP_WORKER_STUCK_FULL_OUTPUT_QUEUE` keyword in error.
- **Generalization:** Propose P59: `_preload_item_pool() startup deadlock → DPP_WORKER_STUCK_FULL_OUTPUT_QUEUE → GetDataTimeoutError`. Verify: MAST error contains both `DPP_WORKER_STUCK_FULL_OUTPUT_QUEUE` AND `GetDataTimeoutError` AND model uses ig_retrieval/trainer.py. Fix: (1) kill job; (2) limit item pool to specific recent partition (`ds=*` → `ds=<recent>`); (3) long-term: enable async item pool reload. Route to `p92_relevance_retrieval` model owner.

---

## 2026-05-17 23:00 PT — AI Codemod "Unknown type name" conveyor block is a distinct sub-class from P52 ⚠️ [validator unavailable]

- **Pattern:** AI Codemod injects non-TorchScript-compatible type annotation → `RuntimeError: Unknown type name '<X>'` at schema_utils.py TorchScript compile → light_cli conveyor blocked (N=1, S661983; distinct error class from P52)
- **Surface:** S661983 (light_cli conveyor blocked 11.6h, 2026-05-10 19:56 – 2026-05-11 07:30 PT)
- **Root mechanism:** D104514584 (Racer AI Codemod) injected `-> torch.fx.Node` return annotation into `fx_create_arg`. TorchScript type resolution at `dper3/core/schema_utils.py:873` cannot resolve `torch.fx.Node` → `RuntimeError: Unknown type name 'torch.fx.Node'`. Conveyor blocked at compile step for 11.6h; no trunk releases shipping; model freshness degraded across MRS/IFR. Mitigated by D104659942 (revert). **Distinct from P52**: P52 error = "Return value was annotated as having type Tuple[X] but is actually of type Tuple[X,Y,Z]" (type-arity mismatch). S661983 error = "Unknown type name" (type not TorchScript-compatible at all). Same root class (AI Codemod bad annotation) but different TorchScript failure sub-class.
- **Detection gap:** P52 covers Tuple-arity annotation mismatch. "Unknown type name" variant (non-TorchScript types: `torch.fx.Node`, `torch.fx.Graph`, `typing_extensions.*`) has no dedicated P-row. No pre-submit lint gate validates TorchScript type compatibility in AI Codemod outputs.
- **Generalization:** Extend P52 (or add P52b): when AI Codemod injects non-TorchScript-compatible type (error: `Unknown type name '<X>'` at schema_utils.py), revert the annotation in the offending function. Long-term: add lint gate in AI Codemod pipeline to validate all injected annotations use only TorchScript-compatible types before merging.
