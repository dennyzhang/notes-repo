# Mega-Learnings — 2026-W18 (Apr 27 – May 3)

_Backfilled 2026-05-16 from `meta sevmanager.sev list --tags=mvai-online-training --created-after=2026-04-27 --created-before=2026-05-03`. 27 SEVs in scope._

---

## 2026-05-16 12:00 PT — Conveyor / cogwheel publish failures dominate (N=8) ⭐ ⚠️ [validator unavailable]

- **Pattern:** 8 of 27 SEVs (~30%) were `[mvai/*]` cogwheel / publish-pipeline failures with explicit root-cause keyword in the title
- **Surface:** S656725 (`cogwheel CUDA OOM + predictor v5486 crash`), S657453 (`online_train_publish failure — stale MaaS config FQN`), S658312 (`cogwheel failure — FBLearner OOM + suffix collision`), S657711 (`TL Publish NaN/Inf`), S658188 (`mvai run crashes with "Set changed size during iteration" on mvai_cli:7258+`), S658142 (`cogwheel_ifr_mtml_trunk_metrics_test publish failure — D103…`), S657827 (`udd_lsr/ifu_lsr/cfr_main publish failure` — multi-model), S657677 (`cogwheel_ig_ranking_esr_test serving_eval — Vanguard pred`)
- **Root mechanism:** Multiple distinct sub-classes share the same cogwheel surface — CUDA OOM, stale MaaS FQN, FBLearner OOM, NaN/Inf, Python `set` iteration bug, Vanguard predictor breakage. **This is the precursor of cluster F (CONVEYOR_REGRESSION) from W20** — same surface, same week-over-week density.
- **Detection gap:** Cogwheel test failures are visible to mvai-reliability oncall but operators across PGs (Threads, IG Feed, Reels, FB IFR) only learn via blocked releases / freshness alerts. No upstream notification when cogwheel breaks.
- **Generalization:** **Cluster F (conveyor regressions) is now N=10+ across W18 + W20.** This is a chronic, systemic miss, not a temporary spike. Justifies dedicated infrastructure: pre-merge cogwheel-readiness gate for any diff touching mvai paths, plus a "what's blocking releases right now" dashboard linked from MRS oncall pages.

---

## 2026-05-16 12:00 PT — Snapshot/publish stalls on individual models (N=8, heterogeneous roots) ⚠️ [validator unavailable]

- **Pattern:** 8 SEVs reported per-model snapshot / streaming stalls, each with distinct root cause
- **Surface:** S657690 (m883552231 stuck publishing), S658369 (m2147179183 not deploying snapshots), S657071 (m2143737814 snapshot transition failure), S656875 (m924908181 model age), S655459 (Reels Retrieval SS T2I Sparse Streaming stopped), S658165 (IG OT jobs stuck — silent failure on delta publisher TCPStore binding), S657498 (ig_organic_feed_mtml PSKIP / TOPRANK), S657977 (Prod T2I OT Job Stuck in Pending State3)
- **Root mechanism:** Heterogeneous — TCPStore binding bug (S658165 — silent failure class), QE entitlement issues, MaaS config drift, scheduler pending, capacity. Same SYMPTOM (snapshot/stream stuck), different ROOTS.
- **Detection gap:** Same lesson as W20 mega-learning #1 (`Snapshot-stuck-CREATING is a heterogeneous symptom`). The R17 trainer-liveness probe — codified in W20 — would have been useful here but didn't exist yet. **This week is the empirical evidence base that motivated R17.**
- **Generalization:** Confirms the W20 finding wasn't a one-week fluke. The "snapshot stuck = many roots" problem is structural. Operationalize: any cron triaging a snapshot-stuck symptom MUST run R17 first (already encoded in `ot-alert-monitor` 2026-05-13).

---

## 2026-05-16 12:00 PT — MAST scheduling / capacity is a recurring blocker (N=5) ⚠️ [validator unavailable]

- **Pattern:** 5 SEVs about MAST job-pending / preemption / hardware-data-colocation
- **Surface:** S658149 (`Jobs pre-empted even while under quota`), S658035 (IG Feed ESR prod OT pending without entitlement over-quota), S657920 (mvai-training-online-2126520686 pending despite crit priority), S657606 (threads u2m blocked — no hardware data colocation), S657977 (Prod T2I Stuck in Pending)
- **Root mechanism:** All 5 are MAST / scheduling-layer failures — quota accounting, preemption logic, hardware-data placement. Pattern: when MAST scheduler decisions diverge from documented behavior, the OT oncall debugs in vain because the root is one layer down.
- **Detection gap:** No automated "if MAST job pending > N minutes despite quota, check MAST oncall first" rule. Operators wasted time investigating model config when the root was scheduler.
- **Generalization:** Add to triage discipline (R-rule candidate): for any `pending`/`preempted` symptom, check MAST oncall + scheduler state BEFORE forming model-side hypothesis. This is upstream-infra-check class (P50 family) but extended to MAST itself, not just data tiers.

---

## 2026-05-16 12:00 PT — One critical cross-org leak (S657101 prevention working in retrospect) ⚠️ [validator unavailable]

- **Pattern:** S656088 (Everstore Read/Write Reliability Dip — L2) and S655556 (Unidash permission errors — L2) appeared in `mvai-online-training` tag-filter results despite being broader-org infra
- **Root mechanism:** Tag broadness — Everstore impacts MRS, Unidash impacts everyone. Both got tagged `mvai-online-training` because they affected the OT tier among others.
- **Detection gap:** Future ot-sev-monitor would now correctly classify these as `OUT_OF_SCOPE` via R18 stage check (Everstore = T1 infra, Unidash = T4 / out-of-pipeline). The fact they showed up in this filter is exactly why R18 was added.
- **Generalization:** **Strong validation that R18 was the right addition.** Pre-R18 W18 corpus has 2 cross-tier leaks; W20 with R18 has zero. Worth surfacing in the systemic-gap report.

---

**Week summary**: 27 SEVs, 4 substantive clusters. Conveyor failures (N=8) + per-model stalls (N=8) + MAST scheduling (N=5) account for 21/27 = 78% of week. Heterogeneous roots within each cluster reinforce W20 findings.
