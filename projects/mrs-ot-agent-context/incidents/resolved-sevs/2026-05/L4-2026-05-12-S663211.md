# S663211 — [mvai/umia_v1_igr] IGR Tab Omni online training fails on checkpoint save

- **Verdict:** MITIGATED_BACKFILL
- **Class:** UNKNOWN_BACKFILL (no fire-time bot triage — pre-2026-05-15 cron)
- **Cluster:** (not classified — backfill stub)
- **P-row:** (not classified — backfill stub)
- **Bot triage thread:** `[no live triage — SEV closed before ot-sev-monitor was running, retroactively backfilled 2026-05-17]`

> **BACKFILL HEADER (added 2026-05-17 thread `[backfill-pass]`):** This archive is a retroactive stub generated from `meta sevmanager.sev metadata` ONLY. The bot did NOT read the live gchat thread, did NOT execute R20/R21/R22/R23, did NOT verify cluster mapping. Scope check passed via `scope_check` tool: `signal_class=mrs_online_training`, rationale: `S663211: title matches explicit OT signal`. **Treat as 'metadata-only stub' not 'verified diagnosis.'** Operator or a future deeper-triage pass can enrich by reading the gchat thread (URL in References below) and re-classifying.

## R20 — same-workload recurrence (last 30d on this model)

`[SKIPPED — backfill stub; no model_id extraction performed]`

## R21 — cross-workload sibling check

`[SKIPPED — backfill stub]`

---

- **Identity:** signal_class=mrs_online_training, level=L4, sev_type=Production, owner=Ezra Khuzadi, impacted=ig-feed-blending, opened=2026-05-12 19:09:36 UTC, mitigated=2026-05-13 09:11:46 UTC, closed=? UTC, duration=28.2 hours
- **Resolution signal:** `[postmortem field empty — backfill from metadata only]`. SEV status=Mitigated. Report marked non-required (L4 auto-policy).

## Timeline
- 2026-05-12 19:09:36 UTC — SEV opened
- 2026-05-13 09:11:46 UTC — Mitigated

## MAST error log slice (±5 min around time_started)
```
[backfill stub — MAST log not pulled; would require re-querying ai.mast-job.error]
```

## Root cause
[postmortem field] Likely regression from D104469704 (8bcf18e4377c), which added a distributed all_reduce in minimal_viable_ai/core/trainer/metrics.py PeriodicStepMetrics.should_compute(). That method is reached from force_validate_metrics before synchronous modelstore/DCP checkpoint saves. If metric reporting is asymmetric or ordered differently across ranks, the new collective can desynchronize ranks and cause the later DCP gather_object collective to time out.

## Mitigation
[postmortem field] Revert or disable the new all_reduce in PeriodicStepMetrics.should_compute(), or scope the synchronization to a rank-safe callsite for the specific metric that needed it. Confirm by rerunning R11357.1 with D104469704 reverted or knob-disabled.

## Prevention
[postmortem field] Avoid adding default-process-group collectives inside generic metrics/reporting helpers unless every caller is guaranteed all-rank and same-order. Add a distributed checkpoint/metrics regression test or canary covering force_validate_metrics before checkpoint save.

## Confidence: 0.20
Backfill stub from metadata only; no fire-time bot triage, no gchat read, no R20/R21/R22 verification. Confidence intentionally low so future enrichment passes (operator-initiated OR cron-driven) recognize this as fertile ground for re-triage.

## References
- SEV: https://www.internalfb.com/sevmanager/view/663211
- GChat space: https://chat.google.com/room/AAQAQT2btHo
- Backfill source: `/tmp/sev-meta/S663211.json` + `/tmp/sev-scope/S663211.json` (generated 2026-05-17 thread `[backfill-pass]` from `MISSING.md` after operator: "each SEV should be one file")
