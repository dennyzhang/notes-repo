# A2126294138 — [AGG] Model 2126294138 (ig_feedrec_esr_ttsn) multiple alerts aggregation - 4

- **Verdict:** NEEDS_INVESTIGATION
- **Class:** MISCONFIG_AGG
- **Cluster:** [CL-018](../../../auto-learnings/failure-patterns.md#cl-018) — symptom-family match; falsifier check skipped retroactively
- **P-row:** `(no P-row match — see Hypothesis section)`
- **Bot triage thread:** `[no live triage URL captured — pre-dcea1e7bc011 cron didn't echo bot reply URLs]`

> **Retroactive header (applied 2026-05-17 11:08 PT via TITLE-ONLY re-classification — fixed from 11:06 PT bulk attack which used content-grep and produced cross-reference false-positives):** This archive predates the verdict/class/cluster/P-row header field (commit `4d2ae13abb6a` 10:55 PT) and FALSIFIER-RESPECT discipline (commit `0a5d77387e24` 11:02 PT). Header above is from title-keyword classification ONLY (not content-grep) so cross-reference mentions don't trip false positives. Falsifier status is documented but NOT actually checked (would require ground-truth queries at fire-time, often impossible due to data retention). **Treat as "title-shape classification" not "verified diagnosis."** Original archive content preserved below; if it cited a different P-row in its Hypothesis section, the in-content citation supersedes this retroactive header for that specific alert.

## R20 — same-workload recurrence (last 30d on this model)

`[VERIFIED-RETROACTIVELY: model_lookup_skipped]` — retroactive backfill scope did NOT execute R20 query (each requires 2+ meta CLI calls × 14 files = too costly for bulk). For per-archive R20: re-run `meta sevmanager.sev list --title-contains=<MODEL_ID> --created-after="30 days ago"` AND `meta monitoring.alert list --alert-contains=<MODEL_ID>` (caveat: alert API retention <30d so prior_alerts=0 is unverifiable).

## R21 — cross-workload sibling check

`[VERIFIED-RETROACTIVELY: family_lookup_skipped, baseline_variant_check_skipped]` — same retroactive-scope reason as R20.

---

- **Identity:** signal_class=AGG, priority=high (urgency=4), assigned_user=none, source_oncall=mrs_online_training, opened=2026-05-15T18:37:28-07:00, fired=2026-05-15T18:37:28-07:00, cleared=unknown (no end_time; verified "No active children" at 2026-05-16T22:05 PDT), duration=~27h+ (open→no-active-children)
- **Resolution signal:** recovered — OneDetection description="No active children" at 22:05 PDT 2026-05-16; AGG itself is parent metadata only. Sub-alerts (see R22 expansion below) verified individually.

## Timeline

- 2026-05-15 18:37 PDT — AGG alert fired on model 2126294138 (ig_feedrec_esr_ttsn). 4 sub-alerts aggregated.
- unassigned throughout
- 2026-05-16 22:05 PDT — verified "No active children" (all sub-alerts cleared); ot-cron-health-watch confirmed.

## R22 expansion — what the AGG actually aggregated

Per R22 (sub-alert expansion via `meta monitoring.alert list --alert-contains 2126294138 --state-is=ACTIVE,CLEARED`), the 4 underlying signals were:

### Real signals (2)

1. **Inference QPS instability (MAJOR)**
   - `Model 2126294138 (ig_feedrec_esr_ttsn baseline) inference QPS`
   - entity=`sum`, metric=`sr.num_requests.sum.60`
   - Owner: `3654113611507034`
   - Interpretation: model's inference layer (downstream of OT) was reporting unexpected QPS variance. Not a training-side OT issue; more likely a serving-side signal (T4) cascading from upstream training.

2. **FEED_UNIFIED_PTURN_ON_AUDIO_ALL_MEDIA UNSTABLE (MAJOR)**
   - `[BASELINE Model][ig_feedrec_esr_ttsn] FEED_UNIFIED_PTURN_ON_AUDIO_ALL_MEDIA is UNSTABLE for model 2126294138`
   - entity=`grouped_time_series_entity` (feed_shadow_uuid_one_feed_esr_monitoring)
   - Owner: `1183422697162055`
   - Interpretation: feed-level shadow-monitoring detected unstable signal-quality on this baseline model. Likely related to (1) since shadow monitoring tracks feed-rendering outcomes that depend on inference behavior.

### Noise / dead-detector (2)

3. `[dead detector] level CI lower bound for train_ne_ratio in ratio pair 2126294138_over_879633040 (v2)` — WARNING. Dead-detector orphan; not a real signal. Should be removed (see step-10 invalid-detector follow-up generator).

4. `[dead detector] level CI lower bound for train_ne_ratio in ratio pair 2126294138_over_2124801213 (v2)` — WARNING. Same class as above.

### Other (INFO-level, not aggregated)

For completeness, `jec.gpu_mem.ig_gpu_mem_util` and `jec.sm_util.ig_sm_util` INFO-level observers also fire on this model but are info-class and don't trigger aggregation.

## Hypothesis

**Real-signal interpretation:** the 2 MAJOR sub-alerts (inference QPS + feed shadow monitoring) suggest a serving-side or feed-rendering instability on the BASELINE variant of model 2126294138. Both are downstream of OT — the OT training job itself was likely healthy throughout (mvai_metrics not pulled in this postmortem; verify if needed via `meta scuba.dataset query -d mvai_metrics --view=samples -w '[{"column":"model_entity_id","op":"eq","values":["2126294138"]}]'`).

**Why it sat 27h unassigned:** AGG-level alerts before R22 (shipped 2026-05-17 08:25 PT) were triage dead-ends — bot couldn't expand sub-alerts. Operator saw "AGG, 4 sub-alerts, no pattern match" and rolled forward without drilling in. Now with R22, future AGGs will expand at triage time.

## Mitigation

[unknown — self-resolved over 27h with no human intervention]

The 2 real signals likely auto-resolved when downstream serving stabilized (feed_shadow_uuid_one_feed_esr_monitoring is a passive monitor; instability detection clears when QPS / signal-quality normalizes).

## Follow-ups (per new step 10 from 2026-05-17 thread `Uc-pVBEXNQ8`)

- **🔧 Invalid detectors to remove (2):** the two `[dead detector]` orphans should be disabled via `meta monitoring.observer disable --observer-id=<id>`. They're polluting AGG counts without representing real signals. Specifically:
  - Observer 2048828965983328: `[dead detector] level CI lower bound for train_ne_ratio in ratio pair 2126294138_over_879633040 (v2)`
  - Observer 816929177841127: `[dead detector] level CI lower bound for train_ne_ratio in ratio pair 2126294138_over_2124801213 (v2)`
- **🔧 AGG threshold review:** the AGG fires when ≥4 sub-alerts active. With 2 dead-detector noise, the threshold is effectively 2 real + 2 noise = 4. If the dead detectors are removed, threshold may need adjustment to fire on 2 real signals.

## R22 retrospective

This archive was originally written **before R22 landed** (2026-05-17 08:25 PT commit `11afbb82cf1c`). The original archive marked sub-alerts as "not accessible" — wrong premise; sub-alerts were always accessible via `meta monitoring.alert list --alert-contains <MODEL_ID>`. Operator caught the missing detail in thread `Uc-pVBEXNQ8` 2026-05-17 10:43 PT. This archive backfilled 2026-05-17 ~10:45 PT to demonstrate post-R22 enrichment.

## Confidence: 0.75 (was 0.3 pre-R22)

After R22 expansion: high confidence that the 2 real sub-alerts were inference-QPS + feed-shadow-monitoring instability; both auto-resolved over 27h. Cannot independently verify mitigation mechanism (no live triage on file from the time it fired).

## References

- AGG alert: https://www.internalfb.com/onedetection/alert?alert_id=ai%2Fmodel_registry%2Falerts%2Fcentralized_alerting%2Faggregation_rules%2Fcentralized_alerting.aggregation_rule%2F2126294138.aggregation_rule%3Burgency%3D4%3B2%3A8
- Real sub-alert 1 (inference QPS): part of `sum` entity (filter `meta monitoring.alert list --alert-contains 2126294138 --entity sum`)
- Real sub-alert 2 (feed shadow monitoring): part of `grouped_time_series_entity` (filter `--alert-contains 2126294138 --entity-contains feed_shadow_uuid`)
- R22 codification: see `~/notes/users/dennyzhang/projects/mrs-ot-agent-src/team_bot/cron-jobs/ot-alert-monitor.md` step i-c.2
- Cluster: matches CL-018 (AGG-rule blind-spot, now routed by R22)
