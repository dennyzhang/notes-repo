# A1480195820275950 — Model 2144816217 (ig_reels_tab_ss_omni_retrieval holdout) online training e2e latency sparse delta

- **Verdict:** MONITOR
- **Class:** NEEDS_INVESTIGATION (P58 falsified — see Hypothesis)
- **Cluster:** [CL-003](../../../auto-learnings/failure-patterns.md#cl-003) (downstream-infra cascade family) — partial match by symptom shape; cluster's evidence list does NOT include this alert because P58 falsifier failed
- **P-row:** `(no P-row match — P58 falsifier failed: no active ZippyDB SEV at fire-time)`
- **Bot triage thread:** `[no live triage on file — alert pre-dated bot's R22/format-2-section restructure on 2026-05-17]`
- **Identity:** signal_class=SPARSE_DELTA, priority=high (urgency=4), assigned_user=none, source_oncall=mrs_online_training, opened=2026-05-16T11:59:59-07:00, fired=2026-05-16T11:59:59-07:00, cleared=2026-05-16T13:59:59-07:00, duration=~2h, model_id=2144816217, model_type_name=ig_reels_tab_ss_omni_retrieval, owner=shuyaoli
- **Resolution signal:** recovered — OneDetection status=4, end_time=2026-05-16T13:59:59-07:00 confirmed. ODS direct verification unavailable post-clearance.

## Timeline

- 11:59 PDT — alert fired (scribe_read_proxy `client_lag_in_seconds` elevated for model 2144816217 holdout)
- 11:59 PDT — unassigned (no assigned_user in feed)
- 13:59 PDT — cleared (OneDetection status→4, end_time set, ~2h duration)

## Underlying signal slice (±5 min around fired_time)

```
[ods/scuba: model-scoped query returned no data even with --constraint model_id=2144816217.
 Likely cause: scribe_read_proxy entity reports lag aggregated across consumer cohorts,
 not per-model. Alert title contained the model_id as a derived label, not as an ODS constraint.
 Per current OTS query API, this metric is not deeply re-queryable post-clearance.
 TODO: file a tracking task for "per-model scribe_read_proxy lag queryability" — currently
 logged only here; if recurring, escalate to OTS team for entity-key schema enhancement.]
```

## R20 — same-workload recurrence (last 30d on this model)

`[VERIFIED: model_2144816217 prior_SEVs=0 (meta CLI); prior_alerts=unverified (OneDetection API retention <30d); local_archives=0 (no per-incident hits in mitigated-sevs/posts/alerts excluding self); cluster_evidence=1 (failure-patterns.md CL-013 evidence line cites this alert: 2026-05-16 IG, 2h self-resolution, P04-candidate); mega_learnings=0]`

- **Meta CLI:** No prior SEVs found via `meta sevmanager.sev list --title-contains 2144816217 --created-after="30 days ago"`. Alert recurrence within 30d **cannot be ruled out** — OneDetection cleared-alert retention is shorter than 30d.
- **Local archives (sweep added 2026-05-17 thread `Uc-pVBEXNQ8` 11:10 PT after operator: "check the local tracking, right? SEVs, posts, and alerts"; **extended 11:18 PT to include mega-learnings**):** searched `~/notes/.../mitigated-{sevs,posts,alerts}/` AND `~/notes/.../mega-learnings/` for any prior `2144816217` reference. Excluded: self, INDEX.md, README.md, MISSING.md, NOISY-MODELS.md (auto-gen / metadata false positives).
  - mitigated-sevs/: 0 hits
  - mitigated-posts/: 0 hits  
  - mitigated-alerts/: 0 hits (excluding self)
  - **mega-learnings/registry/failure-patterns.md: 1 hit** — this alert IS cited in CL-013 evidence line as `**A1480195820275950** (2026-05-16 IG, model 2144816217 ig_reels_tab_ss_omni_retrieval holdout OT e2e latency sparse delta, 2h self-resolution, P04-candidate per operator triage 2026-05-16 22:16 PT)`. **Bot already promoted this incident to CL-013 canonical knowledge.** This is FIRST-CLASS recurrence evidence that the meta-CLI-only check missed.

Summary: this model has no prior PER-INCIDENT triages in bot's archives, BUT this very alert is already canonical CL-013 evidence (operator-triaged 2026-05-16 22:16 PT). Cannot rule out prior cleared alerts (OneDetection retention <30d). Treat as **bot-known via cluster citation but no other prior incident history on this exact model**.

## R21 — cross-workload sibling check (last 24h on model family + baseline variant)

`[VERIFIED: family=ig_reels_tab_ss_omni_retrieval, sibling_alerts_24h=0, baseline_variant_check=skipped]`

No sibling SEVs/alerts on the model family in the 24h window per `meta sevmanager.sev list --title-contains "reels_tab_ss_omni_retrieval"`.

**Known R21 spec gap:** Holdout/baseline variant check was NOT performed. R21 spec uses `model_type_name` family match, but a model's baseline variant typically has a different `model_id` and the same `model_type_name`. The cron didn't separately check whether `<baseline of 2144816217>` was firing concurrently. Same gap exists for all holdout/baseline-paired models. TODO: add explicit baseline-variant resolution to R21.

## Hypothesis (from real-time triage)

Symptom signature is consistent with downstream-infra-cascade family ([CL-003](../../../auto-learnings/failure-patterns.md#cl-003)) and the [P58 ZippyDB RE throttling pattern](../../../mrs-ot-agent-src/known_patterns.md) — scribe_read_proxy lag is the canonical symptom for both.

**But P58 falsifier fails:** P58 requires "(1) Confirm active ZippyDB SEV" via `meta sevmanager.sev list --in-progress | grep zippy`. Checked retroactively: no ZippyDB SEV active during the 11:59-13:59 PT window on 2026-05-16. S665163 (today's ZippyDB SEV) started 2026-05-17 00:15 PT — 10+ hours after this alert cleared.

So the symptom matches the family but the canonical root (ZippyDB SEV) is absent. **Possible explanations** (each requires separate verification):
1. Smaller-scale ZippyDB event that didn't reach SEV threshold
2. Scribe-side backpressure independent of ZippyDB
3. Per-consumer-cohort throttling that affected this holdout's scribe consumer specifically
4. Holdout-only DPP-side issue (lower-priority cohort, lower QoS guarantees)

Without the ODS signal slice + lacking a contemporaneous SEV, cannot disambiguate. **Hypothesis is provisional, not P58-matched.**

## Mitigation

`[unknown — no assigned user; alert self-resolved after ~2h]`. No active P-row to cite. Standard reaction would be "wait for upstream to stabilize", which is what happened.

## Confidence: 0.4

Downgraded from initial 0.7 after P58 falsifier check. Justification:
- Verdict (MONITOR) is correct (no operator action was needed)
- Class (NEEDS_INVESTIGATION) is honest about the falsifier failure
- Cluster (CL-003) match is by symptom-family only, not by evidence (P58 falsifier fails)
- R20 is incomplete (alert retention <30d)
- R21 is incomplete (baseline variant not checked)
- No ODS verification available post-clearance

## What this archive proves about the cron's gaps

This backfill exposed 3 structural cron gaps:

1. **P-row citation discipline must respect falsifiers** (per P-007), not just match by symptom shape. Cron should auto-check the P-row's falsifier section before citing — if falsifier fails, downgrade to `(no P-row match — <P-row> falsifier failed: <reason>)`.

2. **R20 should distinguish "verified 0" from "unverified due to retention"** — current spec lumps both as `prior_alerts=0`.

3. **R21 should explicitly handle baseline/holdout variants** — paired-variant siblings have different model_id but same model_type_name. Current sibling check via family-keyword regex may miss this.

All 3 are TODO items for next cron-prompt restructure.

## References

- Alert: https://www.internalfb.com/onedetection/alert?alert_id=1480195820275950
- Cluster (symptom-family match only): [CL-003 in failure-patterns.md](../../../auto-learnings/failure-patterns.md#cl-003)
- Falsified P-row attempt: [P58 in known_patterns.md](../../../mrs-ot-agent-src/known_patterns.md) — falsifier failed
- Sibling SEVs/alerts: 0 verified; baseline-variant check skipped (R21 spec gap)
- Backfilled 2026-05-17 ~10:55 PT; **re-attacked** 2026-05-17 11:02 PT per operator follow-up thread `Uc-pVBEXNQ8`
