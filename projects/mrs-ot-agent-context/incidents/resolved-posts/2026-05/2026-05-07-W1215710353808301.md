# W1215710353808301 — [IG Reels LSR] online_train_publish Step Failing to Produce OT Diff

- **Identity:** lane=sev_id (S656611), author=Jakub Bester, source_oncall=mrs_online_training, opened=2026-05-07T10:30Z, resolved=2026-05-14T10:30Z (heuristic), duration=7 days
- **Resolution signal:** Check 8 (aged out — post is 9 days old, no activity since May 8; last comment May 8 08:11 UTC, not in last 48h). Degraded=true (heuristic_aged_7d).

## Timeline
- 10:30 UTC May 7 — posted by Jakub Bester (IG Reels LSR — flow f1078311961 online_train_publish step succeeds but produces no OT diff; references SEV S656611)
- 11:14 UTC May 7 — Jiahao Luo (🤖 MVAI Agents): root cause diagnosed as `skip_recurring: True` early return; fix = add `"skip_recurring": False` in workflow_input_override; workaround = `mvai online-training-mgr generate-job-config-diff -m 2124252069`
- 14:27 UTC May 7 — Jiahao Luo: #movebot to MRS Online Training Users
- 08:11 UTC May 8 — Jakub Bester: "I tried running this command, but it fails with error: JOB_NOT_FOUND (errorCode: 407)"
- [no further activity]
- 10:30 UTC May 14 — aged-out heuristic resolution (7d with no activity)

## Post body (full)
```
# [IG Reels LSR] online_train_publish Step Failing to Produce OT Diff
**What is the urgency of your request?**
It is an issue that can wait for best effort support from the group

**Please link the SEV criteria being violated.**
S656611

**What is the title for this issue?**
[IG Reels LSR] online_train_publish Step Failing to Produce OT Diff

**Which stage is the job failing at? (if not applicable plz select others)**
Publish #publish

**Please describe the issue you're encountering in detail with context.**
I launched the following MAAS TL flow f1078183542, which also has an `online_train_publish`
step in f1078311961. However, it fails to produce the diff needed to put this model into OT.

**Please provide (comma-separated) FBLearner Flow/MAST IDs or paste links.**
f1078311961

**Please share your steps & commands to reproduce the issue.**
Checkout to the hash in the flow and run the run_loop.py file.
```

## Comments thread (relevant excerpts)
- Jiahao Luo (🤖): "Root Cause: `skip_recurring: True` in flow f1078311961 causes an early return in `recurring_train.py` (line ~591) BEFORE the TLS config diff generation logic executes — so the step succeeds but produces nothing. Fix: Add `\"skip_recurring\": False` to the `online_train_publish` step's `workflow_input_override` in your run_config. Quick Workaround: `mvai online-training-mgr generate-job-config-diff -m 2124252069`"
- Jakub Bester: "I tried running this command, but it fails with error: Job name mvai-training-online-2124252069 is not in uniqueness cache, error: HpcSchedulerErrorCode::JOB_NOT_FOUND (errorCode: 407)" [workaround failed — model may not have been registered yet]

## Root cause
`skip_recurring: True` (default) in the `online_train_publish` step's `workflow_input_override` causes `recurring_train.py` to take an early-return path before TLS config diff generation, so the step returns SUCCEEDED but produces no OT registration diff. **Matches P36 exactly.**

## Mitigation
Fix: add `"skip_recurring": False` to `online_train_publish` step's `workflow_input_override` in run_config.py; re-run run_loop.py. Quick workaround (`mvai online-training-mgr generate-job-config-diff -m 2124252069`) failed for this user with JOB_NOT_FOUND — possibly the MAST job was not registered in the scheduler cache yet. Resolution outcome not confirmed (aged out).

## Confidence: 0.5
Root cause P36 match is clear from 🤖 agent diagnosis. Mitigation direction solid (P36 runbook). However the workaround failed for the user and no follow-up fix confirmed. Degraded=true (heuristic_aged_7d). Capping at 0.5 per protocol for check-8 heuristic posts.

## References
- Post: https://fb.workplace.com/groups/mrs.ot/permalink/1215710353808301/
- Linked artifacts: S656611 (SEV), f1078183542 (flow), f1078311961 (flow), mvai-training-online-2124252069 (MAST job)
- Pattern: MATCH P36 (online_train_publish silent success via skip_recurring=True)
