# W1319356410159102 — Delta Update Failure

- **Identity:** lane=mast_job_id, author=Gabriel Chen, source_oncall=mrs_online_training, opened=2026-05-06T16:31Z, resolved=2026-05-14T02:38Z (aged out), duration=7d 0h
- **Resolution signal:** Check 8 (aged out — publish_time_epoch 1778110285 + 7×86400 = 1778715085 = 2026-05-14 02:38 UTC; no comment within last 48h indicating active investigation)

## Timeline
- 16:31 UTC May 6 — posted by Gabriel Chen (FB Reels I2I reranker in SilverTorch; first delta update with new unpooled UIH embedding table fails: "Not all sparse embeddings are mapped. Expected 11, got 10")
- 17:22 UTC May 6 — Butterfly synced to T269856176
- 19:12 UTC May 6 — Denny Zhang: diagnosis via Claude: "NEW TABLE NOT IN SERVING MODEL_PARAM_CONFIG. `_get_model_param_config_for_sparse` reads from serving predictor's old config (10 embeddings); new training model has 11. FQN→weight_id mapping fails for 11th table → RuntimeError. Model-side issue."
- 19:20 UTC May 6 — Gabriel Chen: "I assume it is an infra side issue because the checkpoint does have the new embedding table I added as expected but the delta update still failed. Let me take a look at your latest update."
- 02:38 UTC May 14 — (heuristic) 7-day age threshold reached; no further activity.

## Post body (full)
```
# Delta Update Failure

Hi MRS Online Training oncall.

**What are you trying to accomplish at high level?**
The model is Facebook Reels recommendation I2I reranker model published in SilverTorch.
The prod baseline model has pooled UIH feature embedding tables only.
I'm adding an unpooled UIH feature embedding table to the model for the first time
to prepare for UIH modeling changes.

**What's the issue you are running into?**
The online training job (mvai-training-online-2124629126) with the new unpooled UIH
feature embedding table failed the first delta update:
RuntimeError: Not all sparse embeddings are mapped. Expected 11, got 10

The checkpoint in Manifold DOES contain the new unpooled embedding table.

**Links:** mvai-training-online-2124629126, mvai-training-online-2124629131
```

## Comments thread (relevant excerpts)
- Denny Zhang (19:12 May 6): "Standing hypothesis: NEW TABLE NOT IN SERVING MODEL_PARAM_CONFIG. `_get_model_param_config_for_sparse` (utils.py:641) reads predict_fqn_to_weight_id from the serving predictor's model_param_config in Manifold — written when predictor last loaded a snapshot of the OLD model (10 embeddings). New training model has 11. FQN→weight_id mapping fails for 11th table → RuntimeError. Actions: 1) Disable delta-only mode, trigger FULL_SNAPSHOT first; 2) Verify model_param_config updated with 11 weight_id entries; 3) Re-enable delta-only publishing."
- Gabriel Chen (19:20 May 6): "I assume it is an infra side issue because the checkpoint does have the new embedding table I added as expected but the delta update still failed. Let me take a look at your latest update."

## Root cause
When adding a new embedding table (pooled→unpooled, 10→11 tables), the serving predictor's `model_param_config` in Manifold still reflects the old architecture. Delta updates require `_get_model_param_config_for_sparse()` to map all embedding FQNs to weight_ids — new 11th table missing from predictor's config → RuntimeError: "Not all sparse embeddings are mapped. Expected 11, got 10."

## Mitigation
Proposed (Denny Zhang, unconfirmed resolution): Disable delta-only mode → trigger FULL_SNAPSHOT first → predictor loads new architecture → writes new model_param_config with 11 weight_id entries → re-enable delta publishing. Actual resolution not confirmed by author (post aged out with last comment "Let me take a look").

## Confidence: 0.5
Check 8 (aged out, 7d). Root cause articulated by Denny Zhang's bot-assisted diagnosis; Gabriel Chen did not confirm resolution. Mitigation is proposed, not confirmed. Validator should independently check if the post or linked task T269856176 has been updated since May 6.

## References
- Post: https://fb.workplace.com/groups/mrs.ot/permalink/1319356410159102/
- Linked artifacts: mvai-training-online-2124629126, mvai-training-online-2124629131 (SilverTorch I2I reranker)
- Task: T269856176
